# 施工日志：tree-space 底座化与 Workspace 架构重构

## 2026-09-04：立项

- 用户确认 cistella 底层直接构建在 tree-space 上。
- 确认每个业务库作为 Workspace 的一棵子树，并通过插件机制接入。
- 确认 Arrow 与 Opaque 双形态并存，legacy Vault 进入迁移兼容线。

## 2026-09-04：设计细化

- 将后续工作拆分为插件层、聚合门面、迁移兼容、前端映射四个下游工单。
- 固化依赖顺序：18 → 19 → 20 → 21 → 22。
- 补齐 A/B/C 决策、数据分类、阶段门和验收边界。

## 下一步

完成 Workspace Tree 拓扑与新旧模型对照后，提交经理终审；通过后开启工单 19施工。


## 2026-09-05：阶段门通过，进入施工循环

- 用户确认可以开始施工循环。
- 经理判定 A 层方向问题已完成必要确认：tree-space 作为唯一底座，Workspace Tree 作为聚合根，legacy Vault 进入迁移兼容线。
- 工单 18 状态从构建中切换为施工中，先执行 M1：Workspace/Library/Vault 语义与树拓扑收口。
- 下游工单 19—22 仍保持构建中，等待工单 18 输出稳定契约。

## 2026-09-05：M1—M3 连续施工与终审

### M1：语义边界与树拓扑

- 固化 Workspace 为产品级聚合根。
- 固化 Library 为 Workspace 下的领域子树。
- 固化 Vault 为 legacy 兼容输入，不再作为新写入目标。
- 固化顶层子树：literature、references、writing、assets、analysis、imports、system、cache。

### M2：新旧模型映射

- 完成 legacy `manifest.json`、`tables/`、`user/`、`files/`、`derived/search`、`exports/` 到 Workspace Tree 的映射。
- 固化 authority 数据必须迁移；derived/cache 可重建；Opaque 资产保真优先。
- 固化 dry-run、临时目标、校验、发布、回滚的迁移原则。

### M3：下游契约

- 为工单 19 固化插件命名、版本、能力和 Arrow/Opaque block 要求。
- 为工单 20 固化聚合门面最小 API 和 DTO 隔离原则。
- 为工单 21 固化迁移兼容硬性流程。
- 为工单 22 固化前端只消费业务 DTO、不暴露底层布局的约束。

### 终审

经理终审通过：工单 18 达成，可作为工单 19—22 的稳定前置。下一步建议进入工单 19：cistella tree-space 插件层。