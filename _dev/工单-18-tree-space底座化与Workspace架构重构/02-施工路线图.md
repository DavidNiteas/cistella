# 施工路线图：tree-space 底座化与 Workspace 架构重构

## 1. 里程碑

| 里程碑 | 状态 | 施工内容 | 结果 |
|---|---|---|---|
| M1 | ✅ 已完成 | 收口 Workspace/Library/Vault 语义与树拓扑 | A 层通过 |
| M2 | ✅ 已完成 | 完成 authority/derived/cache/import 分类和新旧映射 | B 层通过 |
| M3 | ✅ 已完成 | 固化插件、聚合、迁移、前端的接口边界 | 工单 19—22 准入 |

## 2. 已执行步骤

1. 盘点现有 Vault、Source、Library 语义及代码入口。
2. 绘制 Workspace Tree 顶层与领域子树拓扑。
3. 建立数据分类和生命周期矩阵。
4. 形成 crate 依赖图、注册协议和上层门面边界。
5. 完成经理终审，工单 18 作为后续工单稳定前置。

## 3. 验收标准

- ✅ 文档中不存在 Workspace、Vault、Library 的含义重叠。
- ✅ 每类现有数据都有唯一目标子树和 authority 归属。
- ✅ 新增领域只需新增领域 crate 与注册项，不修改 tree-space 底座。
- ✅ 前端和 desktop 只依赖聚合门面，不依赖 tree-space 内部布局。
- ✅ 工单 19—22 均获得可执行输入契约。

## 4. 风险与偏离处理

- 若 tree-space 当前 API 无法覆盖某一落位，登记偏离申请；不得在领域层复制一套树/桶实现。
- 若 Opaque block 无法验证或恢复，先保留原始材料并暂停该类迁移，不得静默丢弃。
- 若 crate 拆分导致循环依赖，优先调整边界，不通过跨层反向依赖解决。

## 5. 阶段门结果

- 构建中 → 施工中：已通过，用户确认开始施工循环。
- M1 → M2：已通过，树拓扑和术语边界已固化。
- M2 → M3：已通过，新旧模型映射和迁移原则已固化。
- M3 → 达成：已通过，下游契约已固化。

## 6. 后续引用

- 工单 19：引用本工单第 10.1 节作为插件层输入。
- 工单 20：引用本工单第 10.2 节作为聚合门面输入。
- 工单 21：引用本工单第 9 节和第 10.3 节作为迁移兼容输入。
- 工单 22：引用本工单第 10.4 节作为前端映射输入。