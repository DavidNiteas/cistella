# 03. 开发状态：文献资产与收纳中心

## 当前阶段

施工中

## 阶段门结论

- 2026-08-26：工单 08 通过阶段门。
- A 层：文献资产层作为阅读、检索、笔记和导入的共同基础。
- B 层：DocumentAsset 独立记录、SHA-256、同条目重复规则、库内路径、外部链接迁移和删除边界已拍板。
- C 层：按 M1—M4 逐项执行；每个里程碑必须经过实施、不同干事独立初审、必要返工复审，上一里程碑通过后才放行下一里程碑。

## 施工里程碑

- M1：DocumentAsset 模型与兼容读取。**实施完成，独立初审通过**
- M2：Vault 收纳、哈希与重复处理。**实施完成，独立初审通过**
- M3：资产管理与外部链接迁移。**经理终审通过**
- M4：回归、迁移与 portable 验收。**实施完成，待独立初审**

## M1 验证证据

- 新增 `DocumentAsset`、资产类型/存储方式/运行时状态模型。
- 新增 `user/document_assets.json` 原子读写 API。
- `document_assets.json` 存在时为唯一权威；不存在时从工单 06/07 的 `LiteratureItem.files/default_file_id` 生成稳定兼容资产视图；不自动重写旧文件。
- M1 独立初审已核验模型、枚举序列化、权威顺序、旧字段投影、状态重算和原子失败清理。
- M1 验收：28 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，Doc-tests 通过，0 failed。

## M2 验证证据

- 新增 `Vault::import_document_asset` 与 `DocumentAssetImportResult`。
- 收纳前验证源路径存在、为普通文件且扩展名为 PDF；仅首版 PDF 纳入范围。
- 使用 128 KiB 分块读取计算 SHA-256，哈希保存为十六进制字符串；哈希不替代 `asset_id`。
- 库内目标固定为 `files/<item_id>/<asset_id>-<safe_filename>.pdf`，目标创建与发布均不覆盖既有文件。
- 同一条目内相同 `content_hash` 返回 `Duplicate { existing }`，不复制第二份文件、不新增资产记录；不同条目不自动合并。
- 文件复制、哈希或用户资产 JSON 发布失败时清理本轮临时物；用户数据和既有资产不被破坏。
- M2 专门测试覆盖：成功收纳与字段、SHA-256、item-scoped 路径、同条目重复、防覆盖失败语义、非 PDF/目录拒绝、发布失败清理。
- `cargo test -p cistella-core`：31 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，Doc-tests 通过，0 failed。
- M2 独立初审已核验收纳路径、分块 SHA-256、同条目重复处理、输入拒绝、失败清理、默认资产和 M1 兼容；经理已裁定允许进入 M3。

## M3 验证证据

- Core 提供外部 PDF 资产创建、外部资产显式迁移到 Vault、资产类型修改、明确默认项、只移除资产记录和 `item_id + asset_id` 受控路径解析；旧 `file_id` API 保留为兼容入口，不作为新的主入口。
- 外部迁移成功时创建新的 Vault 资产并保留原外部链接；若同条目已有同内容 Vault 资产则返回明确的 `Duplicate`，不创建第二个副本；移除操作不删除外部文件或 Vault 内物理文件。
- Tauri 新增 `list_document_assets`、`import_document_asset`、`link_external_document_asset`、`set_document_asset_kind`、`set_document_asset_default`、`remove_document_asset`、`migrate_external_document_asset` 和 `open_document_asset`；打开命令只接收并验证 `item_id + asset_id`，随后才请求系统默认应用打开文件。
- Reading 与 Vault 工作区均以 `asset_id` 加载和操作资产，显示资产类型、存储方式、大小、SHA-256 摘要、路径与运行时健康状态；支持收纳、外部链接、类型修改、设默认、受控打开、外链复制入库和只移除记录。失败反馈保留当前列表状态。
- `cargo fmt --all` 通过。
- `cargo test -p cistella-core`：33 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，Doc-tests 通过，0 failed；包含外部迁移保留原链接、资产类型/默认项和移除不删物理文件的 M3 覆盖。
- `cargo check -p cistella-desktop` 通过，退出码 0。
- `pnpm build` 通过；Vite ��提示产物单块超过 500 kB，未造成构建失败。
- M3 首次独立初审**不通过**：删除 `LiteratureItem` 时未同步清理同 `item_id` 的 `DocumentAsset` 记录，正常删除路径会留下不可见、不可管理的孤儿资产记录；M4 已被阻断。
- C 层返工裁定：存在 `document_assets.json` 时先发布已过滤的资产记录、再发布条目删除；第二次写入失败时补偿恢复原资产记录。补偿也失败时保留条目、允许资产记录暂缺，绝不形成“条目已删而资产仍在”的孤儿状态；任何 Vault 或外部物理文件均不删除。尚未存在新资产文件的旧库仍只写 `literature_items.json`，不因删除旧条目创建新的权威资产文件。
- 新增专项测试覆盖：普通删除同时移除关联资产记录且保留 Vault 物理文件；资产 JSON 首写失败时不写条目；条目 JSON 次写失败时恢复原资产；补偿写也失败时条目保留且资产记录为空的安全状态。
- 返工验收：`cargo fmt --all`、`cargo test -p cistella-core`（37 个 core 单元测试、工单 07 portable 集成测试 1 个、Doc-tests，0 failed）、`cargo check -p cistella-desktop` 和 `pnpm build` 均通过；Rust incremental 目录拒绝访问与 Vite 单块超过 500 kB 均为既有非阻断提示。
- M3 **返工完成，待不同干事复审**；原初审干事不得承担本次复审，M4 仍不得放行。

## 已知边界

- 首版只收纳 PDF。
- 不包含全文提取、阅读会话、笔记、标注、同步或外部元数据自动合并。
- M2 不修改 Tauri、前端、Source 分析表或顶层治理文件。

## M3 经理终审证据

- 2026-08-26：返工复审干事通过；其只读复核了删除条目时资产记录同步清除、物理文件保留、资产写失败中止、条目写失败补偿、补偿失败的无孤儿安全状态，以及既有 asset ID 闭环。
- 2026-08-26：经理独立复跑 cargo test -p cistella-core、cargo check -p cistella-desktop、pnpm build，全部退出 0。core 37 项单元测试、工单 07 portable 集成测试和 Doc-tests 均通过；Rust incremental 拒绝访问、Vite 单块大于 500 kB 均为不影响退出码的既有提示。
- 经理裁定：M3 关闭，通过；允许按路线图开启 M4，但工单 08 尚未终审归档。


## M4 验证证据

- 新增公共 API 集成测试：`crates/openalex-analysis-core/tests/工单08_m4_文献资产portable.rs`；测试仅通过导出的 `Vault`、`DocumentAsset` 与文献模型 API 驱动真实临时 Vault，不依赖前端或私有测试钩子。
- portable 回归：先收纳库内 PDF、再完整复制 Vault 并重新打开副本；断言库内资产仍以相对路径记录、能通过 `item_id + asset_id` 受控解析到副本根目录内的文件，且健康状态为 `available`。
- 同条目 SHA-256 去重：对同一源 PDF 进行第二次收纳，断言返回 `Duplicate`、资产 JSON 不增加记录、`files/<item_id>` 中的物理文件数不增加。
- 外部链接边界：断言外部资产记录为绝对路径和 `external` 存储方式，复制 Vault 后仍指向 Vault 外；删除外部文件后状态为 `external_unavailable` 且无法解析打开。
- 库内缺失边界：删除复制 Vault 中的库内 PDF 后，状态为 `missing`、解析失败，并断言运行时状态检查不改写 `document_assets.json`。
- 旧 JSON 兼容：只写工单 07 的 `literature_items.json`/`files`，读取资产视图时沿用旧 `file_id` 作为稳定 `asset_id`、可解析旧库内文件；全过程不创建 `user/document_assets.json`，也不重写旧 JSON。
- 失败清理与用户数据保护：Windows 集成测试真实持有 `document_assets.json` 打开句柄以阻断原子替换，断言收纳失败后新复制的 Vault PDF 与临时文件均清理、资产 JSON 与文献 JSON 字节保持原样、资产记录不增加。既有跨平台 core 单测继续覆盖注入式发布失败清理。
- 数据隔离：M4 测试在资产操作前后逐字节比较 `manifest.json` 与 `tables/sources.parquet`，并断言 manifest 不含用户资产字段；用户资产只出现在 `user/` 与 `files/`。
- 2026-08-26：执行 `cargo fmt --all`，通过。
- 2026-08-26：执行 `cargo test -p cistella-core`：37 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，工单 08 M4 资产集成测试 3 个通过，Doc-tests 通过，0 failed。
- 2026-08-26：执行 `cargo check -p cistella-desktop`，退出码 0；执行 `pnpm build`，TypeScript/Vite 构建通过。Rust incremental 目录拒绝访问与 Vite 单块大于 500 kB 仅为既有非阻断提示。
- M4：**实施完成，待不同干事独立初审**。本次无功能范围偏离；不得据此自行终审、归档工单 08 或开启工单 09—12。