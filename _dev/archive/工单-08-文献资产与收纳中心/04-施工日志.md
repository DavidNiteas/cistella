# 04. 施工日志：文献资产与收纳中心

- 2026-08-26：用户授权经理连续推进工单 08—12，并要求每个工单严格执行实施、独立初审、返工复审和经理终审；工单之间不得跨单并行放行。
- 2026-08-26：经理完成工单 08 A/B 设计收口；工单 08 通过阶段门，进入 M1 施工。

## M1

- 2026-08-26：M1 实施收口完成。新增 `DocumentAsset`、资产类型/存储方式/运行时状态模型，以及 `user/document_assets.json` 原子读写 API。
- 2026-08-26：明确兼容策略：`document_assets.json` 存在时为唯一权威；不存在时从工单 06/07 的 `LiteratureItem.files/default_file_id` 生成稳定资产视图；不自动重写旧文件。
- 2026-08-26：补齐 M1 专门测试，覆盖新文件权威、旧字段映射、JSON 往返与临时文件清理、类型/存储方式序列化、available/missing/invalid/external_unavailable 状态重算。
- 2026-08-26：M1 自测通过：28 个 core 单元测试、工单 07 portable 集成测试 1 个、Doc-tests 均通过，0 failed。
- 2026-08-26：M1 独立初审通过。初审核验 DocumentAsset 模型、枚举序列化、`document_assets.json` 权威顺序、旧 `files/default_file_id` 兼容投影、运行时状态计算和原子失败清理；经理裁定允许进入 M2。

## M2

- 2026-08-26：M2 开始施工，范围冻结为 Vault 收纳、SHA-256 哈希、同条目重复处理和失败清理；不修改 Tauri、前端、顶层治理文件，不提前实现 M3/M4。
- 2026-08-26：新增 `Vault::import_document_asset(item_id, source_path, asset_kind)`，返回可判别的 `DocumentAssetImportResult`：`Imported { asset }` 或 `Duplicate { existing }`。
- 2026-08-26：收纳前验证源文件存在、为普通文件且为 PDF；使用 128 KiB 分块读取计算 SHA-256，记录文件大小、媒体类型、导入时间和内容哈希。
- 2026-08-26：库内文件按 `files/<item_id>/<asset_id>-<safe_filename>.pdf` 落位；复制经过同目录临时物，发布拒绝覆盖既有目标；目标名包含独立 `asset_id`，不使用路径或哈希作为资产主标识。
- 2026-08-26：同一条目内发现相同内容哈希时返回 `Duplicate`，不创建第二个物理文件、不新增资产记录；不同条目之间不自动合并。
- 2026-08-26：复制、哈希或用户资产 JSON 发布失败时清理本轮临时物/副本；既有资产记录和既有物理文件保持不变。新增测试覆盖用户数据发布失败后的清理语义。
- 2026-08-26：M2 专门测试覆盖成功收纳、SHA-256、路径格式、首个资产默认标记、同条目重复不变更、非 PDF/目录拒绝和发布失败清理。
- 2026-08-26：执行 `cargo test -p cistella-core`：31 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，Doc-tests 通过，0 failed；编译仅有既有 Windows incremental 目录拒绝访问提示，不影响退出码 0。
- 2026-08-26：M2 实施完成，状态回填为“实施完成，待独立初审”；现正式交付不同干事进行只读独立初审，未放行 M3。

- 2026-08-26：M2 独立初审通过：收纳路径、分块 SHA-256、同条目重复不复制、跨条目不合并、输入拒绝、失败清理、默认资产和 M1 兼容均已核验；cargo test -p cistella-core 31 项通过。经理裁定允许进入 M3。

## M3

- 2026-08-26：M3 范围冻结为资产管理与外部链接迁移；不进入 M4，不开启工单 09—12，不改顶层治理文件。
- 2026-08-26：补齐/收口 Core 资产操作：创建外部 PDF 链接、将外部链接显式复制为新的 Vault 资产、修改资产类型、明确设置默认资产、只移除资产记录，以及由 `item_id + asset_id` 受控解析可打开路径。
- 2026-08-26：外部迁移复用 Vault 收纳与同条目哈希规则；成功时保留原外部资产记录与原外部物理文件，重复时返回明确 `Duplicate`，不新增副本；移除记录不删除任何物理文件。
- 2026-08-26：增加并注册资产 Tauri 命令：列出、收纳、链接外部、修改类型、设默认、移除、迁移和受控打开。`open_document_asset` 不接收任意路径，而是验证 `item_id + asset_id` 后交由系统默认应用处理；旧 `file_id` 命令保持兼容。
- 2026-08-26：Reading 与 Vault 资产主入口切换为 `asset_id`；列表显示类型、存储方式、大小、SHA-256 摘要、路径与运行时健康状态，操作覆盖收纳、外部链接、改类型、设默认、受控打开、外链复制入库和只移除记录。Vault 资产中心同时显示成功/重复/失败反馈；补齐资产中心布局和窄窗口样式。
- 2026-08-26：执行 `cargo fmt --all`，通过。
- 2026-08-26：执行 `cargo test -p cistella-core`：33 个 core 单元测试通过，工单 07 portable 集成测试 1 个通过，Doc-tests 通过，0 failed。
- 2026-08-26：执行 `cargo check -p cistella-desktop`，通过，退出码 0。
- 2026-08-26：执行 `pnpm build`，通过；Vite 仅报告既有的单块产物大于 500 kB 警告，不影响退出码。
- 2026-08-26：M3 实施完成，状态回填为“实施完成，待独立初审”；现正式交付不同干事进行只读独立初审，未放行 M4。
- 2026-08-26：M3 独立初审**不通过**。初审发现：删除 `LiteratureItem` 只更新 `literature_items.json`，未同步清除同 `item_id` 的 `document_assets.json` 记录；前端仅按现存条目展示资产，故正常删除会遗留不可见、不可管理的孤儿资产。经理已阻断 M4，并按 C 层裁定返工边界：删除条目同步移除关联资产记录、绝不删除 Vault 或外部物理文件；任一用户 JSON 写入失败均不得留下“条目已删除但资产记录仍在”的状态。
- 2026-08-26：M3 返工完成。实现采用资产记录先发布、条目后发布的可恢复顺序：首个资产 JSON 写入失败时条目不写；条目 JSON 写入失败时补偿恢复原资产记录；补偿也失败时保留条目而不保留其资产记录，因此仍无孤儿资产。旧兼容库若尚无 `document_assets.json`，则仅更新含旧文件关联的 `literature_items.json`，不凭删除动作创建新权威资产文件。
- 2026-08-26：新增专项测试覆盖普通删除、关联资产移除、Vault 物理文件保留、资产写入失败、条目写入失败补偿、补偿失败安全状态。执行 `cargo fmt --all`、`cargo test -p cistella-core`（37 个 core 单元测试 + 工单 07 portable 集成测试 1 个 + Doc-tests，0 failed）、`cargo check -p cistella-desktop`、`pnpm build` 均通过；Rust incremental 目录拒绝访问与 Vite 单块 >500 kB 为既有非阻断提示。
- 2026-08-26：M3 状态回填为“返工完成，待不同干事复审”。原初审干事不得承担本次复审；M4 继续冻结。

- 2026-08-26：M3 返工复审通过。不同于首审和返工实施者的复审干事已核验资产先写、条目后写、条目写失败补偿、补偿失败无孤儿的安全路径，及新 asset_id 主入口、受控打开、外链迁移与资产中心显示。
- 2026-08-26：经理 M3 终审通过。经理独立复跑 cargo test -p cistella-core（37 个 core 单元测试、工单 07 portable 集成测试、Doc-tests，0 failed）、cargo check -p cistella-desktop 与 pnpm build，均通过。Rust incremental 拒绝访问与 Vite 单块 >500 kB 为既有非阻断提示。经理裁定关闭 M3、正式开启 M4；工单 08 仍未归档。

## M4

- 2026-08-26：M4 仅执行回归、迁移与 portable 验收；未修改顶层治理文件，未开启工单 09—12，未扩展 M3 功能范围。
- 2026-08-26：新增 `crates/openalex-analysis-core/tests/工单08_m4_文献资产portable.rs`。公共 API 集成用例完整复制临时 Vault 后重新打开副本，验证库内 `DocumentAsset` 通过 `item_id + asset_id` 解析到副本内文件；外部链接保持绝对 Vault 外路径，删除外部目标后报告 `external_unavailable`。
- 2026-08-26：同一集成用例验证同条目相同 SHA-256 返回 `Duplicate`，不新增资产记录或库内副本；移除复制 Vault 中的库内 PDF 后报告 `missing`，且状态重算不改写资产记录。
- 2026-08-26：新增旧 JSON 兼容集成用例：仅存在旧 `literature_items.json` 时将 `files/default_file_id` 只读投影为稳定资产视图，可用资产 ID 解析旧库内文件，全程不创建 `user/document_assets.json`、不重写旧 JSON。
- 2026-08-26：新增 Windows 真实失败路径集成用例：持有已存在的 `document_assets.json` 文件句柄阻断原子替换，收纳失败后断言复制文件和临时文件清理、两份用户 JSON 字节不变、资产记录不增加；既有跨平台 core 单测仍覆盖注入式发布失败。
- 2026-08-26：M4 同时断言资产操作前后 `manifest.json` 与 `tables/sources.parquet` 字节相同，manifest 不含用户资产元数据，确认 Source/manifest 不承载用户资产数据。
- 2026-08-26：执行 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo check -p cistella-desktop`、`pnpm build` 均通过。core 为 37 个单元测试、工单 07 portable 集成测试 1 个、工单 08 M4 集成测试 3 个以及 Doc-tests，0 failed；Rust incremental 拒绝访问和 Vite 单块大于 500 kB 为既有非阻断提示。
- 2026-08-26：M4 状态真实回填为“实施完成，待独立初审”。偏离申请：无；M4 不得自行放行终审、归档或后续工单。