# 03. 开发状态：在线文献元数据补全与网络数据源适配

## 当前阶段

工单 13 M1/M2/M3 已全部施工、独立初审与经理终审通过，结论 PASS；工单 13 已达成，待归档。

## 设计输入与决议登记

本工单设计基于经理于 2026-08-31 的立项决策，A/B/C 三层决议已写入 `01-目标与设计.md`，核心摘要如下：

### A 层方向

- 工单 13 目标：在工单 12 基础上扩展在线文献元数据补全与网络数据源适配能力。
- 范围边界：M1 做 Crossref DOI 解析；M2 做 PubMed/Europe PMC；M3 做 OpenAlex API / Semantic Scholar 与多解析器择优。
- 核心原则：本地优先、网络数据作为补全来源、可禁用/可缓存/可错误回退。

### B 层设计

- 抽象：`RemoteMetadataResolver` trait + `RemoteResolverRegistry` 标识符分派。
- 缓存：`<Vault>/cache/remote_metadata/<namespace>/<sha256(identifier)>.json`，默认 30 天过期。
- 网络层：`reqwest` 异步 HTTP，默认 10 秒超时、1 次重试。
- 与工单 12 集成：解析结果转为 `SourceRecord`，复用 `LibraryImporter` 预览/提交。
- 错误策略：`RemoteMetadataUnavailable`、`RemoteMetadataNotFound`、`RemoteMetadataMalformed`。

### C 层默认

- 标识符规范化：DOI/PMID/PMCID/ISBN/OpenAlex ID。
- 解析器顺序：DOI → Crossref（M1）→ Semantic Scholar/OpenAlex API（M3）。
- Tauri 命令：`resolve_remote_metadata`、`import_by_identifier`。
- UI 入口：Reading 工作区「通过标识符导入」。
- 测试：本地 mock/录制响应，覆盖缓存、错误、端到端导入。

## 施工里程碑

- M1：Crossref DOI 解析。🟢 已完成并通过经理终审（2026-08-31）
- M2：PubMed / Europe PMC。🟢 已完成并通过经理终审（2026-08-31）
- M3：OpenAlex API / Semantic Scholar 与择优。🟢 已完成并通过经理终审（2026-08-31）

## 验证证据

- 阶段门：2026-08-31，经理拍板 A/B/C 决议并授权撰写四件套。
- 四件套文档已在本目录落地：
  - `01-目标与设计.md`
  - `02-施工路线图.md`
  - `03-开发状态.md`
  - `04-施工日志.md`
- M1 代码已落地，新增/修改文件见 `04-施工日志.md`。
- M1 验收命令结果（因本机 page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo test -p cistella-core --lib`：100 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `cargo check -p cistella-desktop`：通过
  - `pnpm build`：通过
- 真实 Crossref API 手动验证：`curl -A "cistella/0.1.0 (mailto:contact@example.com)" https://api.crossref.org/works/10.1038/s41586-021-03819-2` 返回 200 与 JSON 响应。

## M2 施工验证证据（待经理终审确认）

- 施工日期：2026-08-31
- 新增/修改文件：
  - `crates/openalex-analysis-core/src/import/remote/pubmed.rs`（新增）
  - `crates/openalex-analysis-core/src/import/remote/identifier.rs`（PMID/PMCID 规范化增强）
  - `crates/openalex-analysis-core/src/import/remote/registry.rs`（PubMedResolver 注册与测试构造函数）
  - `crates/openalex-analysis-core/src/import/remote/mod.rs`（导出 `PubMedResolver`）
  - `crates/openalex-analysis-core/src/lib.rs`（导出 `PubMedResolver`）
  - `crates/openalex-analysis-studio/src/App.tsx`（标识符类型自动检测提示）
  - `crates/openalex-analysis-core/tests/工单13_m2_pubmed_pmcid解析.rs`（新增集成测试）
- 新增依赖：无（继续复用 M1 已声明的 `reqwest` 与 `tokio`）。
- 验收命令结果（因 Windows page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo test -p cistella-core --lib`：104 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m2_*'`：8 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `cargo check -p cistella-desktop`：通过
  - `pnpm build`：通过（仅 chunk size 警告，与 M1 一致）
- 偏离申请：无。

## M2 经理终审记录

- 终审日期：2026-08-31
- 终审人：经理
- 复跑验收命令（因本机 page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo check -p cistella-desktop`：通过
  - `cargo test -p cistella-core --lib`：104 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m2_*'`：8 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `pnpm build`：通过（仅 chunk size 警告）
- 审查结论：M2 交付物满足 `02-施工路线图.md` §M2 关闭标准，代码、测试、文档一致，不影响 M1 与工单 12 功能，终审 **PASS**。
- 放行决定：M2 闭合，准许召开 M3 阶段门。

## M1 经理终审记录

- 终审日期：2026-08-31
- 终审人：经理
- 复跑验收命令（因本机 page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo check -p cistella-desktop`：通过
  - `cargo test -p cistella-core --lib`：100 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `pnpm build`：通过（仅 chunk size 警告）
- 审查结论：M1 交付物满足 `02-施工路线图.md` 关闭标准，代码、测试、文档一致，不影响工单 12 导入功能，终审 **PASS**。
- 放行决定：M1 闭合，准许召开 M2 阶段门。

## M3 施工验证证据（待经理终审确认）

- 施工日期：2026-08-31
- 新增/修改文件：
  - `crates/openalex-analysis-core/src/import/remote/openalex_api.rs`（新增 OpenAlex API 解析器）
  - `crates/openalex-analysis-core/src/import/remote/semantic_scholar.rs`（新增 Semantic Scholar 解析器）
  - `crates/openalex-analysis-core/src/import/remote/registry.rs`（择优合并、配置感知构造、解析器顺序）
  - `crates/openalex-analysis-core/src/import/remote/mod.rs`（导出新增解析器）
  - `crates/openalex-analysis-core/src/import/remote/resolver.rs`（无修改，仅复用）
  - `crates/openalex-analysis-core/src/app_dirs.rs`（AppConfig 增加 `remote_resolvers_enabled`）
  - `crates/openalex-analysis-core/src/lib.rs`（导出新增类型）
  - `crates/openalex-analysis-core/Cargo.toml`（新增 `percent-encoding` 依赖）
  - `crates/openalex-analysis-studio/src-tauri/src/lib.rs`（切换 `resolve_best`、新增 `clear_remote_metadata_cache` 命令）
  - `crates/openalex-analysis-studio/src/App.tsx`（新增「清除在线解析缓存」按钮与国际化字符串）
  - `crates/openalex-analysis-core/tests/工单13_m3_多解析器择优.rs`（新增集成测试）
  - `crates/openalex-analysis-core/tests/工单14_m1_portable模式与数据边界.rs`（因 AppConfig 新增字段补充 `..Default::default()`，无逻辑变更）
- 新增依赖：`percent-encoding` 2.x（cistella-core）。
- 验收命令结果（因 Windows page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo test -p cistella-core --lib`：111 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m2_*'`：8 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m3_*'`：9 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `cargo check -p cistella-desktop`：通过
  - `pnpm build`：通过（仅 chunk size 警告，与 M1/M2 一致）
- 偏离申请：
  - 解析器启用/禁用配置通过 `RemoteResolverRegistry::from_app_dirs(&AppDirectories)` 在构建时读取，而非直接修改 `new()`；`new()` 保持默认全部启用以保证库测试无隐式文件系统依赖。该实现仍满足「构建时读取配置、只注册启用解析器」的要求，已在 `04-施工日志.md` 登记。
  - 择优错误优先级定为 `NotFound > Malformed > Unavailable > Other`，已在 `04-施工日志.md` 登记。

## M3 经理终审记录

- 终审日期：2026-08-31
- 终审人：经理
- 复跑验收命令（因本机 page file 限制，core/desktop 全量测试使用 `CARGO_BUILD_JOBS=1`）：
  - `cargo fmt --check`：通过
  - `cargo check -p cistella-desktop`：通过
  - `cargo test -p cistella-core --lib`：111 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m1_*'`：7 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m2_*'`：8 passed / 0 failed
  - `cargo test -p cistella-core --test '工单13_m3_*'`：9 passed / 0 failed
  - `cargo test -p cistella-core`：全部通过（lib + 所有 integration tests）
  - `cargo test -p cistella-desktop`：8 passed / 0 failed
  - `pnpm build`：通过（仅 chunk size 警告）
- 审查结论：M3 交付物满足 `02-施工路线图.md` §M3 关闭标准，代码、测试、文档一致，不影响 M1/M2 与工单 12 功能，终审 **PASS**。
- 放行决定：M3 闭合，工单 13 达成，准予归档。

## 阶段门裁定

- 经理于 2026-08-31 核验 01/02 的 A/B/C 决议、范围边界、网络抽象、缓存策略、与工单 12 集成方案、错误处理与 TBD 管理，阶段门通过。
- 经理于 2026-08-31 召开阶段门并授权 M1「Crossref DOI 解析」施工；M1 已施工、初审、终审通过并闭合。
- 经理于 2026-08-31 召开 M2 阶段门并授权 M2「PubMed / Europe PMC 适配」施工；M2 已施工、初审、终审通过并闭合。
- 经理于 2026-08-31 召开 M3 阶段门并授权 M3「OpenAlex API / Semantic Scholar 与择优」施工；M3 已施工、初审、终审通过并闭合。
- 工单 13 全部里程碑（M1/M2/M3）已达成，满足闭合条件，准予归档。
- 归档路径：`_dev/archive/工单-13-在线文献元数据补全与网络数据源适配/`。
