# 04. 施工日志：Vault 与库模型

## 施工日志

- 2026-08-25：建立工单骨架。
- 2026-08-25：在 core 中引入 vault 语义层，新增 Vault / VaultOpenOptions / VaultManifest 等主类型。
- 2026-08-25：桌面壳层内部状态从 dataset 语义切换为 vault 语义。
- 2026-08-25：query / import 路径进一步收束到 Vault / VaultManifest 语义。
- 2026-08-25：manifest 结构切换为 Vault 命名作为主命名。
- 2026-08-25：core 补出 VaultContext 摘要，供桌面侧读取路径、manifest 与表数量。
- 2026-08-25：桌面侧改为直接复用 core 提供的 VaultContext。
- 2026-08-25：manifest / table / provenance / derived data 的职责边界已补充到目标与路线图。
- 2026-08-25：验证桌面 Rust crate 的检查通过。

## 偏离申请

- 暂无。

## 验收记录

- Vault 语义层已经可以承接核心库对象。
- Dataset 旧语义仍以兼容别名方式保留。
- 库模型仍在持续收束中。

## 备注

该工单当前处于语义迁移与边界收口阶段。
- 2026-08-25：VaultManifest / VaultContext 的最小测试已补充。
- 2026-08-25：`cargo test -p cistella-core` 通过。

- 2026-08-25：收口 Vault 契约。`Vault::open` 读取 manifest 后统一校验 table file 路径；仅允许非空相对路径，拒绝绝对路径、根路径、盘符前缀和 `..` 路径段。`Vault::table_path` 在运行时再次校验并相对于 Vault 根目录拼接。
- 2026-08-25：在 `CoreError` 增加清晰的 `InvalidTablePath` 错误，补充正常相对路径、绝对路径、路径逃逸及运行时防御测试。
- 2026-08-25：明确 `Dataset` 及相关旧命名仅为施工期兼容层，不新增 Dataset 主 API；本次不引入多来源融合、不扩展 UI。
- 2026-08-25：执行 `cargo test -p cistella-core`，7 个单元测试和 0 个 Doc-tests 全部通过。

## 本次收口验收记录

- 路径契约：通过。manifest 中的 table file 路径按相对路径解释，不能逃逸 Vault 根目录，也不能使用绝对路径作为运行时依赖。
- `Vault::open`：通过。非法 table file 路径在打开阶段即失败。
- `Vault::table_path`：通过。合法路径解析为 `Vault 根目录 / 相对路径`，非法路径在运行时仍被拒绝。
- 范围控制：通过。未引入多来源融合，未修改 UI。
- 关闭建议：满足本次施工关闭标准，提交经理验收。

