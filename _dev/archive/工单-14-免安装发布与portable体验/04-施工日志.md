# 04. 施工日志：免安装发布与 portable 体验

- 2026-08-28：经理基于设计路线第 14 章「免安装发布与 portable 体验」立项工单 14，明确优先于工单 13 推进。
- 2026-08-28：经理拍板工单 14 的 A/B/C 三层决议，明确 M1 只做 portable 模式与数据边界，M2 做打包产物与迁移向导，M3 做发布流程与可选增强。
- 2026-08-28：本干事按四件套纪律完成 `01-目标与设计.md`、`02-施工路线图.md`、`03-开发状态.md`、`04-施工日志.md` 撰写；未实施任何代码，未进入 M1。
- 2026-08-28：经理通过阶段门并授权 M1「Portable 模式与数据边界」施工；M2/M3 仍未授权。
- 2026-08-30：施工干事完成 M1 实施：新增 `cistella-core/src/app_dirs.rs` 与相关错误变体；Tauri 层新增 `app_directories`、`recent_vaults`、`update_recent_vaults`、`is_portable_mode`、迁移命令并注册；UI 在 Vault 工作区与设置页显示当前模式、后端最近库列表与迁移入口；新增集成测试 `crates/openalex-analysis-core/tests/工单14_m1_portable模式与数据边界.rs`。全部验收命令通过，状态登记为「待独立初审」。
- 2026-08-30：M1 独立初审结论 PASS（附 P2 返工项）。阻塞/必须修复：P2-2 测试未真正验证相对路径存储；建议同轮修复：P2-1（Core 层路径转换封装）、P2-4（唯一目标目录碰撞上限）。P2-3/P2-5 延后。经理确认返工范围。
- 2026-08-30：返工干事完成 P2 修复：P2-2 修正相对路径存储测试并新增 Core 层封装闭环测试；P2-1 在 Core 层新增 `AppDirectories::save_recent_vaults` 强制 portable 模式相对路径不变量，并同步简化 Tauri `update_recent_vaults` 命令；P2-4 为 `unique_migration_target` 增加 10_000 次循环上限。P2-3/P2-5 维持延后。全部验收命令通过，M1 重新进入待独立初审状态。
- 2026-08-30：M1 独立初审复审结论 PASS；经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 91、M1 集成 10、core 全量、desktop 8、check、pnpm build。M1 完成。
- 2026-08-30：经理召开阶段门并授权 M2「打包产物与迁移向导」施工；M3 仍未授权。
- 2026-08-30：施工干事完成 M2 实施：新增 `cistella-core/src/backup.rs` 与 `CoreError::Zip`；Tauri 层新增 `backup_vault`、`restore_vault` 命令并注册；UI 在设置页增加迁移向导对话框、备份当前库与从备份恢复按钮；Tauri 打包配置启用 `msi`/`nsis` 并通过 `scripts/make-portable.js` 生成 portable zip；新增集成测试 `crates/openalex-analysis-core/tests/工单14_m2_备份恢复与迁移向导.rs`。全部验收命令通过，状态登记为「待独立初审」。
- 2026-08-30：M2 独立初审结论 **NEEDS REWORK**。初审发现 P1 阻塞项 2 项、P2 建议项 3 项、P3 文档项 1 项。经理确认返工范围：必须修复 P1-1/P1-2；建议同轮修复 P2-1/P2-3；P2-2 延后；P3 随 P1 修复同步更新。具体见 `03-开发状态.md` 中「M2 独立初审记录」。
- 2026-08-30：返工干事完成 M2 返工：修复 P1-1（`bundle.icon` 改为 `["icons/icon.ico"]`）、P1-2（`[[bin]] name` 改为 `cistella`）、P2-1（迁移向导根据当前模式禁用同模式选项并显示提示）、P2-3（迁移/备份/恢复后端错误回显在弹窗/设置页）、P3（同步 02/03 文档描述）。全部既有验收命令复验通过；`pnpm --dir crates/openalex-analysis-studio tauri build` 成功生成 msi + nsis；`node crates/openalex-analysis-studio/scripts/make-portable.js` 成功生成 portable zip。M2 重新进入待独立初审状态。
- 2026-08-30：M2 独立初审复审结论 PASS；经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 93、M1 集成 10、M2 集成 5、core 全量、desktop 8、check、pnpm build、tauri build（msi/nsis）、make-portable.js（zip）。M2 完成。
- 2026-08-30：经理召开阶段门并授权 M3「发布流程与可选增强」施工。
- 2026-08-30：施工干事完成 M3 实施：新增 `cistella-core/src/release.rs` 与 `CoreError::UpdateCheckFailed`；Tauri 层新增 `get_app_version`、`check_update` 命令并注册；UI 在设置页显示当前版本与更新状态；新增 `update.json`、集成测试 `crates/openalex-analysis-core/tests/工单14_m3_发布流程与可选增强.rs`、回归脚本 `crates/openalex-analysis-studio/scripts/release-smoke-test.js`、发布检查清单与单文件 exe 评估文档。全部验收命令通过，状态登记为「待独立初审」。
- 2026-08-30：M3 独立初审结论 **NEEDS REWORK**。P1-1：`make-portable.js` 改用 `tar -acf` 后产物扩展名为 `.zip` 但实际为 POSIX tar 归档，不是 Windows 可解压的 zip；必须修复为生成真正 zip 并保留 `cistella-portable/` 空目录。P2-1：建议在 `发布检查清单.md` 中补充 zip 格式校验步骤。经理确认返工范围。
- 2026-08-30：返工干事完成 M3 返工：将 `make-portable.js` 改为 PowerShell + .NET `ZipFile::CreateFromDirectory` 生成真正 zip；补充 `发布检查清单.md` 产物格式校验与反斜杠路径分隔符说明。全部验收命令复验通过；`file` 校验为 `Zip archive data`；PowerShell 解压后 `cistella-portable/` 目录存在；`release-smoke-test.js` 通过。M3 重新进入待独立初审状态。
- 2026-08-30：M3 独立初审复审结论 PASS；经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 93、M1/M2/M3 集成测试（10/5/7）、core 全量、desktop 8、check、pnpm build、tauri build（msi/nsis）、make-portable.js（真正 zip）、release-smoke-test.js。M3 完成。
- 2026-08-30：工单 14 全部里程碑（M1/M2/M3）完成并经理终审通过；本工单目录归档到 `_dev/archive/`。
