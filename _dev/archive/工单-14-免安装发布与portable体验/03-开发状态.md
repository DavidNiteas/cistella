# 03. 开发状态：免安装发布与 portable 体验

## 当前阶段

M1 **经理终审通过**；M2「打包产物与迁移向导」独立初审结论 **NEEDS REWORK**，正在返工；M3 仍待授权。

## 设计输入与决议登记

本工单设计基于经理于 2026-08-28 的立项决策，A/B/C 三层决议已写入 `01-目标与设计.md`，核心摘要如下：

### A 层方向

- 工单 14 目标：完成 cistella「免安装发布与 portable 体验」，让产品达到可交付状态。
- 范围边界：M1 只做 portable 模式检测、数据目录切换、最近库/配置持久化与迁移接口；M2/M3 做打包产物、备份恢复向导、可选自动更新。
- 核心原则不变：库即一切、复制即走、打开即用、本地优先。

### B 层设计

- portable 模式判定：检测 exe 所在目录是否存在 `cistella-portable/` 目录。
- 数据目录抽象：`AppDirectories` 结构体，区分 portable/installed 两种模式。
- 最近库列表：`recent_vaults.json`，portable 模式下使用相对路径。
- 配置外置：`config/app.json`，不写入注册表。
- 迁移接口：`migrate_vaults_to_portable` / `migrate_vaults_to_installed`。
- 打包产物：安装包（msi/nsis）与 portable zip。

### C 层默认

- portable 标志目录名：`cistella-portable/`。
- 最近库 JSON 字段：`vaults: [{ path, name, opened_at }]`。
- Tauri 命令：`app_directories`、`recent_vaults`、`update_recent_vaults`、`is_portable_mode`、迁移命令。
- UI 入口：首页/设置页显示模式与最近库列表。
- 测试：临时目录构造 portable/installed 环境，验证切换、持久化、相对路径、迁移回滚。

## 施工里程碑

- M1：Portable 模式与数据边界。**已施工完成，待独立初审**
- M2：打包产物与迁移向导。**已施工完成，待独立初审**
- M3：发布流程与可选增强。**未授权**

## 验证证据

- 阶段门：2026-08-28，经理拍板 A/B/C 决议并授权撰写四件套。
- 四件套文档已在本目录落地：
  - `01-目标与设计.md`
  - `02-施工路线图.md`
  - `03-开发状态.md`
  - `04-施工日志.md`
- M1 施工验收命令已执行并全部通过：
  - `cargo fmt --check`：通过
  - `cargo test -p cistella-core --lib`：91 passed
  - `cargo test -p cistella-core --test '工单14_m1_*'`：9 passed
  - `cargo test -p cistella-core`：全绿（含既有工单测试）
  - `cargo test -p cistella-desktop`：8 passed
  - `cargo check -p cistella-desktop`：通过
  - `pnpm build`：通过
- 状态：独立初审 PASS（附 P2 返工项）。

## M1 独立初审记录

- 初审时间：2026-08-28
- 初审结论：**PASS（附 P2 返工项）**
- 全部验收命令通过：fmt、core lib 91、M1 集成 9、core 全量、desktop 8、check、pnpm build。
- P2-1（建议同轮）：`save_recent_vaults` 低层 API 不自动相对化，存在调用方破坏不变量风险；建议在 Core 层增加带 `AppDirectories` 上下文的保存封装或明确文档。
- P2-2（必须修复）：集成测试 `portable_mode_stores_relative_paths_and_resolves_to_absolute` 未真正验证相对路径存储；测试断言存在漏洞，需补充先转换再保存/加载的真实相对路径测试。
- P2-3（可延后）：迁移时符号链接被静默跳过。
- P2-4（建议同轮）：`unique_migration_target` 在极端碰撞场景下可能无限循环；建议增加上限。
- P2-5（可延后）：配置解析失败不降回默认配置。
- 经理确认返工范围：必须修 P2-2；建议同轮修 P2-1 和 P2-4；P2-3/P2-5 延后。

## M1 返工记录

- 返工时间：2026-08-30
- 返工结论：P2 阻塞项已修复，建议同轮项已修复，可重新提交独立初审。
- 修改文件：
  - `crates/openalex-analysis-core/src/app_dirs.rs`
    - 新增 `AppDirectories::save_recent_vaults(&self, vaults)`，portable 模式下自动调用 `make_vault_paths_portable` 再写入，强制「portable 模式存储相对路径」的不变量。
    - `unique_migration_target` 增加 `MAX_MIGRATION_SUFFIX = 10_000` 上限，超过后返回 `CoreError::MigrationFailed`，避免极端碰撞场景下无限循环。
  - `crates/openalex-analysis-core/tests/工单14_m1_portable模式与数据边界.rs`
    - 修复 `portable_mode_stores_relative_paths_and_resolves_to_absolute`：保存前调用 `make_vault_paths_portable` 转换，通过 JSON 解析断言存储路径为相对路径 `vaults/my-library`，再加载并解析回绝对路径。
    - 新增 `app_directories_save_recent_vaults_relativizes_inside_portable_root`，验证 Core 层封装方法的转换-保存-加载-解析闭环。
  - `crates/openalex-analysis-studio/src-tauri/src/lib.rs`
    - `update_recent_vaults` 命令改为直接调用 `dirs.save_recent_vaults(&vaults)`，移除外层手动相对化逻辑。
- P2 处理状态：
  - P2-2（必须修复）：已修复。
  - P2-1（建议同轮）：已修复（方案 A，Core 层封装强制 invariant）。
  - P2-4（建议同轮）：已修复。
  - P2-3（可延后）：延至 M2 完整迁移向导评估。
  - P2-5（可延后）：延至 M2 配置 UI 中明确策略。
- 验证证据：全部验收命令重新执行并通过。
  - `cargo fmt --check`：通过
  - `cargo test -p cistella-core --lib`：91 passed
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed（原 9 + 1 新增）
  - `cargo test -p cistella-core`：全绿
  - `cargo test -p cistella-desktop`：8 passed
  - `cargo check -p cistella-desktop`：通过
  - `pnpm build`：通过

## M1 经理终审记录

- 终审时间：2026-08-30
- 终审结论：**PASS**
- 复跑验收命令结果：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：91 passed; 0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed; 0 failed。
  - `cargo test -p cistella-core`：全量通过（含工单 07/08/09/10/11/12/14 全部集成测试）。
  - `cargo test -p cistella-desktop`：8 passed; 0 failed。
  - `cargo check -p cistella-desktop`：通过。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
- 遗留待办（不阻塞 M1）：
  - 迁移时符号链接处理策略（P2-3）延至 M2。
  - 配置解析失败降级策略（P2-5）延至 M2。
  - Windows 增量编译目录锁定警告为非阻断项。

## M2 施工验证证据

- 阶段门：经理已授权 M2「打包产物与迁移向导」施工。
- 全部验收命令已执行并通过：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：93 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m2_*'`：5 passed；0 failed。
  - `cargo test -p cistella-core`：全量通过（含工单 07/08/09/10/11/12/14 全部集成测试）。
  - `cargo test -p cistella-desktop`：8 passed；0 failed。
  - `cargo check -p cistella-desktop`：通过。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
- 打包产物：
  - Tauri bundle 目标已配置为 `["msi", "nsis"]`；`bundle.icon` 指向 `icons/icon.ico`。
  - `src-tauri/Cargo.toml` 中 `[[bin]] name` 为 `cistella`，因此 release 产物为 `target/release/cistella.exe`。
  - 自定义脚本 `crates/openalex-analysis-studio/scripts/make-portable.js` 在 `cargo tauri build` 后生成 `cistella-<version>-portable.zip`（含 `cistella.exe`、空 `cistella-portable/` 目录与 `README-portable.txt`）。
  - 一键命令：`pnpm --dir crates/openalex-analysis-studio tauri build`（生成 installer）后运行 `node crates/openalex-analysis-studio/scripts/make-portable.js`（生成 portable zip）。
- 备份/恢复实现：使用已在 `Cargo.lock` 中的 `zip` crate（8.6.0），将 Vault 目录打包为 zip 并原样恢复，恢复目标已存在时返回错误。

## M2 独立初审记录

- 初审时间：2026-08-30
- 初审结论：**NEEDS REWORK**
- P1-1（必须修复）：`tauri.conf.json` 中 `bundle.icon` 为 `[]`，导致 `pnpm tauri build --bundles msi` 失败 `Couldn't find a .ico icon`。修复：改为 `["icons/icon.ico"]`（该文件已存在）。
- P1-2（必须修复）：`make-portable.js` 期望 `target/release/cistella.exe`，但实际 binary 名为 `cistella-desktop`（见 `src-tauri/Cargo.toml`）。修复推荐方案 A：将 `[[bin]] name` 从 `cistella-desktop` 改为 `cistella`。
- P2-1（建议同轮）：迁移向导在 installed 模式下选择 portable 目标会预览错误路径并被后端拒绝。修复：UI 根据 `isPortableMode` 禁用/提示，或支持在目标位置创建 `cistella-portable/` 标记目录后再迁移。
- P2-2（延后）：备份/迁移静默跳过符号链接，已同意延至 M2/M3 评估。
- P2-3（建议同轮）：后端错误未回显在迁移/备份/恢复对话框内，仅写入全局状态栏。修复：在弹窗内显式展示错误。
- P3：`02-施工路线图.md` 写「新增 Tauri portable 输出目标」与实际 `["msi","nsis"]` + `make-portable.js` 不符；`zip` 直接写在 crate `Cargo.toml` 而非 workspace；产物名与 `cistella-desktop.exe` 不一致。随 P1 修复同步更新文档。

## M2 返工记录

- 返工时间：2026-08-30
- 返工结论：P1/P2/P3 返工项已修复；P2-2 延后；全部既有验收命令已复验通过；Tauri 打包与 portable zip 生成结果待本记录末尾补充。
- 经理确认返工范围：必修 P1-1/P1-2；建议同轮 P2-1/P2-3；P2-2 延后；P3 随代码修复同步更新。
- 修改文件：
  - `crates/openalex-analysis-studio/src-tauri/tauri.conf.json`：将 `bundle.icon` 从 `[]` 改为 `["icons/icon.ico"]`，修复打包时找不到 .ico 的问题（P1-1）。
  - `crates/openalex-analysis-studio/src-tauri/Cargo.toml`：将 `[[bin]]` 段中的 `name` 从 `cistella-desktop` 改为 `cistella`（`[package] name` 保持 `cistella-desktop` 不变），使 release 产物与 `make-portable.js` 期望一致（P1-2）。
  - `crates/openalex-analysis-studio/src/App.tsx`：
    - 迁移向导 Step 2 根据 `appDirs.isPortableMode` 禁用当前同模式选项，并显示「当前已经是便携/安装模式」提示（P2-1）。
    - `confirmMigration` 增加 try/catch，将后端错误捕获到 `migrationError` 状态并在弹窗内显示（P2-3）。
    - 新增 `backupRestoreError` 状态；`backupCurrentVault` 与 `restoreFromBackup` 捕获后端错误并回显在设置页备份/恢复按钮下方（P2-3）。
  - `_dev/工单-14-免安装发布与portable体验/02-施工路线图.md`：M2 交付物说明改为「msi + nsis + make-portable.js 脚本组装 portable zip」；产物名改为 `cistella.exe`；`zip` 依赖位置说明为 `crates/openalex-analysis-core/Cargo.toml`（P3）。
  - `_dev/工单-14-免安装发布与portable体验/03-开发状态.md`：同步 M2 施工验证证据中的打包产物描述（P3）。
- P1/P2/P3 处理状态：
  - P1-1（必须修复）：已修复。
  - P1-2（必须修复）：已修复。
  - P2-1（建议同轮）：已修复。
  - P2-2（延后）：仍延后（符号链接静默跳过）。
  - P2-3（建议同轮）：已修复。
  - P3（文档同步）：已修复。
- 返工后必须复验：
  - `pnpm --dir crates/openalex-analysis-studio tauri build` 成功生成 msi + nsis。
  - `node crates/openalex-analysis-studio/scripts/make-portable.js` 成功生成 portable zip。
  - 全部既有验收命令全绿：`cargo fmt --check`、`cargo test -p cistella-core --lib`、`cargo test -p cistella-core --test '工单14_m1_*'`、`cargo test -p cistella-core --test '工单14_m2_*'`、`cargo test -p cistella-core`、`cargo test -p cistella-desktop`、`cargo check -p cistella-desktop`、`pnpm build`。
- 验证结果：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：93 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m2_*'`：5 passed；0 failed。
  - `cargo test -p cistella-core`：全量通过（含工单 07/08/09/10/11/12/14 全部集成测试）。
  - `cargo test -p cistella-desktop`：8 passed；0 failed。
  - `cargo check -p cistella-desktop`：通过（仅 Windows 增量编译目录锁定警告，非阻断）。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
  - `pnpm --dir crates/openalex-analysis-studio tauri build`：通过；生成 `target/release/bundle/msi/cistella_0.1.0_x64_en-US.msi` 与 `target/release/bundle/nsis/cistella_0.1.0_x64-setup.exe`；release binary 为 `target/release/cistella.exe`。
  - `node crates/openalex-analysis-studio/scripts/make-portable.js`：通过；生成 `target/release/cistella-0.1.0-portable.zip`。

## M2 经理终审记录

- 终审时间：2026-08-30
- 终审结论：**PASS**
- 复跑验收命令结果：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：93 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m2_*'`：5 passed；0 failed。
  - `cargo test -p cistella-core`：全量通过（含工单 07/08/09/10/11/12/14 全部集成测试）。
  - `cargo test -p cistella-desktop`：8 passed；0 failed。
  - `cargo check -p cistella-desktop`：通过（仅 Windows 增量编译目录锁定警告，非阻断）。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
  - `pnpm --dir crates/openalex-analysis-studio tauri build`：成功生成 `target/release/bundle/msi/cistella_0.1.0_x64_en-US.msi`、`target/release/bundle/nsis/cistella_0.1.0_x64-setup.exe` 与 `target/release/cistella.exe`。
  - `node crates/openalex-analysis-studio/scripts/make-portable.js`：成功生成 `target/release/cistella-0.1.0-portable.zip`。
- 遗留待办（不阻塞 M2）：
  - 迁移时符号链接处理策略（P2-2）延至 M3 评估或后续工单。
  - Windows 增量编译目录锁定警告为非阻断项。

## M3 施工验证证据

- 阶段门：经理已授权 M3「发布流程与可选增强」施工。
- 代码与文档交付物：
  - `crates/openalex-analysis-core/src/release.rs`：版本解析、本地 update.json 读取、语义化版本比较、占位式 `check_update`。
  - `crates/openalex-analysis-core/tests/工单14_m3_发布流程与可选增强.rs`：7 个集成测试覆盖版本解析、更新检测、缺失/畸形 JSON。
  - `crates/openalex-analysis-studio/src-tauri/src/lib.rs`：新增 `get_app_version`、`check_update` Tauri 命令并注册。
  - `crates/openalex-analysis-studio/src-tauri/update.json`：`{"latest_version":"0.1.0"}`。
  - `crates/openalex-analysis-studio/src/App.tsx`：设置页显示当前版本与更新状态。
  - `crates/openalex-analysis-studio/scripts/release-smoke-test.js`：一键回归脚本。
  - `_dev/工单-14-免安装发布与portable体验/发布检查清单.md`：发布环境、命令、产物路径、签名说明、回归测试、人工检查项。
  - `_dev/工单-14-免安装发布与portable体验/单文件exe评估.md`：可行性、风险与延后建议。
- 全部既有验收命令已复验通过，状态登记为「待独立初审」。

## M3 独立初审记录

- 初审时间：2026-08-30
- 初审结论：**NEEDS REWORK**
- P1-1（必须修复）：`make-portable.js` 在 M3 返工中从 `Compress-Archive` 改为 `tar -acf`，导致产物 `target/release/cistella-0.1.0-portable.zip` 实际为 POSIX tar 归档（`file` 检测结果：`POSIX tar archive (GNU)`），不是 Windows 用户可双击解压的 zip 文件。修复：改用 PowerShell/.NET `System.IO.Compression.ZipFile::CreateFromDirectory` 或等效方案生成真正的 zip；同时确保 `cistella-portable/` 空目录在 zip 中保留。
- P2-1（建议同轮）：`发布检查清单.md` 应补充 zip 格式校验命令（如 `file` / PowerShell `Expand-Archive` 测试），避免后续再次误用 tar。

## M3 返工记录

- 返工时间：2026-08-30
- 返工结论：P1-1 已修复；P2-1 已修复。
- 经理确认返工范围：必修 P1-1；建议同轮 P2-1。
- 修改文件：
  - `crates/openalex-analysis-studio/scripts/make-portable.js`：将 `tar -acf` 替换为 PowerShell + .NET `[System.IO.Compression.ZipFile]::CreateFromDirectory`，生成真正的 zip 文件并保留 `cistella-portable/` 空标记目录（P1-1）。
  - `_dev/工单-14-免安装发布与portable体验/发布检查清单.md`：新增「产物格式校验」小节，补充 `file` 校验、PowerShell `Expand-Archive` 测试与 `cistella-portable/` 存在性检查，并说明反斜杠路径分隔符的已知限制（P2-1）。
- 返工后复验结果：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：93 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m2_*'`：5 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m3_*'`：7 passed；0 failed。
  - `cargo test -p cistella-core`：全量通过。
  - `cargo test -p cistella-desktop`：8 passed；0 failed。
  - `cargo check -p cistella-desktop`：通过（仅 Windows 增量编译目录锁定警告，非阻断）。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
  - `pnpm --dir crates/openalex-analysis-studio tauri build`：成功生成 msi + nsis + `target/release/cistella.exe`。
  - `node crates/openalex-analysis-studio/scripts/make-portable.js`：成功生成 `target/release/cistella-0.1.0-portable.zip`。
  - `file target/release/cistella-0.1.0-portable.zip`：`Zip archive data, at least v2.0 to extract, compression method=store`。
  - PowerShell `Expand-Archive` 解压后 `cistella-portable/` 目录存在。
  - `node crates/openalex-analysis-studio/scripts/release-smoke-test.js`：`SMOKE TEST PASSED`。

## M3 经理终审记录

- 终审时间：2026-08-30
- 终审结论：**PASS**
- 复跑验收命令结果：
  - `cargo fmt --check`：通过。
  - `cargo test -p cistella-core --lib`：93 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m1_*'`：10 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m2_*'`：5 passed；0 failed。
  - `cargo test -p cistella-core --test '工单14_m3_*'`：7 passed；0 failed。
  - `cargo test -p cistella-core`：全量通过（含工单 07/08/09/10/11/12/14 全部集成测试）。
  - `cargo test -p cistella-desktop`：8 passed；0 failed。
  - `cargo check -p cistella-desktop`：通过（仅 Windows 增量编译目录锁定警告，非阻断）。
  - `pnpm build`：通过（仅 chunk size 警告，非阻断）。
  - `pnpm --dir crates/openalex-analysis-studio tauri build`：成功生成 MSI、NSIS 安装包与 release exe。
  - `node crates/openalex-analysis-studio/scripts/make-portable.js`：成功生成真正的 zip；`file` 校验为 `Zip archive data`；PowerShell 解压后 `cistella-portable/` 目录存在。
  - `node crates/openalex-analysis-studio/scripts/release-smoke-test.js`：`SMOKE TEST PASSED`。
- 遗留待办（不阻塞 M3 / 工单 14 关闭）：
  - 迁移时符号链接处理策略延至后续工单或维护阶段评估。
  - portable zip 使用 Windows 反斜杠路径分隔符，Windows 原生工具可正常解压；若未来需要更强跨平台兼容性，可改用正斜杠生成 zip。
  - Windows 增量编译目录锁定警告为非阻断项。

## 阶段门裁定

- 经理于 2026-08-28 核验 01/02 的 A/B/C 决议、范围边界、数据目录抽象、最近库/配置外置、迁移接口、打包产物方向与 TBD 管理，阶段门通过。
- M1 已通过经理终审并固化提交；经理于 2026-08-30 召开阶段门并授权 M2 施工。
- M2 已通过经理终审；经理于 2026-08-30 召开阶段门并授权 M3「发布流程与可选增强」施工。
- M3 已通过经理终审；工单 14 全部里程碑完成，进入归档流程。
