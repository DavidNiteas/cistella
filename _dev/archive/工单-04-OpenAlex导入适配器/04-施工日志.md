# 04. 施工日志：OpenAlex 导入适配器

## 施工日志

- 2026-08-25：建立工单骨架。
- 2026-08-25：引入 SourceAdapter 抽象。
- 2026-08-25：补出 OpenAlexSourcesAdapter 作为首个来源适配器。
- 2026-08-25：桌面导入命令改为经由适配器执行。
- 2026-08-25：前端设置页开始展示来源适配器信息。
- 2026-08-25：前端导入后开始回显 vault_id 与来源输入路径。
- 2026-08-25：core / desktop 补出 OpenAlex Sources 导入前预检。
- 2026-08-25：预检结果可回显分区数、manifest 存在性与快照日期。
- 2026-08-25：桌面端补出失败导入的请求保留与重试入口。
- 2026-08-25：将 OpenAlex 导入改为同父目录 staging 构建，目标输出路径只通过最终目录重命名发布。
- 2026-08-25：明确 manifest 为 staging 内最后写入的提交标记，并对 parquet、arrow、manifest 文件执行同步。
- 2026-08-25：拒绝已有输出目录，避免失败重试覆盖有效 Vault 或混合旧文件；保持现有 `ImportOptions` 与 `SourceAdapter::import` API 形状不变。
- 2026-08-25：为 staging 错误清理、模拟中断、已有目标保护和预检失败不创建目标补充 core 测试。

## 偏离申请

- 暂无。

## 验收记录

- OpenAlex 已从产品中心退为来源适配器。
- 导入路径已收束为 cistella Vault。
- 适配器骨架、预检、自动连接、失败重试已具备。
- 本次可靠导入施工完成：失败不会在请求的输出目录留下可被识别为 Vault 的半成品；成功只在 manifest 与数据完整写入后发布目标目录。
- `cargo test -p cistella-core` 已通过 10 项测试。
- `cargo check -p cistella-desktop` 通过。

## 备注

- 强制终止可能留下带 `.cistella-import-*.tmp` 标识的 staging 目录；它不等于用户请求的 Vault 输出路径，也不会被桌面自动连接。专门的遗留临时目录清理不纳入本工单。
- 已有输出目录不做隐式覆盖；重试时保留最近请求，但应选择新的输出路径或由用户先明确处理已有目录。

