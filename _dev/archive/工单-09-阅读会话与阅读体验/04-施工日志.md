# 04. 施工日志：阅读会话与阅读体验

- 2026-08-26：工单08完成M1—M4、独立初审/返工复审和经理终审，已归档；按串行规则开立工单09。
- 2026-08-26：工单09完成A/B设计收口并通过阶段门，进入施工。
- 2026-08-26：确认范围边界：会话行为与ReadingStatus分离；新会话只引用item_id + asset_id；不实现内置阅读器、页码进度、全文、笔记或同步。

## M1

- 2026-08-26：严格在 Core 实现 ReadingSession 基础层，不修改 Tauri、桌面 UI 或工单 M2—M4。
- 2026-08-26：新增 `ReadingSession`（`session_id`、`item_id`、`asset_id`、开始/最近打开/结束时间与状态）及 `ReadingSessionState`（`active`、`paused`、`closed`），使用稳定 camelCase JSON 字段和小写状态值。
- 2026-08-26：新增 `user/reading_sessions.json` 读写 API。缺少该文件按空历史读取；写入采用具名临时文件与原子替换，替换失败时不破坏既有 JSON 并清理临时物。
- 2026-08-26：会话写入前校验 `item_id` 存在且 `asset_id` 确属该条目；此校验不打开或解析资产物理路径，因此文件缺失、不可读或外部断链不抹除有效关联的会话历史。
- 2026-08-26：新增 M1 单测覆盖缺失资产会话保留与 JSON 往返、跨条目关联拒绝和旧会话 JSON 保持、写入替换失败后的旧 JSON/临时文件保护。
- 2026-08-26：执行 `cargo fmt --all`，通过；执行 `cargo test -p cistella-core`，40 个 core 单元测试、工单07 portable 集成测试 1 个、工单08 M4 资产集成测试 3 个及 Doc-tests 均通过，0 failed。
- 2026-08-26：M1 实施完成，待独立初审；无功能范围偏离，M2 及工单10—12未开启。


- 2026-08-26：M1独立初审通过。初审干事只读核验 ReadingSession 稳定序列化、原子持久化、item_id + asset_id 关联、缺失资产历史保留、失败清理与公共导出，未发现阻断项。
- 2026-08-26：经理M1终审通过。经理独立复跑 cargo test -p cistella-core（40 个 core 单元测试、工单07 portable 1 个、工单08 M4 集成 3 个与Doc-tests，0 failed）；裁定关闭M1、正式开启M2。

## M2

- 2026-08-26：严格只实施会话命令与恢复策略；未修改桌面前端，未开启 M3/M4 或工单10—12。
- 2026-08-26：在 Core 新增会话启动、恢复、暂停、结束、最近列表、继续目标和继续执行 API；会话操作以 `item_id + asset_id` 为唯一资产引用，不接受原始文件路径。
- 2026-08-26：启动、恢复和继续会话的 Core API 先验证条目—资产关联并解析既有受控资产路径，再委托桌面后端发起系统打开请求；仅请求成功接受后，才原子写入新会话或更新 `last_opened_at`。系统打开、路径解析或会话 JSON 写入失败均不提前更新时间或破坏旧记录。
- 2026-08-26：明确继续选择：优先 `last_opened_at` 最新的未关闭会话；不存在未关闭会话时选择 `last_opened_at` 最新的历史会话，并在成功打开后建立新的 active 会话。最近会话列表保留资产缺失/断链记录，运行时状态由资产模型计算；不可用资产继续失败时不更新时间。
- 2026-08-26：注册 Tauri 受控命令 `start_reading_session`、`resume_reading_session`、`pause_reading_session`、`end_reading_session`、`list_recent_reading_sessions`、`continue_reading_target`、`continue_reading_session`；Tauri 从字符串 UUID 解析 `item_id + asset_id`，不向 WebView 暴露通用路径 opener。
- 2026-08-26：新增 Core 单测覆盖启动→暂停→恢复→结束生命周期、继续目标的未关闭优先/历史回退及已关闭历史新会话、opener 拒绝或资产缺失时会话 JSON 与 `last_opened_at` 保持不变；M1 原子替换失败保护继续作为 M2 会话写入共用持久化层的证据。
- 2026-08-26：执行 `cargo fmt --all` 通过；执行 `cargo test -p cistella-core` 通过（43 个 core 单元测试、工单07 portable 集成测试 1 个、工单08 M4 资产集成测试 3 个、Doc-tests，0 failed）；执行 `cargo check -p cistella-desktop` 通过。Rust incremental 目录出现既有“拒绝访问（os error 5）”提示，但命令退出成功。
- 2026-08-26：M2 实施完成，待不同干事独立初审；无功能范围偏离，M3/M4 与工单10—12未开启。

- 2026-08-26：M2独立初审通过。不同干事只读核验 item_id + asset_id 受控入口、打开接受后才写入、暂停/结束边界、继续选择、不可用资产与原子失败语义；未发现阻断项。
- 2026-08-26：经理M2终审通过。经理独立复跑 cargo test -p cistella-core（43 个core单元测试、工单07 portable 1 个、工单08资产集成3个、Doc-tests，0 failed）及 cargo check -p cistella-desktop，均通过。裁定关闭M2、正式开启M3。

## M3

- 2026-08-26：严格只实施 Reading 工作区 UI 闭环；未修改 Core/Tauri 会话语义或序列化、未开启 M4 或工单10—12，未派发子代理。
- 2026-08-26：新增 ReadingSessionSummary 前端模型与按当前 Vault 隔离的会话加载逻辑。进入 Reading 工作区时并行加载文献/资产和最近会话；切换或断开 Vault 时清空本地视图，刷新失败时保留已显示的会话历史并反馈错误。
- 2026-08-26：新增最近阅读列表与统一“继续阅读”入口，显示文献、资产、会话状态、开始/最近打开时间及 Core 计算的运行时资产健康状态。不可用（含缺失、不可读、无效、外链断开）会话不被删除，明确显示无法继续打开的原因。
- 2026-08-26：新增 `start_reading_session`、`resume_reading_session`、`pause_reading_session`、`end_reading_session`、`continue_reading_session` 的 UI 调用闭环。Reading 新入口全部只传递 `item_id + asset_id` 或使用受控继续命令；不调用旧 `file_id` 文件入口，也不暴露任意路径打开能力。
- 2026-08-26：可用资产在 Reading 文献卡片中提供“开始阅读”；最近会话提供恢复、暂停、结束。资产收纳与资产元数据管理从 Reading 操作区移回 Vault 职责；Reading UI 不再提供 `ReadingStatus` 修改控件。
- 2026-08-26：补充最小的会话列表、不可用提示和窄窗口样式；未实现内置阅读器、进度、全文、笔记或同步。
- 2026-08-26：执行 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo check -p cistella-desktop` 与 `pnpm build`，均通过。Core 43 个单元测试、工单07 portable 1 个、工单08 M4 集成3个及 Doc-tests 通过，0 failed。Rust incremental `os error 5` 和 Vite 单块超过500 kB仅为既有非阻断提示。
- 2026-08-26：M3实施完成，待不同干事独立初审；无功能范围偏离，M4及工单10—12未开启。

### M3 返工：Vault 切换连接中隔离

- 2026-08-26：首次 M3 独立初审判定不通过。问题为 `connect_vault` 等待期间仅清空了条目与部分反馈，旧 `vaultPath` / `vaultSummary`、资产、最近会话与 Reading 反馈仍可能被 Reading 工作区当作当前 Vault 数据渲染；M4 未放行。
- 2026-08-26：严格只在 `crates/openalex-analysis-studio/src/App.tsx` 修复连接状态控制，未修改 Core/Tauri 会话契约，未启动 M4 或工单10—12，未派发代理。
- 2026-08-26：新增 `vaultConnectionGenerationRef`。`beginVaultConnection` 在每次手动连接和自动恢复候选连接开始时递增 generation，并同步清空 Vault 路径/摘要、Source 数据、个人条目、资产、最近会话以及 Literature/Reading 反馈；因此连接等待期间 `hasVault` 立即为假，旧库无法继续在 Reading 中显示。
- 2026-08-26：`applyVaultContext` 只接受当前 generation；`refreshVaultContext`、自动恢复、个人库读取、最近会话读取及 Source 刷新均以 generation 或预期 Vault 路径隔离晚到响应。手动切换失败时再次维持清空后的未连接状态并显示本次错误，不恢复错误归属的旧库数据。
- 2026-08-26：检查前端包 `package.json`：仅提供 `dev`、`build`、`preview`、`tauri` 脚本，未配置 Vitest/Jest 或现有前端测试设施。为避免扩张 M3 返工范围，未新增测试依赖；以集中且带注释的四个状态控制函数（`beginVaultConnection`、`isCurrentVaultConnection`、`applyVaultContext`、`clearVaultContext`）作为静态审查单元，并由 `pnpm build` 验证 TypeScript 与生产构建。
- 2026-08-26：执行 `cargo fmt --all` 通过；执行 `cargo test -p cistella-core` 通过（43 个 core 单元测试、工单07 portable 1 个、工单08 M4 文献资产集成 3 个、Doc-tests，0 failed）；执行 `cargo check -p cistella-desktop` 通过；执行 `pnpm build` 通过。既有 Rust incremental `拒绝访问（os error 5）` 与 Vite 单块超过 500 kB 提示均未导致命令失败。
- 2026-08-26：M3返工完成，待不同于首次初审干事的干事进行只读复审；M4仍不得开启。


### M3 第二次返工：代际与后端连接提交隔离

- 2026-08-26：第二次独立初审判定不通过，并明确两项已裁定 C 层阻断：（1）Source 刷新、个人条目/资产、最近阅读会话的晚到响应只比较 Vault 路径，无法区分 `A → B → A` 中最早 A 代际与新 A 代际；（2）后端 `connect_vault` 打开成功后无条件写入全局 `vault_path`，因此 A 先发、B 后发且 B 先完成时，晚完成的 A 仍可把后端当前库改回 A。M4 未放行。
- 2026-08-26：第二次返工仅修改 `App.tsx` 与 Tauri 连接状态决策。自动恢复和手动连接均把单调递增 `generation` 传至 `connect_vault`；前端以不可变 `expectedGeneration + expectedVaultPath` 快照隔离 `refresh`、`loadLiteratureItems`、`loadDocumentAssets`、`loadPersonalLibrary`、`loadReadingSessions` 的成功与失败写入，并在工作区 effect、自动恢复、手动连接、`refreshVaultContext` 与操作后的刷新链显式传递该快照。连接失败维持清空后的未连接状态。
- 2026-08-26：后端以同一 `Mutex` 管理当前 Vault 路径和 `latest_connection_generation`。连接请求在打开 Vault 前登记 generation；低于当前 generation 的晚到请求立即返回 stale 错误；打开完成后再次比较 generation，只有仍为最新者能提交路径。这样过期连接的返回结果和后端状态提交均不能污染当前库。
- 2026-08-26：新增 desktop crate 的最小纯状态决策单测：覆盖 `A(generation 1) → B(generation 2)` 中 B 提交后 A 晚完成仍不能覆盖 B，以及旧 generation 晚到登记不能降低最新 generation。`cargo test -p cistella-desktop` 通过（2 passed）。
- 2026-08-26：范围未偏离：未变更 Core 公共契约、工单08资产语义或 M2 `item_id + asset_id` 受控阅读命令；未新增前端测试依赖、未派发子代理、未开启 M4 或工单10—12。M3为“返工完成，待不同干事复审”。


### M3 经理终审与M4开启

- 2026-08-26：不同干事对第二次返工进行只读复审并通过。复审确认 `expectedGeneration + expectedVaultPath` 已贯穿 Source、个人库、资产和会话异步写入，后端过期 `connect_vault` 无法覆写新代际已提交的 Vault 路径，且 Reading 入口继续只使用 `item_id + asset_id`。
- 2026-08-26：经理M3终审通过。经理复跑 `cargo test -p cistella-desktop`（2 项竞态单测）、`cargo test -p cistella-core`（43 个core单元测试、工单07 portable 1 个、工单08资产 portable 3 个及Doc-tests）、`cargo check -p cistella-desktop` 与前端 `pnpm build`，均通过。裁定关闭M3并正式开启M4；Rust incremental `os error 5` 和 Vite 单块超过500 kB为既有非阻断提示。

### M4 实施：portable与回归验收

- 2026-08-26：新增 `crates/openalex-analysis-core/tests/工单09_m4_阅读会话portable.rs`，未修改 Core/Tauri 产品契约，未新增依赖，未实现阅读器、进度、全文、笔记或同步，也未启动工单10—12。
- 2026-08-26：测试一建立最小有效 Vault、文献条目和可复制的 Vault PDF 资产，接受 opener 后启动并暂停会话；复制整个 Vault 后重新打开副本，以相同 `item_id + asset_id` 加载暂停历史并在副本内成功恢复。测试断言会话事实不依赖原机绝对路径，并断言会话操作不会改写 `manifest.json`、`tables/sources.parquet` 或 `user/document_assets.json`；首次会话仅新增 `user/reading_sessions.json`。
- 2026-08-26：测试二同时覆盖删除库内资产和断开外链资产。两类会话均保留且可列出，状态为 `missing` / `externalUnavailable`；`continue_reading_session` 在资产不可用时失败、不调用 opener，且 `reading_sessions.json` 字节不变、会话不删除。
- 2026-08-26：实施干事实跑 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo test -p cistella-desktop`、`cargo check -p cistella-desktop` 和前端 `pnpm build`，全部成功。Core：43 个单元测试、工单07 portable 1 个、工单08资产 portable 3 个、工单09阅读会话 portable 2 个和Doc-tests；desktop：2 个竞态单测。Rust incremental `os error 5` 与 Vite 单块超过500 kB仅为既有非阻断提示。
- 2026-08-26：偏离无。M4实施完成，待不同于实施干事的干事独立初审；未写M4经理通过，未归档工单。

### M4 初审不通过与C层返工：全Vault写入边界

- 2026-08-26：不同干事独立初审判定 M4 不通过。原 portable 测试虽比较了 `manifest.json`、`tables/sources.parquet`、`user/document_assets.json` 并限制首次新增文件，但未对全 Vault 建立字节快照；因此不能严格排除阅读会话操作改写 `user/literature_items.json` / `ReadingStatus`、库内 PDF 或其他既有文件，也不能捕获既有文件删除。M4 不得进入经理终审。
- 2026-08-26：本次 C 层返工仅修改 `crates/openalex-analysis-core/tests/工单09_m4_阅读会话portable.rs`。新增递归相对路径→字节完整快照与复用断言。条目创建、资产导入完成后，在首次 `start_reading_session` 前快照原 Vault；`start + pause` 后要求文件集合严格为基线加 `user/reading_sessions.json`，并逐字节比较所有基线文件。故 `user/literature_items.json` / `ReadingStatus`、库内 PDF、manifest、表及任意既有文件的越权写入或删除都会失败。
- 2026-08-26：复制 Vault 后，在 `resume_reading_session` 前建立副本完整快照；恢复后要求路径集合不增不减，逐字节比较除 `user/reading_sessions.json` 外的所有文件。保留原有“副本路径打开、非原 Vault 绝对路径”、缺失库内资产、外部断链资产与失败继续不改写历史覆盖。
- 2026-08-26：返工验证通过：`cargo fmt --all`；`cargo test -p cistella-core`（43 个 Core 单元测试、工单07 portable 1 个、工单08资产 portable 3 个、工单09阅读会话 portable 2 个及 Doc-tests）；`cargo test -p cistella-desktop`（2 个竞态单测）；`cargo check -p cistella-desktop`；前端 `pnpm build`。Rust incremental `os error 5` 与 Vite 单块超过 500 kB 均未导致失败。
- 2026-08-26：无偏离、无产品契约或依赖变更；未归档，未开启 M4 后续阶段或工单10—12。M4 状态为“返工完成，待不同干事复审”。

### M4 经理终审与归档

- 2026-08-26：不同干事对 M4 第二次返工进行只读复审并通过；复审确认完整 Vault 快照以路径集合和逐字节比较约束会话写入边界，portable 副本恢复、不可用资产历史保留及失败继续不写入均已覆盖。
- 2026-08-26：经理独立复跑 core、desktop 竞态、desktop check 和前端生产构建，均通过。裁定 M4 关闭、工单09已达成并归档；工单10可在归档完成后才立项。
