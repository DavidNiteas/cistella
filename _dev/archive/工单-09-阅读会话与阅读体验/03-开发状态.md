# 03. 开发状态：阅读会话与阅读体验

## 当前阶段

已达成

## 阶段门结论

- 2026-08-26：工单08已归档，工单09设计收口完成并进入施工。
- A层：为用户提供可恢复的最近阅读与继续阅读行为，不改变产品库/资产核心。
- B层：ReadingSession独立用户记录、asset_id关联、原子写入、缺失资产保留和数据边界已拍板。
- C层：按M1—M4逐项实施，均须不同干事初审与经理终审。

## 施工里程碑

- M1：ReadingSession模型与原子持久化。**经理终审通过**
  - 已实现 `ReadingSession`、`ReadingSessionState` 与 `user/reading_sessions.json` 的原子读写；新会话持久化先校验 `item_id + asset_id` 归属，不解析文件可用性，因此缺失、不可读或外部断链资产的既有会话记录可保留。
  - 已新增单测覆盖：缺失库内资产的会话往返与临时文件清理；跨条目资产关联拒绝且旧会话 JSON 不变；原子替换失败后旧 JSON 与临时文件清理。
  - 2026-08-26：执行 `cargo fmt --all` 通过；执行 `cargo test -p cistella-core` 通过（40 个 core 单元测试、工单07 portable 集成测试 1 个、工单08 M4 资产集成测试 3 个、Doc-tests，0 failed）。
  - M2 仍待开始，不得提前放行。
- M2：会话命令与恢复策略。**经理终审通过**
  - 已新增 Core 公共 API：`start_reading_session`、`resume_reading_session`、`pause_reading_session`、`end_reading_session`、`list_recent_reading_sessions`、`continue_reading_target` 与 `continue_reading_session`；全部只经 `item_id + asset_id` 关联资产，不接收用户给出的文件路径。
  - 启动、恢复和继续阅读均先由 Core 解析受控资产，再由 Tauri 后端调用系统 opener；只有 opener 请求接受后才写入新会话或更新 `last_opened_at`。暂停/结束仅更新会话生命周期，不修改资产、物理文件或 `ReadingStatus`。
  - 继续规则：优先最近打开的未关闭会话；若没有未关闭会话，选择最近打开的历史会话并成功打开后新建 active 会话。最近列表保持不可用会话，并以运行时资产状态标示；失败打开不更新时间。
  - 已注册 Tauri 受控命令：`start_reading_session`、`resume_reading_session`、`pause_reading_session`、`end_reading_session`、`list_recent_reading_sessions`、`continue_reading_target`、`continue_reading_session`。
  - 2026-08-26：执行 `cargo fmt --all` 通过；执行 `cargo test -p cistella-core` 通过（43 个 core 单元测试、工单07 portable 集成测试 1 个、工单08 M4 资产集成测试 3 个、Doc-tests，0 failed）；执行 `cargo check -p cistella-desktop` 通过。
  - M3 仍待开始，不得提前放行。
- M3：Reading工作区UI闭环。**经理终审通过**
  - Reading 工作区已改为会话优先：显示最近阅读会话、统一“继续阅读”入口，以及按会话提供恢复、暂停、结束动作；会话与启动入口均调用 M2 的受控阅读会话命令，不再从该工作区调用旧 `file_id` 或通用文件打开入口。
  - 会话列表显示会话状态、开始/最近打开时间和运行时资产健康状态。缺失、不可读、无效或外链断开资产的会话始终保留并显示原因；刷新或操作失败只反馈错误，不清空已有会话历史。
  - 可用资产可从文献条目卡片通过 `item_id + asset_id` 启动阅读；资产收纳/文件管理仍归 Vault 工作区。Reading UI 未新增或修改 `ReadingStatus`，未实现内置阅读器、页码进度、全文、笔记或同步。
  - 2026-08-26：执行 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo check -p cistella-desktop` 与 `pnpm build` 均通过；测试为 43 个 core 单元测试、工单07 portable 1 个、工单08 M4 资产集成 3 个、Doc-tests，0 failed。构建仅有既有 Rust incremental `os error 5` 提示与 Vite 单块超过 500 kB 警告，命令均成功退出。
  - 2026-08-26：首次独立初审不通过：新 Vault 的 `connect_vault` 尚在等待时，旧 `vaultPath` / 摘要、资产、最近会话及 Reading 反馈尚可能被当作当前数据渲染，M4 未放行。
  - 2026-08-26：返工将连接过程收口为显式 generation：`beginVaultConnection` 在发起连接前立即清空 Vault 上下文、个人库视图、资产、会话与反馈；`applyVaultContext`、自动恢复、刷新、个人库读取与 Source 刷新均以当前 generation / Vault 路径拒绝晚到结果。连接失败后保持未连接的错误状态，不恢复旧库数据。
  - 2026-08-26：前端包未配置测试脚本或测试运行器；未为本次狭义返工引入新测试依赖。状态控制已集中为可静态审查的 `beginVaultConnection`、`isCurrentVaultConnection`、`applyVaultContext`、`clearVaultContext` 四段，生产 TypeScript/Vite 构建通过。
  - 2026-08-26：第二次独立初审仍不通过，并裁定两项 C 层阻断：其一，`refresh`、个人库/资产/会话读取仅按路径拒绝晚到响应，`A → B → A` 时旧 A 代际仍可能写回新 A 视图；其二，后端 `connect_vault` 可由过期完成的连接无条件覆写全局 Vault 指针，导致后续 B 视图请求实际读取 A。M4 未放行。
  - 2026-08-26：第二次返工严格限于连接竞态隔离。前端连接调用（自动恢复与手动连接）显式传递单调 generation；`refresh`、`loadLiteratureItems`、`loadDocumentAssets`、`loadPersonalLibrary`、`loadReadingSessions` 及工作区/恢复/刷新调用链均捕获并同时验证 `expectedGeneration + expectedVaultPath` 后才写入 Source、个人库、资产、会话或相应反馈。Reading/Literature 操作后的受影响刷新与反馈同样沿用该快照，未改变 M2 的 `item_id + asset_id` 受控命令语义。
  - 2026-08-26：Tauri 后端将 Vault 路径与最新连接代际收口在同一互斥状态中；每个 `connect_vault(path, generation)` 先登记代际，打开完成后仅当该代际仍为最新才提交 Vault 路径，否则返回可解释的 stale 错误且不改动当前库。新增最小单测覆盖“1 先发、2 后发且 2 先完成、1 后完成时仍保持 B”及“晚到的旧请求不能降低最新代际”。未修改 Core 公共契约，未启动 M4 或工单10—12，也未引入依赖。
  - 2026-08-26：第二次返工验证：`cargo test -p cistella-desktop` 通过（新增后端竞态单测 2 个）；按工单验收命令执行 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo check -p cistella-desktop` 与 `pnpm build` 均通过；Core 为 43 个单元测试、工单07 portable 1 个、工单08资产集成 3 个及 Doc-tests，0 failed。该返工随后经不同干事复审及经理终审关闭，M4已按下方证据正式开启。
- M4：portable与回归验收。**经理终审通过**


## M3 经理终审证据

- 2026-08-26：不同干事复审通过；复审确认前端以 `expectedGeneration + expectedVaultPath` 双重快照拒绝 `A → B → A` 的旧响应，后端仅允许最新连接代际提交当前 Vault 路径，且未绕过 M2 的 `item_id + asset_id` 受控阅读命令。
- 2026-08-26：经理终审复跑 `cargo test -p cistella-desktop`（连接竞态单测 2 项通过）、`cargo test -p cistella-core`（43 个 core 单元测试、工单07 portable 1 个、工单08资产 portable 3 个及 Doc-tests 通过）、`cargo check -p cistella-desktop` 与前端 `pnpm build`，均通过。
- 经理裁定：M3关闭，通过；正式开启M4。Rust incremental `os error 5` 与 Vite 单块超过 500 kB 均为既有非阻断提示。

## M4 实施、初审返工与当前证据

- 2026-08-26：新增集成测试 `crates/openalex-analysis-core/tests/工单09_m4_阅读会话portable.rs`，未修改 Core/Tauri 产品契约，也没有新增依赖；既有 portable、库内资产缺失、外部链接断链和失败继续不改写历史覆盖均保留。
- 2026-08-26：M4 首次独立初审**不通过**。初审指出原测试只逐项比较 `manifest.json`、`tables/sources.parquet`、`user/document_assets.json`，并只检查新增文件集合，不能证明阅读会话操作没有改写 `user/literature_items.json`（包括 `ReadingStatus`）、Vault 内 PDF 或其他既有文件，也不能发现既有文件被删除；M4 未提交经理终审。
- 2026-08-26：本次 C 层返工严格限于测试写入边界，不改产品代码或依赖。测试新增递归的“相对路径 → 文件字节”完整 Vault 快照与可复用断言：首次 `start + pause` 后，文件路径集合必须严格等于基线集合加 `user/reading_sessions.json`，且每一个基线文件的字节完全相同；这直接覆盖 `user/literature_items.json` / `ReadingStatus`、所有库内 PDF、`manifest.json`、表数据、资产元数据以及其他既有文件，亦能发现任意既有文件删除。
- 2026-08-26：复制 Vault 后，在 `resume` 前再次建立副本完整快照；恢复后路径集合必须完全不变，并逐字节核验除 `user/reading_sessions.json` 外的全部路径未改写。会话 JSON 是唯一允许不同的文件，且 opener 仍断言实际打开的是副本根目录内的资产而非原 Vault 绝对路径。
- 2026-08-26：返工干事实跑 `cargo fmt --all`、`cargo test -p cistella-core`、`cargo test -p cistella-desktop`、`cargo check -p cistella-desktop` 及前端 `pnpm build`，均通过。Core：43 个单元测试、工单07 portable 1 个、工单08资产 portable 3 个、工单09阅读会话 portable 2 个与 Doc-tests；desktop 竞态单测 2 个通过。Rust incremental `os error 5` 与 Vite 单块超过 500 kB 仅为既有非阻断提示。
- 偏离：无。M4 当前仅为“返工完成，待不同干事复审”；不得写为经理通过、不得归档工单，工单10—12仍不得开启。

## 验收命令

```powershell
cargo test -p cistella-core
cargo check -p cistella-desktop
pnpm build
```

## 遗留与边界

- 不做内置阅读器、页码进度、全文索引、笔记标注、同步。
- 工单10—12未开启。
- TBD项在施工实验中回填，不在施工期擅自扩大范围。

## M1 经理终审证据

- 2026-08-26：不同干事独立初审通过；核验模型序列化、关联校验、缺失资产会话保留、原子失败保护与公共导出。
- 2026-08-26：经理独立复跑 cargo test -p cistella-core，40 个 core 单元测试、工单07 portable 1 个、工单08 M4 集成 3 个与 Doc-tests 均通过，0 failed。
- 经理裁定：M1关闭，通过；M2正式开启。


## M2 经理终审证据

- 2026-08-26：不同干事独立初审通过，已核验受控资产入口、打开成功后才持久化、暂停/结束边界、继续选择与不可用资产不改写会话。
- 2026-08-26：经理独立复跑 cargo test -p cistella-core（43 个core单元测试、工单07 portable 1 个、工单08资产集成3个与Doc-tests，0 failed）和 cargo check -p cistella-desktop，均通过。
- 经理裁定：M2关闭，通过；M3正式开启。


## M4 经理终审证据

- 2026-08-26：不同干事第二次返工复审通过。复审确认全 Vault 相对路径到字节快照覆盖任意新增、删除或改写，唯一允许变化为 user/reading_sessions.json；副本恢复同样受限，并保持副本内受控资产路径；缺失/外链断开会话保留且失败继续不写入。
- 2026-08-26：经理独立复跑 cargo test -p cistella-core（43 个 core 单元测试、工单07 portable 1 个、工单08 portable 3 个、工单09 portable 2 个及 Doc-tests）、cargo test -p cistella-desktop（2 个竞态单测）、cargo check -p cistella-desktop 与前端 pnpm build，均通过。
- 经理裁定：M4关闭，通过；工单09目标已达成，移入归档。Rust incremental os error 5 与 Vite 单块超过 500 kB 为既有非阻断提示。
