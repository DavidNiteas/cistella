# 03. 开发状态：品牌与产品重定位

## 当前阶段

已达成，已归档。

## 已完成

- cistella 品牌已落地到仓库顶层与桌面应用。
- 桌面应用标题、产品名与前端主叙事已切换为 cistella。
- Core / Desktop crate 已完成重命名为 cistella-core 与 cistella-desktop。
- CLI 已从核心 crate 的对外产品路径中退出；仓库级文档不再把命令行作为产品入口。
- `_dev` 工程治理入口、设计路线与状态账本已建立。
- 仓库级用户可见文档已改写为 cistella 视角：
  - `README.md`
  - `RUST_ARCHITECTURE.md`
  - `crates/openalex-analysis-studio/BUILD.md`
- OpenAlex 叙事已收束为“重要数据源 / 导入适配器”，不再作为产品中心。

## 遗留范围

仅保留以下历史路径或兼容命名，不作为用户可见产品叙事：

- `crates/openalex-analysis-studio/`：当前桌面工程历史目录名。
- `crates/openalex-analysis-core/`：当前 core 工程历史目录名。
- `openalex_*` 字段、函数、适配器与测试名：用于 OpenAlex 数据源适配器和来源溯源。
- `.gitignore` 中的 `openalex-library`、`openalex-sources`：本地样例/原始数据目录忽略规则。
- lockfile 与依赖名中的 `cli`：第三方 Tauri CLI 依赖事实，不是 cistella 对外 CLI 功能。

## 验证证据

- 2026-08-25：`rg -n -i "OpenAlex Analysis|openalex analysis|Legacy CLI" README.md RUST_ARCHITECTURE.md crates/openalex-analysis-studio/BUILD.md` 无命中。
- 2026-08-25：`rg -n -i "OpenAlex" README.md RUST_ARCHITECTURE.md crates/openalex-analysis-studio/BUILD.md` 仅命中数据源/适配器或历史路径说明。
- 2026-08-25：`cargo check -p cistella-desktop` 通过。

## 状态判断

工单 01 已通过经理终审并归档；剩余旧名均为历史路径、兼容命名或数据源适配语义。



