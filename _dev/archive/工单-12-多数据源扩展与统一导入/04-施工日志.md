# 04. 施工日志：多数据源扩展与统一导入

- 2026-08-27：经理立项并拍板工单 12 的 A/B/C 三层决议，明确 M1 只做离线 BibTeX 导入，RIS/DOI 补全/OpenAlex works 延后，Source 分析工作区保持独立。
- 2026-08-27：本干事按 `_dev/工单-11-笔记与标注/` 四件套纪律完成本工单 `01-目标与设计.md`、`02-施工路线图.md`、`03-开发状态.md`、`04-施工日志.md` 撰写；未实施任何代码，未进入 M1。

- 2026-08-27：经理核验四件套并通过阶段门；M1 BibTeX 离线文件导入已授权施工；M2/M3 仍未授权。
- 2026-08-27：M1 实施完成。实现 `ExternalIdentifier` 与 `LiteratureItem`/`LiteratureItemDraft` 扩展；新增 `import/` 子模块（`source_record`、`record_source`、`bibtex_source`、`conflict`、`library_importer`、`bibtex_importer`）；在 `Vault` 上新增 `inspect_bibtex_import`、`commit_bibtex_import`、`load_source_batches`；新增 Tauri 命令 `inspect_literature_import`/`import_literature_file`；在 Reading 工作区新增「导入文献」按钮、预览面板与结果回显；新增 `crates/openalex-analysis-core/tests/工单12_m1_BibTeX导入.rs` 覆盖 9 个场景。
- 偏离申请：原设计依赖 `biblatex = "0.10"`，但当前环境无法访问 crates.io（`Could not resolve host: index.crates.io`），无法下载该 crate。为保证 M1 功能可交付、测试可运行，改为自研最小 BibTeX 解析器，位于 `crates/openalex-analysis-core/src/import/bibtex_source.rs`。解析器支持常见 `@type{key, field = {value}/"value", ...}` 格式、嵌套花括号、注释跳过，字段映射与冲突检测逻辑不变。此偏离仅影响解析实现，不影响接口、数据模型或持久化格式。M2/M3 评估是否重新引入 `biblatex` 或其他成熟解析库。
- 验收命令执行结果：`cargo fmt --check` 通过；`cargo test -p cistella-core --lib` 75 passed；`cargo test -p cistella-core --test '工单12_m1_*'` 9 passed；`cargo test -p cistella-core` 全部通过；`cargo test -p cistella-desktop` 8 passed；`cargo check -p cistella-desktop` 通过；`pnpm build` 通过。M1 待独立初审/经理终审，不自行初审或终审。
- 2026-08-27：独立初审结论 NEEDS REWORK。阻塞问题 P1：编辑已有文献条目会清空 `external_identifiers`，导致导入来源身份丢失；建议同轮修复 P2-1（导入提交后未通知本地搜索索引）、P2-2（冲突匹配未覆盖 `openalex`）。经理确认返工范围：必须修 P1，建议修 P2-1/P2-2，其余 P2-3/4/5 由干事酌定。
- 2026-08-27：M1 返工完成。修复 P1（前后端双重防御保留 `external_identifiers`）、P2-1（导入提交后触发本地搜索索引更新）、P2-2（`openalex` 纳入冲突匹配）、P2-4（提交失败回滚错误组合包装）。新增 3 个测试覆盖上述修复。P2-3 延后，P2-5 已在文档中说明。干事自评 READY_FOR_REVIEW，等待重新独立初审。
- 2026-08-27：独立初审复审结论 PASS。返工项全部修复到位，验收命令全绿；仅记录 UI 外部标识可视化、errors 字段语义、命令层两次加载等后续优化点，不阻塞 M1。
- 2026-08-27：经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 75、M1 集成 12、core 全量、desktop 8、check、pnpm build。M1 完成并归档。
- 2026-08-27：经理召开阶段门并授权 M2/M3 施工。M2 做 RIS 离线文件导入；M3 做 OpenAlex works 本地导入与基于本地数据的 DOI 补全接口；外部 HTTP 网络调用延后。
- 2026-08-27：M2 施工完成。新增 `RISRecordSource` 与通用 `LiteratureLibraryImporter`，`SourceRecord` 扩展摘要/关键词/页码/卷字段，UI 增加格式选择，新增 9 个 RIS 集成测试。干事自评 READY_FOR_REVIEW，等待独立初审。
- 2026-08-27：M2 独立初审结论 NEEDS REWORK。阻塞问题 P1：RIS `SN`/`IS` 字段命名空间映射颠倒；P2：文档/注释不一致。经理确认返工范围：必须修 P1 与 P2，P3 非阻塞风险延后。
- 2026-08-27：M2 返工完成。修正 `SN`/`IS` 映射；`SN` 按条目类型映射为 `issn`/`isbn`；`IS` 不再生成 `issn`。同步修正文档/注释不一致。新增测试覆盖。干事自评 READY_FOR_REVIEW，等待重新独立初审。
- 2026-08-28：M2 独立初审复审结论 PASS；经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 79、M1 集成 12、M2 集成 11、core 全量、desktop 8、check、pnpm build。M2 完成。
- 2026-08-27：M3 施工完成。新增 `OpenAlexWorksRecordSource`（`openalex_works_source.rs`）与 `DoiResolver`/`LocalOpenAlexDoiResolver`（`doi_resolver.rs`）；`ImportFormat` 扩展 `OpenAlexWorks`；`LiteratureLibraryImporter` 按格式分派；`Vault` 新增 `resolve_doi_via_local_openalex`、`search_local_openalex_works`、`preview_literature_records`；Tauri 新增 `preview_openalex_work`、`resolve_doi_local`、`list_local_openalex_works`；Reading 工作区导入格式增加「OpenAlex works」，支持目录选择、关键词搜索、候选预览与提交。新增 11 个 M3 集成测试。
- 依赖说明：`cistella-core` 新增 `flate2` workspace 依赖（`Cargo.toml` / `crates/openalex-analysis-core/Cargo.toml`）。`flate2` 已存在于 `Cargo.lock` 与本地 cargo cache，未引入新的 crates.io 包下载；M3 因此同时支持 `.jsonl` 与 `.jsonl.gz` 本地 works 文件。
- 2026-08-28：M3 独立初审结论 PASS；经理终审结论 PASS。亲自复跑全部验收命令均通过：fmt、core lib 83、M1 集成 12、M2 集成 11、M3 集成 11、core 全量、desktop 8、check、pnpm build。M3 完成，工单 12 全里程碑达成。
- 数据偏差说明：仓库 `openalex-sources/` 实际只包含 sources parquet 分区，没有 works 的 JSONL/Gzip 数据。M3 实现按通用目录结构扫描 `*.jsonl` / `*.jsonl.gz`，测试使用临时 fixture 构造 works 分区，不依赖真实 `openalex-sources/`。
- 验收命令执行结果：`cargo fmt --check` 通过；`cargo test -p cistella-core --lib` 83 passed；`cargo test -p cistella-core --test '工单12_m1_*'` 12 passed；`cargo test -p cistella-core --test '工单12_m2_*'` 11 passed；`cargo test -p cistella-core --test '工单12_m3_*'` 11 passed；`cargo test -p cistella-core` 全量通过；`cargo test -p cistella-desktop` 8 passed；`cargo check -p cistella-desktop` 通过；`pnpm build` 通过（仅 chunk size 警告）。干事自评 `READY_FOR_REVIEW`，等待独立初审/经理终审。