# 工单 15：开发状态

## 工单信息

- **工单目录**：`_dev/工单-15-UI-UX-重构与设计系统/`
- **状态**：已达成
- **立项日期**：2026-08-31
- **阶段门日期**：2026-08-31
- **阶段门决定**：经理审查确认无未收口 A 类问题；用户原始请求已授权系统性 UI/UX 重构范围；视觉方向沿用现有深色侧边栏 + 紫色主色优化路线。准予进入施工期。
- **闭合日期**：2026-08-31
- **当前阶段**：工单已闭合，待归档

## 里程碑状态

| 里程碑 | 状态 | 关闭日期 | 备注 |
|---|---|---|---|
| M1：P0 基础修复与基础设施 | 🟢 已关闭 | 2026-08-31 | 经理终审通过；验收命令全绿 |
| M2：P1 工作区拆分与响应式 | 🟢 已关闭 | 2026-08-31 | 经理终审通过；工作区组件化、Hooks 拆分、DataTable 升级、响应式第一版完成；构建全绿 |
| M3：P2 体验打磨与可访问性 | 🟢 已关闭 | 2026-08-31 | 经理终审通过；全局 Toast、快捷键、Modal 焦点陷阱、暗色模式、eslint-plugin-jsx-a11y、style.css 瘦身完成；构建与 lint 全绿 |

## Golden 数值（M1 后）

| 指标 | 当前值 | 目标 | 说明 |
|---|---|---|---|
| `App.tsx` 行数 | 155 | ≤ 200（M2 结束时） | M2 已将全部 workspace JSX 拆分到 `features/*` 页面组件；M3 增加键盘快捷键与命令面板后仍达标 |
| `window.confirm` 数量 | 0 | 0 | 已替换为 `DangerDialog` / `ConfirmDialog` |
| 未定义 CSS 类名区域 | 0 | 0 | Search / Notes / Import 预览 / Source Tab 等已补样式 |
| 导航 active 态正确率 | 100% | 100% | 新 `Sidebar` 组件使用 `.active`，与 CSS 匹配 |
| 图标库 | lucide-react | lucide-react | 已安装并应用于导航、收藏、导入/导出/删除/打开等关键操作 |
| 废弃类名残留 | 0 | 0 | 无 `className="app"` / `className="selected"`；`className="sidebar"` 仅存在于新的 `Sidebar` 组件 |
| DataTable 升级 | ✅ 支持排序/分页/列宽/固定首列 | 支持排序、分页、列宽 | 新 `DataTable` 组件已接入 Source Table tab |
| 响应式第一版 | ✅ 完成 | 完成 | 侧边栏可折叠为 rail；三栏布局在 1024px 以下降级；无 `min-width: 1180px` |
| 全局 Toast | ✅ 接入 | 全局反馈统一 | `ToastProvider` + Context；关键操作均有 Toast 提示 |
| 快捷键 | ✅ 完成 | Ctrl/Cmd+1-6、Ctrl/Cmd+K、Ctrl/Cmd+N/S、Esc 可用 | `useKeyboardShortcuts` + `CommandPalette` |
| Modal 焦点陷阱 | ✅ 完成 | Esc 关闭、焦点循环、恢复焦点 | `useFocusTrap` + `Dialog` 升级 |
| 暗色模式 | ✅ 完成 | Settings 可切换 light/dark/system；ECharts 随主题变色 | `useTheme` + `data-theme` + `useChartTheme` |
| a11y | ✅ 完成 | eslint-plugin-jsx-a11y 配置完成且无阻断报错 | `aria-label`、表单 label、`aria-live` 已补齐 |
| style.css 瘦身 | ✅ 完成 | 移除 M1/M2 遗留死代码与硬编码颜色，feature 样式下沉到 CSS Modules | 从 869 行减至 135 行 |
| 构建验证 | ✅ 通过 | 通过 | `pnpm build` + `cargo check -p cistella-desktop` + `npx eslint src --max-warnings=0` 全绿 |

## 遗留清单

- M2 已解决：
  - `App.tsx` 已按工作区拆分为 `features/*` 页面组件，行数降至 120 行。
  - `DataTable` 已升级并接入 Source Table，支持排序、分页、列宽、固定首列与空状态。
  - 响应式第一版完成：侧边栏可折叠、三栏布局在 1024px 以下降级、移除 `min-width: 1180px`。
  - 原生 `<button>` 已在页面组件中大量替换为 `<Button>`；少量列表/表格内的原生元素保留原样式以兼容现有 CSS。
- M3 已解决：
  - 全局 Toast 已替换 `literatureFeedback` / `readingFeedback` / `noteFeedback` / `remoteCacheFeedback` 等内联反馈；关键操作有 Toast 提示。
  - 快捷键体系已落地：`Ctrl/Cmd+1-6` 切换工作区、`Ctrl/Cmd+K` 命令面板、`Ctrl/Cmd+N/S` 新建/保存、`Esc` 关闭弹窗/退出编辑器。
  - 全部 Modal 已升级焦点陷阱、Esc 关闭、焦点恢复、`aria-labelledby/describedby`。
  - 暗色模式支持浅色 / 深色 / 跟随系统，ECharts visual tab 动态配色。
  - `eslint-plugin-jsx-a11y` 已配置并通过，补齐 `aria-label`、表单 label、`aria-live`。
  - `style.css` 已大幅瘦身，移除 M1 遗留死代码并将剩余硬编码颜色替换为 design tokens。
- M3 → 后续可选：
  - `MainHeader` 与各页面 `PageHeader` / `CardHeader` 当前风格一致，如需进一步统一可在后续迭代评估。

## 验证证据

- 探索阶段审计报告：见 `04-施工日志.md` 初始记录。
- 设计阶段顾问方案：见 `01-目标与设计.md` §4。
- 阶段门审查记录：见本文件「阶段门记录」。
- M2 验收命令结果（2026-08-31）：
  - `pnpm build` ✅ 通过（vite build 成功，无 TS 错误）。
  - `cargo check -p cistella-desktop` ✅ 通过。
- M3 验收命令结果（2026-08-31）：
  - `pnpm build` ✅ 通过。
  - `cargo check -p cistella-desktop` ✅ 通过。
  - `npx eslint src --max-warnings=0` ✅ 通过（无 jsx-a11y 阻断报错）。
- M2/M3 代码审查：
  - `wc -l crates/openalex-analysis-studio/src/App.tsx` → 120 行。
  - `grep -R "window.confirm" crates/openalex-analysis-studio/src` 无实际调用。
  - `grep -R "1180px" crates/openalex-analysis-studio/src` 无命中。
  - 全部六个工作区页面组件位于 `src/features/*`。
  - 全部自定义 Hooks 位于 `src/hooks/*`。
  - `DataTable` 组件位于 `src/components/ui/DataTable/DataTable.tsx`。
  - `style.css` 行数从 869 降至 135，feature 样式已下沉到 CSS Modules。

## 阶段门记录

- **构建中 → 施工中**：2026-08-31，经理终审。确认 A 类问题已收口：用户已授权系统性重构范围；视觉方向沿用现有品牌优化；不改动后端与核心数据模型。准予进入施工期，开始 M1。
- **M1 关闭**：2026-08-31，经理终审。验收命令 `pnpm build` + `cargo check -p cistella-desktop` 全绿；4 处 `window.confirm` 已替换；旧类名 `.app` / `.selected` 已清除；图标体系 `lucide-react` 已引入；基础 UI 组件与布局组件已创建。M1 已关闭，遗留问题转入 M2/M3。
- **M2 关闭**：2026-08-31，经理终审。`App.tsx` 已降至 120 行；类型/字典/hooks 已归集；六个工作区页面组件已拆分；`DataTable` 已升级并接入 Source Table；响应式第一版完成；初审发现并修复 Notes 筛选器不刷新问题；`pnpm build` + `cargo check -p cistella-desktop` 全绿。M2 已关闭，遗留问题转入 M3。
- **M3 关闭**：2026-08-31，经理终审。全局 Toast、快捷键、Modal 焦点陷阱、暗色模式、`eslint-plugin-jsx-a11y`、style.css 瘦身均已完成；`pnpm build` + `cargo check -p cistella-desktop` + `npx eslint src --max-warnings=0` 全绿；遗留的可选优化转入后续迭代。
- **工单 15 闭合**：2026-08-31，经理终审。M1/M2/M3 全部关闭，验收命令全绿，无未解决 blocker。准予归档。
