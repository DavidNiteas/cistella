# 04. 施工日志：阅读工作区 MVP

- 2026-08-26：经理完成 B 层设计收口；顾问建议已纳入，确定“库内副本为真相、外部链接为显式例外”。
- 2026-08-26：工单通过阶段门，进入施工；等待干事按 `02-施工路线图.md` 实施 M1—M4。

- 2026-08-26：干事完成 M1（core 文件关联与路径安全）。`LiteratureItem` 以默认字段兼容旧 JSON；新增库内 PDF 复制、外部绝对链接、默认文件、移除关联与受控解析。库内文件固定写入 `files/<item_id>/`，路径在写入和打开前均验证为条目自己的库内相对 PDF，且规范化结果不得逃出 Vault。
- 2026-08-26：干事完成 M2（Tauri 受控打开）。新增 `add_literature_vault_file`、`add_literature_external_file`、`remove_literature_file`、`set_literature_default_file` 与 `open_literature_file` 命令。打开命令只接受 item/file 标识，后端经 core 解析后通过 `tauri-plugin-opener` 发起系统打开；未增加任意路径打开接口，也未写入阅读状态。
- 2026-08-26：干事完成 M3（Reading 工作区闭环）。条目卡片支持加入 Vault、仅链接外部 PDF、打开默认/指定文件、设默认、移除关联及阅读状态操作；外部文件显式标记不随库复制。打开或路径解析失败仅显示文件操作反馈，关联、默认文件和阅读状态保持不变。
- 2026-08-26：干事完成 M4 回归。`cargo test -p cistella-core` 通过（19 passed，0 failed）；`cargo check -p cistella-desktop` 通过；`pnpm build` 通过。Vite 仅报告既有 bundle 大小提示，不阻断构建。
- 2026-08-26：干事回填状态账本，工单进入“施工收口完成，待经理验收关闭”。未修改顶层设计路线、顶层开发状态或其他工单。

- 2026-08-26：干事按 M1 范围复核既有 core 实现并补充符号链接逃逸回归测试。确认模型兼容、PDF 约束、Vault 副本、外部绝对链接、默认文件和受控解析均符合已拍板设计；未修改 Tauri、前端或顶层治理文件。已在当前 Windows 环境实际创建越界符号链接并验证解析被拒绝。
- 2026-08-26：经理按用户明确要求重置工单 07 的施工治理状态：任何既存的 M1—M4 范围代码和历史“完成”回报均只作为候选实现，不得替代“每个里程碑由可见干事实施、再由不同干事独立初审”的闭环证据。当前仅 M1 等待独立初审派发；M2—M4 尚未放行或派发。
- 2026-08-26：经理以当前工作树复跑 `cargo test -p cistella-core`，结果为 20 passed、0 failed。预核验记录：`next_vault_file_relative_path` 的存在性检查与 `fs::copy` 之间仍需由 M1 初审重点审查无覆盖/悬空符号链接/并发写入语义；在初审结论前不得放行 M2。


- 2026-08-26：M1 独立初审通过：20 项 core 测试通过；旧 JSON、PDF 限制、库内复制、外部链接、默认规则、路径/符号链接逃逸与失败不破坏均已核验。经理裁定允许进入 M2。

- 2026-08-26：M2 实施（重派）完成当前候选的实审与收口。桌面后端已注册五个文献文件命令：添加库内副本、添加外部链接、移除关联、设默认与打开。`open_literature_file` 仅接收 `itemId`、`fileId`，由 core 的 `resolve_literature_file_path` 完成受控 PDF 路径解析后，使用官方 `tauri-plugin-opener` 的 Rust `OpenerExt` 接口交由系统默认应用打开；WebView 未获 `opener` 能力，不能绕过该后端命令指定任意路径。打开请求仅返回 `requestAccepted`，不写入 `reading_status` 或任何 Vault 用户数据。待运行 `cargo check -p cistella-desktop` 后由不同干事独立初审。
- 2026-08-26：M2 实施验证：`cargo fmt --check` 与 `cargo check -p cistella-desktop` 均成功。构建输出含既有 Rust incremental 编译目录“拒绝访问”提示，但命令以成功状态结束，未出现编译错误。M2 交付进入待不同干事独立初审状态；在初审通过前不得放行 M3。

- 2026-08-26：M2 独立初审通过：五个后端命令、受控 item/file 打开、core 路径验证、官方 opener 与最小 WebView 权限、无状态写回均已核验；`cargo fmt --check` 与 `cargo check -p cistella-desktop` 通过。经理裁定允许进入 M3。

- 2026-08-26：M3 实施完成（Reading 工作区闭环）。在 `App.tsx` 审查既有候选后补齐了编辑态文件关联区：已保存条目可在编辑态“加入 Vault”或“仅链接外部 PDF”，并可查看文件列表、打开指定文件、设置默认和移除关联；新条目在保存前明确提示不能关联文件。条目卡片保留同一套文件操作，并以“库内副本 · 可随库复制”或“外部链接 · 不随库复制”显示 portable 边界。没有有效默认文件时，卡片及编辑态均明确提示用户从文件列表选择，打开默认不会猜测性选择文件。
- 2026-08-26：M3 的 Vault 状态闭环：连接、恢复、刷新上下文时先清空旧库条目与文件反馈，再加载当前 Vault 的条目；异步列表响应会按当前 Vault 路径丢弃过期结果；连接/刷新失败清空 Reading 状态。文件操作继续只调用受控的 `itemId + fileId` 后端 command，失败保留原列表与阅读状态。
- 2026-08-26：M3 验证：在 `crates/openalex-analysis-studio` 运行 `pnpm build` 成功（`tsc && vite build` 通过）。Vite 报告既有单 bundle 大于 500 kB 的非阻断提示；无 TypeScript 或构建错误。M3 待不同干事独立初审，初审通过前不得放行 M4。

- 2026-08-26：M3 独立初审通过：前端文件关联、portable 提示、默认文件、失败不破坏、Vault 切换与异步旧响应保护均已核验；`pnpm build` 通过。经理裁定允许进入 M4。

- 2026-08-26：M4 实施干事完成回归、portable 验收与归档准备。新增 `crates/openalex-analysis-core/tests/工单07_m4_portable.rs` 作为最小公共 API 集成验证，不修改既定业务范围。
- 2026-08-26：M4 portable 验证实际创建原始 Vault 与临时复制 Vault；确认库内 PDF 记录为 `files/<item_id>/incoming-paper.pdf`，复制 Vault 后通过受控解析得到复制库内文件；确认外部 PDF 记录为绝对路径，复制后不以复制库为根，删除原外部文件后解析失败；确认复制库内库文件缺失时条目 JSON 保持不变；确认 `.literature_items.*.tmp` 无残留，并清理本轮验证临时目录。
- 2026-08-26：M4 完整验收命令通过：`cargo test -p cistella-core`（20 个单元测试、1 个 M4 集成测试、0 个失败；Doc-tests 0 个失败）、`cargo check -p cistella-desktop`（退出码 0）、`pnpm build`（`tsc && vite build` 成功）。Vite bundle 大小提示与 Rust incremental 目录拒绝访问提示均为非阻断既有环境提示。
- 2026-08-26：M4 首次运行中发现 Windows canonical 路径使用 `\\?\` 前缀导致验收测试直接与普通绝对路径比较失败；该问题仅存在于新增验证断言，不是产品业务缺陷。已将测试断言改为比较 canonicalized Vault 根与目标路径，复跑通过；无业务代码修复或设计偏离。
- 2026-08-26：M4 施工交付，状态为“实施完成，待不同干事独立初审”。依照治理协议，下一步应由不同于 M4 实施者的可见干事执行只读独立初审；初审通过后交经理终审，不得直接归档。

- 2026-08-26：M4 独立初审不通过。阻断项：现有 portable 测试只检查正常流程后的临时文件不存在，未触发“临时文件已写入、替换失败、临时文件清理”的真实路径；既有单元测试同样在临时文件写入前失败。经理裁定退回 M4 返工，禁止经理终审与归档。

- 2026-08-26：M4 返工完成（针对首次独立初审唯一阻断项）。未削弱原 portable 断言，也未改变业务持久化语义；仅将原子写入内部流程拆为 `atomic_replace` 委托 `atomic_replace_with` 的等价替换阶段 seam。新增 `atomic_replace_cleans_temp_after_replace_phase_failure`：先写入已有用户文件，再在替换阶段观察并读取已写入的 `.literature_items.*.tmp` 临时文件，随后注入 `PermissionDenied` 替换错误；断言错误返回后临时文件已被删除、同目录无残留临时文件且原用户文件字节保持不变。该测试直接覆盖“临时文件已写入 → 替换阶段失败 → 清理 → 原文件不变”的失败分支。
- 2026-08-26：M4 返工验证待执行：`cargo test -p cistella-core`、`cargo check -p cistella-desktop` 与 `pnpm build`。返工交付后必须由不同于 M4 实施者的干事复审；复审通过前不得经理终审或归档。
- 2026-08-26：M4 返工验证完成。最终 `cargo test -p cistella-core` 通过（22 个单元测试、1 个 M4 集成测试、0 个失败，Doc-tests 通过）；此前 `cargo check -p cistella-desktop` 与 `pnpm build` 均已通过，返工仅增加 core 测试辅助 seam 与测试，不改变桌面或前端编译路径。M4 返工交付，提交不同干事复审。

- 2026-08-26：M4 返工完成：通过 `atomic_replace_with` 测试 seam 与真实替换失败测试，证明临时文件已写入后替换失败会清理临时文件且原目标不变；`cargo test -p cistella-core` 22 passed。
- 2026-08-26：不同干事完成 M4 返工后复审并通过；确认 portable 集成测试、M1—M3 契约与三条验收命令均无阻断。
- 2026-08-26：经理终审完成：独立复跑 `cargo test -p cistella-core`、`cargo check -p cistella-desktop`、`pnpm build`，全部通过；确认 bundle 与 incremental 编译提示为非阻断环境提示。工单关闭，准备归档。
