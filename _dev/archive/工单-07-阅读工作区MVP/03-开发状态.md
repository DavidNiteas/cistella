# 03. 开发状态：阅读工作区 MVP

## 当前阶段

已达成，已归档

## 阶段门与闭环结论

- 2026-08-26：通过阶段门，进入施工。
- M1：实施完成，独立初审通过。
- M2：实施完成，独立初审通过。
- M3：实施完成，独立初审通过。
- M4：实施完成；首次独立初审发现临时文件清理测试覆盖不足，返工后由不同干事复审通过。
- 2026-08-26：经理完成独立终审，允许工单关闭并归档。

## 施工里程碑

- M1：core 文件关联与路径安全 API。**已完成，独立初审通过**
- M2：Tauri 受控系统打开命令。**已完成，独立初审通过**
- M3：Reading 工作区文件管理与打开闭环。**已完成，独立初审通过**
- M4：回归、portable 验收与归档准备。**已完成，返工后复审通过**

## 经理终审证据

经理于 2026-08-26 以当前工作树独立复跑并确认：

```text
cargo test -p cistella-core
```

- 22 个 core 单元测试通过。
- 1 个 M4 portable 集成测试通过。
- 0 failed。
- Doc-tests 通过。

```text
cargo check -p cistella-desktop
```

- 通过，退出码 0。

```text
pnpm build
```

- `tsc && vite build` 通过。
- 629 modules transformed。
- 仅有既有 bundle 体积提示，不阻断验收。

经理还审阅了：

- `crates/openalex-analysis-core/tests/工单07_m4_portable.rs` 的复制库、外部链接、缺失文件和临时目录清理验证；
- `literature.rs` 中 `atomic_replace_with` 的注入失败测试与真实 `replace_file` 失败测试；
- M1—M3 的条目、受控打开和 Reading 工作区契约。

## 关闭裁定

工单 07 达成目标，符合关闭标准，允许归档。后续阅读器内置预览、EPUB、笔记、标注、全文检索、同步等目标不得回填本工单，另立后续工单。

## 已知边界

- 首版仅 PDF 与系统默认阅读器。
- 不包含内置预览、笔记、标注、全文检索、OCR、同步或自动合并。
- 移除关联不会隐式删除 Vault 中的用户文件。
- 外部绝对路径是可断裂的本机链接；文件缺失或没有默认 PDF 应用时只报告本次失败，不改条目、默认文件或阅读状态。
