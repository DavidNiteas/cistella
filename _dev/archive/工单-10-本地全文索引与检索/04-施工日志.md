# 04. 施工日志：本地全文索引与检索

## 2026-08-26｜设计期立项与文档收口

- 经理创建工单 10 四件套，将工单定位为 Vault 内本地全文索引与检索能力的设计期工作单元。
- 锁定派生数据边界：`derived/search/` 独立、可删、可重建、可复制；不得写入 `manifest.json`、`tables/`、`user/`、`files/`，不得持久化或返回绝对路径。
- 锁定首版范围：文献元数据统一可检索；仅可用库内 PDF 文本层建立正文索引；外部资产不建正文索引，仅保留元数据命中；排除 OCR、多格式提取、向量/LLM/RAG、在线服务和 OpenAlex 专属通道。
- 锁定实现与 portable 原则：进程内 Rust 引擎、纯 Rust PDF 提取、Core 内部依赖隔离、`item_id + asset_id` 稳定引用、命中资产仅通过既有受控打开链路打开。
- 锁定一致性原则：增量更新加 reconciliation；内容 hash 改变且新提取失败时移除旧正文命中；新代际在 working 构建后原子切换，失败保留旧成功索引。
- 锁定桌面并发边界：M3 设立独立 Search 工作区，所有异步查询/状态/任务反馈必须复用工单 09 的 `generation + Vault path` 隔离。
- 已定义 M1—M4 的施工步骤、关闭标准、统一验收命令和 TBD by experiment 回填账户。

### 阶段门批注

- 阶段门结论：**未通过**。
- 原因：本次授权仅限设计期文档落单；尚未开始 M1，也未进入施工循环。
- 施工纪律：不得派发实施或初审干事，不得写入产品代码、依赖、测试或配置；工单 11—12 保持未开启。

### 本次改动范围

仅治理文档：

```text
_dev/工单-10-本地全文索引与检索/01-目标与设计.md
_dev/工单-10-本地全文索引与检索/02-施工路线图.md
_dev/工单-10-本地全文索引与检索/03-开发状态.md
_dev/工单-10-本地全文索引与检索/04-施工日志.md
_dev/设计路线.md
_dev/开发状态.md
```

未运行产品构建或测试命令：本次没有代码、依赖、测试或配置变更，统一验收命令已登记为未来施工里程碑的关闭要求。

## 2026-08-26｜M1 索引基座实施完成（待不同实例干事初审）

### 阶段门与施工范围

- 经理已裁定工单 10 阶段门**通过**，本次实施授权严格限定为 M1《索引基座》。
- 本实施未启动 M2 的 PDF 文本提取/限额/reconciliation，未启动 M3 的查询 API、Tauri 命令或 Search 工作区，未启动 M4，也未开启工单 11—12。

### 已交付

- 新增 Core `search` 模块与 ID-only 领域模型：`SearchIndexStatus`（`missing` / `ready` / `stale` / `building` / `degraded` / `failed`）、状态视图、版本化 manifest、文献元数据记录、资产内容记录契约和 M2 的 `pendingExtraction` 占位状态。
- 索引派生数据严格位于 `derived/search/`；建立 `working/<generation>` → `generations/<generation>` → `manifest.json` 的受控提交路径。构建失败时清理 working；有既有 ready generation 时失败不改变 active manifest；首次失败以派生 `failure.json` 表达 failed 状态。
- 元数据索引真实读取文献条目的 title/authors/tags，并保留确定性 metadata fingerprint；可用、库内 PDF 仅登记为待 M2 提取的内容 worklist。外部、缺失、不可用、非 PDF 或孤儿资产不写入内容记录；本 M1 不读取 PDF 正文。
- 所有持久化记录和可加载的 Core 结果仅使用 `item_id`、`asset_id` 与派生字段，不含 Vault 根目录、PDF 路径、盘符、用户名或临时路径。派生目录被删除时，`search_index_state` 返回 `missing`，而 Vault 的权威数据继续可读。
- 未新增 Cargo 依赖；M1 采用版本化 JSON generation 作为可检查、可复制的最小元数据索引基座，搜索查询与引擎封装仍明确留给 M3。

### 定向验证

- 新增 6 个 Core M1 单测：权威文件全快照不变与 ID-only persistence；可用库内 PDF worklist 与外部/不可用资产隔离；失败 generation 清理与旧 ready generation 保留；首次失败不阻塞 Vault；版本不兼容 stale/重建；复制 Vault portable 与删除索引后主库可用。
- 已执行并通过：

  ```powershell
  cargo test -p cistella-core
  cargo test -p cistella-desktop
  cargo check -p cistella-desktop
  # crates/openalex-analysis-studio
  pnpm build
  ```

- 实况：Core 49 个库内测试、工单 07/08/09 portable 集成测试共 6 个全部通过；Desktop 2 个竞态测试通过；`cargo check` 通过；前端构建通过。Rust 命令出现已有的 incremental 缓存目录 `os error 5` 非阻断提示，Vite 提示单个压缩后 chunk 超过 500 kB，均未阻断本里程碑验收。

### 偏离申请与风险

- **无偏离申请。**未引入 Tantivy 或 PDF 提取依赖：这是遵守 M1 只建立基座、M2 才进行文本提取与资源实验的范围控制，不改变已锁定的 M2/M3 方向。
- 待不同实例干事初审的重点：检查派生目录的 Windows 路径/符号链接安全、manifest 提交失败时的旧 generation 保护、状态语义是否足以供后续 M2/M3 消费，以及新增公开 Core 类型未泄漏路径。


## 2026-08-26｜M1 P1 返工完成（待新的不同实例干事复审）

### 范围纪律

- 本次仅处理 M1 初审退回的三项 P1 与设计期阶段门叙述冲突：未启动 M2 的 PDF 文本提取、资源限额或 reconciliation，未启动 M3 查询执行 API、Tauri 命令或 Search 工作区，未启动 M4，也未开启工单 11—12。

### P1 闭环

1. **派生目录路径边界**：删除仅以 canonical `starts_with` 判断的方案。现在以 `symlink_metadata` 逐段验证 Vault 下预期的 `derived`、`derived/search`、`working`、`generations` 及 working/committed generation 都是本体目录；Windows 同时检查 `FILE_ATTRIBUTE_REPARSE_POINT`，故 directory symlink、junction 和其他 reparse point 一律拒绝。目录仅在父组件已验证后逐级 `create_dir` 创建；原子写不再调用 `create_dir_all`。这防止 `derived/search -> user/files` 写入权威目录，也防止 `derived -> Vault 外` 在安全检查前创建 `search/`。
2. **最小检索领域契约与真实分析基座**：新增纯 Core 的 `SearchQuery` / `SearchFieldScope`、文献级 `SearchHit`、`SearchFieldMatch`、任务状态以及资产级 `SearchIndexIssue`；不提供查询执行/桌面接口。`MetadataIndexRecord` 同时保存原始显示字段和每字段的无路径 terms。分析器固定为 Unicode NFKC 后 lowercase、字母数字词边界、连续 CJK 2-gram。固定样本 `ＦＯＯ Rust 与机器学习`、`ÁLICE Müller`、`张伟`、`Data-Science`、`知识图谱` 验证显示保留、稳定 term、字段可辨、文献级多资产匹配形状和无路径 JSON。
3. **版本状态一致性**：状态判断补齐 generation 的 metadata analyzer / PDF extractor 版本检查，与 load 规则一致；两个字段任一漂移都报告 `stale`，load 返回 unavailable 而非状态 `ready` 后读取失败。
4. **治理语境**：01/02 中原“阶段门未通过/禁止施工”改为带日期的历史设计期记录；当前动态授权仍只以 03 为准。

### C 层实验回填

- **CJK window = 2**：作为 M1 确定、可序列化、无需全文引擎的相邻字符基线。固定样本确认可产生 `与机`、`机器`、`器学`、`学习` 和 `知识`、`识图`、`图谱`；该选择不承诺排序/召回效果。索引体积、PDF 正文资源限额与并发仍按原计划留给 M2。

### 路径安全限制与权衡

- 本实现使用 Rust 标准库的 `symlink_metadata` 和 Windows 文件属性，跨平台拒绝符号链接，并在 Windows 拒绝全部 reparse point；不再把 canonical 后“仍在 Vault 内”误当作安全。
- 标准库未提供跨平台、句柄级 `openat(..., O_NOFOLLOW)` 等完全消除恶意并发替换的原语，因此每一个创建/提交/原子写前均重验目录链，覆盖正常应用内与测试可构造的重定向；对拥有同等本地文件系统权限的对手，仍存在检查与操作之间的系统级 TOCTOU 窗口。该限制不扩大到权威目录写入，后续若形成威胁模型需求，应单列安全工单而非在 M1 引入平台专有服务。

### 待复审证据

- Core 定向 `search` 测试已通过 9 项（含 3 个 Windows junction 场景、generation 版本漂移、Unicode/CJK 字段分析）。
- 已实际执行并通过统一验收：`cargo test -p cistella-core`（52 个库内测试 + 工单 07/08/09 portable 6 个测试）、`cargo test -p cistella-desktop`（2 个竞态测试）、`cargo check -p cistella-desktop`、`pnpm build`（629 modules）。Rust incremental 缓存收尾仍有既有 `os error 5` 非阻断提示；Vite 报告单 chunk 1,374.25 kB（gzip 450.58 kB）超过 500 kB，未阻断构建。
- 返工完成，现**待新的不同实例干事独立复审**；本实施干事不作初审或终审。


## 2026-08-26｜M1 第二次复审 P1 范围受限返工

### 触发与范围

- 第二次独立复审指出：`generations/<active-id>` 在状态检查/加载路径可能未经过 no-follow/reparse 验证，`working/<id>` 子目录也未被一致地作为不安全布局处理。
- 本实施仅闭环这一个 P1；没有改动 PDF 提取、索引查询执行、Tauri/UI、M4 或工单 11—12，未派发子代理，未清理或回滚既有未提交变更。

### 实施记录

1. `search_index_state()` 先验证整个 `working/` 子项集合；存在不安全子项即报告 `degraded`，不会把其解释为普通 `building`。解析 manifest 的 active generation 后，在读取 `metadata.json` 前再次验证 `derived/search/generations/<active-id>` 及父目录链；失败直接 `degraded`，不跟随链接读取兼容 JSON。
2. `load_search_index_generation()` 保留状态门槛，但在该门槛之后重新取得 expected active generation 路径并独立调用目录链验证；验证失败映射为 `SearchIndexUnavailable`，避免“先前 ready 状态”成为后续文件读取的安全依据。
3. `has_incomplete_search_working_generation()` 从布尔 best-effort 扫描改为 `Result<bool>`：逐项 `symlink_metadata` + Windows reparse 属性验证，每一个正常子项才可成为 working generation；异常、链接、junction、reparse point 和非目录条目均向上返回安全错误，状态语义为 `degraded`。
4. 新增 Windows junction 回归：active generation 指向 Vault 外、`user/`、`files/` 的三种兼容 `metadata.json` 目标，以及 working generation 指向 Vault 外兼容 metadata；均验证 `degraded`、`load_search_index_generation()` 安全失败、Vault/外部目标快照不变。测试专门将目标 metadata 做成格式兼容，以证明拒绝发生于读取前。

### 施工结论

- P1 所指的 generation/working 子目录读取面已按既有 M1 “拒绝 symlink/junction/reparse point”策略闭环。
- 标准库路径检查仍保留已披露的同权限恶意并发替换 TOCTOU 限制；本次没有以平台专有实现扩大 M1 范围。
- 返工完成后，M1 **待新的不同实例干事最终复审**；本实施干事不作初审或终审。

### 验收

- 已先通过定向验证：`cargo test -p cistella-core search -- --nocapture`，10 项 search 单元测试通过（含新增 committed/working generation junction 覆盖）。
- 完整统一验收的实际输出见本条目后续结果；验收范围固定为 Core、Desktop、Desktop check 与前端生产构建。

### 完整统一验收结果（2026-08-26）

```powershell
# F:\dev-repo\cistella
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

全部通过：Core 53 个 library 测试、工单 07/08/09 portable 集成测试 6 项、Desktop 2 项竞态测试；Desktop `cargo check` 通过；前端构建转换 629 个 modules 并成功产出。非阻断提示仍为 Rust incremental 缓存目录 `os error 5` 拒绝访问，以及 Vite 主 chunk 1,374.25 kB（gzip 450.58 kB）超过 500 kB。

## 2026-08-26｜M1 最后一个文件级路径安全 P1 范围受限返工

### 执行边界

- 接收最终独立复审退回的单一 P1：目录链已安全，但 `manifest.json`、active generation `metadata.json` 与 `failure.json` 仍可能作为文件级 symlink / reparse point 被状态或 loader 跟随。
- 本次只修改 Core `search` 模块、其 Windows 定向测试与本工单动态状态/日志；未修改 PDF 提取、Tauri/UI、查询执行、M4、工单 11/12，未派发子代理，未清理或回滚历史工作树。

### 实施记录

1. 增加索引自有普通文件校验，使用 `symlink_metadata` 验证文件必须为普通文件，拒绝 symlink；Windows 同时拒绝 `FILE_ATTRIBUTE_REPARSE_POINT`。保留目录与文件的独立校验，避免“目录安全即文件安全”的错误假设。
2. `search_index_state()` 在进入 manifest / failure 状态分支前验证它们；在读取 active generation `metadata.json` 前再验证该文件。任一不安全文件均报告 `Degraded`，不会降级解释为正常的缺失、失败或构建中状态。
3. `load_search_index_generation()` 在 status 已为 ready 后，仍在实际 metadata JSON 读取前独立做普通文件校验；不安全路径统一返回 `SearchIndexUnavailable`。JSON 读取统一封装为先做普通文件校验再 `fs::read`，避免以后新增读取点绕过该约束。
4. 新增并实际执行 Windows 文件级 symlink 回归：外部兼容 manifest、外部兼容 metadata、Vault `user/` 中兼容普通 metadata、外部兼容 failure。测试以兼容 JSON 作为链接目标，证明结果为 `Degraded` 是安全拒绝而不是 JSON 损坏偶然导致；还断言 loader 失败以及 Vault/外部目标完整快照不变。
5. 安全的普通文件若内容损坏，仍由既有状态逻辑处理；本次没有把普通 JSON 解析故障改写为路径安全错误。标准库无跨平台句柄级 no-follow 原语导致的同权限 TOCTOU 限制继续按前序记录保留。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo test -p cistella-core search -- --nocapture
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- Search 定向测试：11 项通过。
- Core：54 个 library 测试通过；工单 07 / 08 / 09 portable 集成测试 6 项通过。
- Desktop：2 项竞态测试通过；`cargo check` 通过。
- 前端构建：629 modules transformed，成功产出；Vite 提示主 chunk 1,374.25 kB（gzip 450.58 kB）超过 500 kB，未阻断。
- Rust incremental session 收尾在 Desktop test/check 中仍报告既有 `os error 5` 拒绝访问，未影响命令成功。

### 交接结论

- 最后一个文件级路径安全 P1 已完成范围受限返工，**待新的不同实例干事最终复审**。
- 本实施干事不进行复审或经理终审；M2—M4 及工单 11—12 继续锁定。


## 2026-08-26｜M1 悬空 `derived` 目录链接状态语义 P1 范围受限返工

### 执行边界

- 接收独立复审退回的单一 P1：`search_index_state()` 在检查 `derived/` 和 `derived/search/` 时使用 `Path::exists()`，该 API 会跟随悬空 symlink/junction/reparse point，使不安全对象被误报为 `Missing`。
- 本次只修改 Core `search` 模块、Windows 定向测试与本工单动态状态/日志；未进入 M2—M4，未修改 PDF 提取、Tauri/UI、查询执行或工单 11—12，未清理、回滚或重置历史未提交变更。

### 实施记录

1. 新增可选目录的 no-follow 检查：以 `fs::symlink_metadata()` 将 `NotFound` 识别为真正缺失；任何现存但不是本系统拥有的真实目录的对象（包括 symlink、Windows junction/reparse point、非目录）一律返回路径安全错误。
2. `search_index_state()` 在任何 `derived/`、`derived/search/` 的 `Path::exists()` 分支之前执行该检查。只有真正不存在的目录可形成 `Missing`；悬空或非悬空重定向对象统一形成 `Degraded`。loader 继续通过状态门槛映射为 `CoreError::SearchIndexUnavailable`。
3. 新增 Windows 回归：分别建立 `derived -> Vault 外不存在目标` 与 `derived/search -> Vault 外不存在目标` 的目录符号链接；均断言状态为 `Degraded`、loader 安全失败、Vault 和外部目标递归快照不变，且目标目录没有被读取或创建。

### 本次验收证据（2026-08-26）

- 定向验证：`cargo test -p cistella-core dangling_derived_directory_links_are_degraded_without_reading_or_writing_targets -- --nocapture` 通过（1 项）。
- 完整统一验收（均通过）：
  - `cargo test -p cistella-core`：55 个 Core library 测试通过；工单 07 / 08 / 09 portable 集成测试共 6 项通过。
  - `cargo test -p cistella-desktop`：2 项竞态隔离测试通过。
  - `cargo check -p cistella-desktop`：通过。
  - `pnpm build`：通过，629 modules transformed；主产物 1,374.25 kB（gzip 450.58 kB）。
- 非阻断环境提示：Rust incremental session 收尾仍报告既有 `os error 5` 拒绝访问；Vite 仍提示单个 chunk 超过 500 kB。
- M1 仍**待新的不同实例干事复审**，本实施不作初审或经理终审；M2—M4 与工单 11—12 继续锁定。

## 2026-08-26｜M1 索引自有文件重建写入 P1 范围受限返工

### 执行边界

- 接收独立复审退回的单一 P1：`rebuild_metadata_search_index()` / `prepare_search_root()` 的写入路径不得删除、原子替换或静默修复指向外部的 `failure.json`、`manifest.json` 或同类索引自有文件。
- 本次仅修改 Core `search` 模块、Windows 定向测试与工单 10 的动态状态/施工日志；未进入 M2—M4，未触及 PDF 提取、Tauri/UI、查询执行或工单 11—12，未派发子代理，未清理、回滚或重置历史未提交变更。

### 实施记录

1. `prepare_search_root()` 在完成目录链 no-follow 验证后，额外验证 root-owned `manifest.json` 与 `failure.json`：允许真实缺失或已验证普通文件，拒绝 symlink、junction、Windows reparse point 与非普通文件；拒绝发生在 working generation 创建及任何构建侧写入之前。
2. 新增受控删除入口。重建在清除旧 `failure.json` 前必须验证该路径仍是普通索引自有文件；不安全对象立即返回 `CoreError::InvalidSearchIndexPath`，不会把链接当作可清理的派生垃圾。
3. 原子写入在验证父目录后、创建临时文件/替换目标前再次验证既有目标。该约束同时保护 `manifest.json`、`failure.json` 与 generation `metadata.json`：普通文件可按既有原子语义更新，链接/reparse 不会被静默替换成常规文件。
4. 失败标记写入分支在写入前也显式重验 `failure.json`；若其在重建期间变为不安全布局，优先返回 `InvalidSearchIndexPath` 而不掩盖为普通构建失败。标准库不具备句柄级无跟随原语的同权限 TOCTOU 限制继续按前序记录保留。
5. 新增 Windows 回归 `rebuild_rejects_index_owned_file_links_without_repairing_them`：分别将 `failure.json` 与 `manifest.json` 指向 Vault 外兼容 JSON，调用重建并断言 `InvalidSearchIndexPath`、链接对象仍存在、Vault 与外部目标递归快照完全不变。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core search -- --nocapture
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- Search 定向测试：13 项通过。
- Core：56 个 library tests 通过；工单 07 / 08 / 09 portable 集成测试共 6 项通过。
- Desktop：2 项测试通过；`cargo check` 通过。
- 前端构建：629 modules transformed，成功产出；Vite 提示主 chunk 1,374.25 kB（gzip 450.58 kB）超过 500 kB，未阻断。
- Rust incremental session 收尾仍报告既有 `os error 5` 拒绝访问，未影响命令成功。

### 交接结论

- 该单一 P1 的范围受限返工已完成，M1 现**待新的不同实例干事复审**。
- 本实施不作初审或经理终审；M2—M4 和工单 11—12 继续锁定。

## 2026-08-26｜M1 既有 generation `metadata.json` 重建绕过 P1 范围受限返工

### 执行边界

- 接收独立复审退回的单一 P1：重建虽会在状态/loader 拒绝不安全活动 generation 文件，但此前未在写入侧全量验证 `working/*` 与 `generations/*`；因此可能用新的 committed generation 和新 manifest 绕过既有不安全布局。
- 本次仅修改 Core `search` 模块、Windows 定向测试及工单 10 的动态状态/施工日志。未进入 M2—M4，未修改 PDF 提取、Tauri/UI、查询执行或工单 11—12，未清理、回滚或重置历史未提交变更。

### 实施记录

1. 在 `prepare_search_root()` 的任何 `derived/search` 目录创建之前，新增现有布局 no-follow 预扫描：若 `derived/` 或 `derived/search/` 存在，则校验 root-owned `manifest.json` / `failure.json` 与任何已经存在的 `working/`、`generations/`。真正缺失仍允许首次建库；已存在的链接、junction、Windows reparse、非目录/非普通文件固定报 `InvalidSearchIndexPath`。
2. 对 `working/*`、`generations/*` 做完整枚举。每一个 generation entry 必须为真实自有目录；其存在的 `metadata.json` 必须通过普通、非 symlink、非 reparse 的文件校验。此规则同时覆盖 active、nonactive committed generation 与中断遗留 working generation，因而新 manifest 不得绕过任何既有不安全 metadata 对象。
3. 在补齐正常缺失的 `working/`、`generations/` 目录后再次执行同一完整扫描，作为 caller 删除 failure marker、创建新的 working generation 或开始构建写入前的最后门禁。保留原有原子写入前目标复验及同权限 TOCTOU 限制记录。
4. 新增 Windows 定向回归 `rebuild_rejects_existing_generation_metadata_file_links_without_bypassing_them`：active / nonactive / working 三类 generation，分别让 `metadata.json` 指向 Vault 外部兼容 JSON 或 `user/` 下兼容 JSON（共六种）；每种均断言 rebuild 返回 `InvalidSearchIndexPath`、文件链接未被清理/替换、Vault 与外部目标递归快照完全不变。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- `cargo fmt --check`：通过。
- Search 定向测试：14 项通过。
- `cargo test -p cistella-core`：57 个 Core library tests 通过；工单 07 / 08 / 09 portable 集成测试共 6 项通过。
- `cargo test -p cistella-desktop`：2 项竞态隔离测试通过；`cargo check -p cistella-desktop`：通过。
- `pnpm build`：通过，629 modules transformed；主产物 1,374.25 kB（gzip 450.58 kB），Vite 的 500 kB 提示未阻断。
- Rust incremental session 收尾仍报告既有 `os error 5` 拒绝访问，未影响本次命令成功。

### 交接结论

- 单一 P1 的范围受限返工完成；M1 状态为：**待新的不同实例干事复审**。
- 本次施工不充当初审或经理终审；M2—M4 和工单 11—12 继续锁定。

## 2026-08-26｜M1 状态/加载全量既有 generation no-follow 扫描 P1 范围受限返工

### 执行边界

- 接收独立复审退回的单一 P1：重建路径已扫描完整既有索引布局，但 `search_index_state()` 与 `load_search_index_generation()` 此前只在读取面检查活动 generation；不安全的非活动 committed generation 仍可能让索引显示 `Ready` 并使 loader 成功。
- 本次仅修改 Core `search` 模块、Windows 定向测试及工单 10 的动态状态/施工日志。未启动、实现或预埋 M2—M4；未修改 PDF 提取、Tauri/UI、查询执行或工单 11—12；未派发子代理，未清理、回滚或重置历史未提交变更。

### 实施记录

1. 将既有索引布局的完整 no-follow 预扫描提升为状态、加载与重建共用的私有安全门禁：它枚举 `working/*` 与 `generations/*`，要求每一 entry 均为真实自有目录，并要求其存在的 `metadata.json` 为普通、非 symlink、非 Windows reparse 的自有文件。
2. `search_index_state()` 在任何 working/manifest/active generation 状态分支前执行该全量扫描。任何 active、nonactive committed 或 working generation 的目录/文件重解析均固定返回 `Degraded`，不再因当前 active generation 正常而报告 `Ready`。
3. `load_search_index_generation()` 在 `Ready` 状态读取后、实际读取 active generation 前再次独立执行同一全量扫描；检查—读取间出现的非活动或 working generation 重解析固定映射为 `SearchIndexUnavailable`。重建仍沿用同一门禁并固定返回 `InvalidSearchIndexPath`。
4. 扩展既有 metadata 文件链接矩阵：active / nonactive / working × Vault 外部 / Vault `user/` 六种情形均断言状态 `Degraded`、加载 `SearchIndexUnavailable`、重建 `InvalidSearchIndexPath`、Vault 与外部目标快照不变。新增 `all_existing_generation_directory_links_degrade_status_and_loader`，对同一三类 generation 与两类目标的 directory junction 六种情形断言同样的三条安全语义和快照不变。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core search -- --nocapture
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- `cargo fmt --check`：通过；`cargo test -p cistella-core search -- --nocapture`：15 项 Search 测试通过。
- `cargo test -p cistella-core`：58 个 Core library tests 通过；工单 07 / 08 / 09 portable 集成测试共 6 项通过。
- `cargo test -p cistella-desktop`：2 项竞态隔离测试通过；`cargo check -p cistella-desktop`：通过。
- `pnpm build`：通过，629 modules transformed；主产物 1,374.25 kB（gzip 450.58 kB），Vite 的 500 kB 提示未阻断。
- Rust incremental session 收尾仍报告既有 `os error 5` 拒绝访问，未影响全部命令成功。M1 在该干事视角保持“待新的不同实例只读复审”。

### 交接结论

- 此次仅修复状态/加载未全量扫描 inactive committed/working generation 的 P1；实施干事不作初审或经理终审。
- M2—M4 与工单 11—12 继续锁定。


## 2026-08-26｜M1 经理终审通过，M2 实施启动

### 授权与边界

- 经理依据不同实例独立初审 `PASS`，并独立复跑统一验收（Core 58 library + portable 6、Desktop 2、`cargo check`、`pnpm build`）后正式关闭 M1。
- 本轮仅授权 M2《PDF 文本提取、失败隔离与增量/reconciliation》；不得启动 M3/M4 或工单 11/12，不得派发子代理，不得清理、回滚或重置历史工作树。
- 实施前已阅读工程治理规范与工单四件套；本轮先调研纯 Rust PDF 依赖的 API/许可，再实施。

## 2026-08-26｜M2 PDF 文本提取、失败隔离与增量/reconciliation 实施完成

### 执行范围

- 仅完成 M2；未启动 M3/M4、Tauri Search command、Search 工作区、查询执行 API 或工单 11/12。未派发子代理，未清理、回滚或重置历史工作树。
- 先以 `cargo info lopdf` 和本地下载 crate 的 `Cargo.toml` 查验 API/许可：选择 `lopdf 0.44.0`、MIT、`default-features = false`、Rust 1.88+；当前 Rust 1.96.0 可用。该库为纯 Rust，未新增网络、服务进程、OCR 或外部二进制依赖。

### 实施记录

1. 将 search generation schema 升为 `2`，`PDF_TEXT_EXTRACTOR_VERSION` 由 `pending-m2` 升为 `lopdf-0.44-text-v1`；M1 generation 因版本不兼容安全显示 `Stale` 并重建。保留 `pendingExtraction` 的反序列化兼容，但 M2 不再生成它。
2. 新增受限 PDF 文本层提取：只对 Vault 内、PDF、可用资产通过既有 `item_id + asset_id` 受控路径解析；先检查 32 MiB 文件限额，再用 `lopdf` 检查 encryption/page 数，使用 per-page `extract_text_chunks_with_limit` 防止解压炸弹，检查 2,000,000 字符和 5 秒时长上限。
3. 资产记录从 M1 pending 扩展到 `indexed/noText/missing/externalUnavailable/encrypted/unsupported/parseFailed/limitExceeded/cancelled`；非 `indexed` 形成只含 item/asset ID 与受限说明的 `SearchIndexIssue`。外部资产绝不打开正文。
4. 记录实际读取字节的 SHA-256、提取器/分析器版本和 metadata fingerprint。相同 hash + 成功版本复用正文，标题/作者/标签变更只刷新元数据/fingerprint；失败状态会在 reconciliation 重试。若 PDF 内容 hash 变更而新提取失败，生成新 record 的 `content=None`，因新 generation 原子发布而不会带入旧正文。
5. 新增显式 `reconcile_search_index()`：它做增量收敛、补新增、移除孤儿并重试失败/不可用 Vault PDF，但不绑定 `Vault::open`，确保连接不等待提取。M3 只能将其置于桌面异步任务层。
6. 新增真实可控 PDF 定向测试：text/noText/encrypted/broken/missing/external/unsupported/file-limit，替换、删除、新增、metadata-only、portable、M1 stale schema、ID-only 和全 Vault 权威文件不变。保留 M1 的 generation/manifest/failure 链接安全覆盖。

### 资源实验与 golden 回填

- 固定 corpus：8 个单页、可控 Vault PDF；输入总计 **5,024 B**、提取正文 **408 字符**。
- 2026-08-26 定向实测：首次构建 **105 ms**；仅 metadata 变更的 reconciliation **77 ms**；当次 generation derived 输出 **5,234 B**。
- 已固化限额：文件 **32 MiB**、页数 **500**、正文 **2,000,000 字符**、每页解压内容 **8 MiB**、时长 **5 s**；Core 内并发 **1**，页批次 **8**。测试采用各阶段小于 5 s 的回归阈值。
- 不测/不承诺：跨平台进程峰值内存没有为 M2 引入系统探针；M3 查询时延和 M4 全 Vault/index-size 比例仍按路线图保留给后续里程碑。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core search -- --nocapture
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- `cargo fmt --check`：通过。
- Search 定向 19 项通过；fixed corpus 输出为 8 assets / 8 pages / 5,024 B / 408 chars / first 105 ms / incremental 77 ms / derived 5,234 B。
- `cargo test -p cistella-core`：62 个 library tests 通过；工单 07/08/09 portable 共 6 项通过。
- `cargo test -p cistella-desktop`：2 项通过；`cargo check -p cistella-desktop`：通过。
- `pnpm build`：通过，629 modules transformed；Vite 主 bundle 1,374.25 kB（gzip 450.58 kB）大于 500 kB 提示线，未阻断。
- Rust incremental 目录收尾仍出现既有 `os error 5` 拒绝访问提示，未导致任一验收命令失败。

### 交接结论

- M2 实施完成，当前状态：**待新的不同实例干事只读初审**。
- 本实施干事不作初审或经理终审；M3/M4 和工单 11/12 继续锁定。



## 2026-08-26｜M2 P1：权威操作增量索引更新返工完成

### 返工授权与范围

- 按独立初审 P1，仅修复“`reconcile_search_index()` 等同全量重建、权威文献/资产操作未接入增量索引更新”的问题。
- 严格限定在 M2 Core：未启动 M3 查询 API、Tauri command、Search 工作区或异步 UI，未启动 M4、工单 11、工单 12，未派发子代理，也未清理、回滚或重置历史工作树。

### 实施与设计闭环

1. 定义路径无关 `SearchIndexChange`（metadata changed、asset changed、asset removed、item removed）与内部受影响集合；事件只保存稳定 `item_id` / `asset_id`，不携带 Vault/PDF 路径。
2. 新增 `Vault::update_search_index_incrementally()`：仅针对已有 ready generation 构建新的 working generation，克隆未受影响 metadata/content/issue records；只对受影响的 Vault PDF 调用受控解析、文件 hash 与 `lopdf` 提取。metadata-only 更新只刷新该 item metadata record 及关联 asset record 的 metadata fingerprint，不读取任何 PDF。
3. 文献创建、更新、删除和 metadata mutation；DocumentAsset 导入、外部链接、迁移、角色/默认设置、移除等权威操作均在其权威 JSON 成功持久化后 best-effort 触发对应增量变更。派生失败不会传播回该权威操作，故不会回滚/污染 `user/literature_items.json`、`user/document_assets.json` 或 Vault PDF。
4. 保留 `reconcile_search_index()` 为显式**全量**收敛路径：用于缺失更新、孤儿清理、失败/不可用项重试及 Vault 文件系统漂移；它不再是正常操作的唯一更新机制，也不会在 `Vault::open` 中执行。
5. 全量与增量共享 M1 generation 原子发布及路径安全门禁：working 生成 → committed generation → manifest；完整既有布局扫描、目录/文件 no-follow、Windows reparse 防护、失败保留旧 ready generation和 ID-only 派生边界均未削弱。
6. 内容 hash 变化后的新 PDF 若解析失败，仍以新的 `content=None` 记录替换旧正文；该规则由全量 reconciliation 测试继续覆盖。

### 定向验证

- 新增 `m2_authoritative_mutations_publish_incrementally_without_touching_unaffected_pdfs`：验证 metadata-only 更新自动发布新 generation 且没有 PDF access/hash；新增 asset 时仅 access/hash 新 asset；移除 asset 和删除 item 不访问 PDF，且 generation 立即移除相应记录。
- 新增 `m2_incremental_failure_never_rolls_back_authority_and_reconciliation_converges`：将派生 `manifest.json` 人为变为不安全目录使增量发布失败，验证 `update_literature_item` 仍成功、权威 JSON 保存新标题；移除派生故障后 `reconcile_search_index()` 全量收敛最新 metadata。
- 将原“reconciliation incremental”测试更名为 `m2_full_reconciliation_converges_orphans_and_drops_old_text_when_replacement_fails`，准确验证全量兜底下的文件替换失败旧正文清除、孤儿/新增/删除收敛。
- fixed-corpus golden 改为测量权威 `update_literature_item` 内的 metadata-only 增量发布，而不是将随后的全量 reconciliation 错记为 incremental。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core search -- --nocapture
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
pnpm build
```

- `cargo fmt --check`：通过。
- Search 定向：**21** 项通过；fixed corpus：8 assets / 8 pages / 5,024 B / 408 chars / first 47 ms / metadata authority incremental 39 ms / derived 5,234 B。
- Core：**64** 个 library tests 通过；工单 07 / 08 / 09 portable：**6** 项通过。
- Desktop：**2** 项通过；`cargo check -p cistella-desktop`：通过；`pnpm build`：通过，629 modules transformed。
- 非阻断：Rust incremental 收尾出现既有 `os error 5` 拒绝访问；Vite 主 bundle 1,374.25 kB（gzip 450.58 kB）超过 500 kB 提示线。

### 交接

- M2 P1 返工已实施并完成本实施干事自验，当前正确状态为：**待新的不同实例干事只读初审**。
- 本实施干事不作初审/终审，M3/M4、工单 11—12 继续锁定。

## 2026-08-26｜M2 经理终审通过，M3 实施启动

### 审核链与裁定

- M2 初版经不同实例独立初审识别出 P1：日常权威文献/资产操作未接入受影响集合的增量发布，`reconcile_search_index()` 被错误承担为唯一更新路径。
- 返工实施干事完成 `SearchIndexChange`、权威操作后的 best-effort 增量发布、无关 PDF 不访问测试及派生失败不回滚测试；另一新的不同实例干事只读复审结论为 `PASS`，无 P0/P1 阻断。
- 经理复核确认 M2 未越界至 M3 查询执行、Tauri command 或 Search UI，且增量/全量共用 M1 的 working generation → committed generation → manifest 原子发布与 no-follow/reparse 路径门禁。

### 经理独立验收

```powershell
cargo fmt --check
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# crates\openalex-analysis-studio
pnpm build
```

- 全部通过：Core 64 library + 工单 07/08/09 portable 6；Desktop 2；前端 629 modules。
- 非阻断提示：Rust incremental session 收尾 `os error 5`；Vite 主 chunk 1,374.25 kB（gzip 450.58 kB）超过 500 kB 提示线。
- 记录 P2：公开批量保存 API 可绕过增量事件，当前领域操作不受影响，旁路调用由显式 reconciliation 收敛；后续收敛 API 时审计，不阻断 M2。

### 放行

- **M2 正式关闭。** 现仅授权 M3《查询 API、桌面命令与 Search 工作区》实施。
- M4 与工单 11—12 保持锁定；M3 必须遵循“实施干事 → 不同实例独立初审 → 必要返工/复审 → 经理终审”。

## 2026-08-26｜M3 实施：查询 API、桌面命令与独立 Search 工作区（待不同实例独立初审）

- 阅读治理规范和本工单四件套后，仅在 M3 授权范围内施工；未开始 M4，未开启工单 11/12，未派发子代理，未执行 clean/reset/回滚。
- Core 增加路径无关的 `SearchQueryPage` / `SearchQueryResult`、`asset_state` 命中说明和可执行 `query_search_index()`；空/失效索引以 `Unavailable { index_state }` 返回，而非空命中。实现标题、作者、标签、正文范围与文献级聚合；命中资产仅携带 `item_id + asset_id` 和派生健康状态。
- 为 full rebuild/reconciliation 增加 cancellation 检查：逐资产处理前及 manifest 提交前均可停止；取消走派生 working 清理路径并保持旧 ready generation。
- Tauri 增加 local Search 受控 commands 和后台任务协调器。任务记录绑定连接 generation 与 Vault path；异步完成后只有仍属于同一连接的反馈可见。前端仅以 ID 调用既有 `open_document_asset`，没有路径透传和新增阅读会话。
- App 新增独立 Search 工作区：搜索框、范围、健康状态、同步/重建/取消、问题列表、加载/空/错误/unavailable、文献级结果、摘要、命中资产受控打开和 offset 分页。`clearVaultContext()` 在开始连接时清空 Search 数据；查询、健康、任务和问题的成功/失败写入全部二次检查 generation + Vault path。
- 回填 M3 C 层：同字段 AND、元数据优先/`item_id` 稳定排序、字段/资产稳定顺序、默认 50/最大 100 offset 分页、最多 240 Unicode 字符摘要与 `…` 截断标记。
- 自验完成：`cargo fmt --check`、`cargo test -p cistella-core search -- --nocapture`（23 passed）、`cargo test -p cistella-desktop`（4 passed）、`cargo check -p cistella-desktop`、`pnpm build`（629 modules）均通过。既有非阻断提示为 Rust incremental `os error 5` 与 Vite >500 kB bundle warning。
- 停止点：**待不同实例独立初审**；不进行本实例初审或经理终审。

## 2026-08-26｜M3 P1 范围受限返工：持续轮询与连接级任务控制面（待新的不同实例复审）

### 授权边界

- 仅处理独立初审的 P1-1 与 P1-2：Search 前端轮询不可只执行一次；Search 后端任务控制面必须按 connection generation + Vault path 隔离。
- 未进入 M4，未开启工单 11/12，未派发子代理；未执行 clean/reset/回滚，未修改 Core 派生索引原子性、路径安全、无路径 API 或资产受控打开约束。

### P1-1：前端任务状态可靠收敛

- `App.tsx` 的 `refreshLocalSearchHealth()` 现在返回本次已通过 generation + Vault path 门禁的 task state。
- 新增 `src/localSearchPolling.js`，以 350 ms 周期持续刷新处于 `building` 的任务；只要仍为 `building` 就排定下一次轮询。非 `building`（`succeeded`、`failed` 或取消后的 `idle`）立即停止，并在同一请求快照内二次刷新 health、刷新 issues。
- cleanup 和每个异步阶段均先检查当前请求；Vault/generation 变化后，过期 poll 不再调用 refresh 或 terminal 写入。
- `src/localSearchPolling.test.mjs` 以可控 timer 覆盖超过两个轮询周期后的成功、失败、取消收敛，以及过期 Vault 轮询停止；`node --test` 共 4 项通过。

### P1-2：后端控制面严格隔离

- `SearchIndexTaskRegistry` 以 `SearchIndexTaskOwner { connection_generation, vault_path }` 作为任务槽键，而不是复用单一全局任务状态。
- `start_search_index_task()`、后台 finish 回填、可见任务查询、取消命令均定位当前连接 owner 或任务自身 owner；旧 A 的运行/终态不会阻塞或污染 B，也不会与 A → B → A 中新的 A generation 混同。
- 当前连接没有任务槽时取消返回 `Idle`；旧任务槽不会被作为当前取消的返回值。
- Desktop 测试复用命令实际调用的 begin/cancel/finish registry 函数，验证 A 运行时 B 可启动且 B 取消独立、A 的失败不能写入 B、A → B → A 的历史完成/失败与当前 A3 取消均按代际隔离。

### 验收记录

```powershell
# F:\dev-repo\cistella
cargo fmt --check
cargo test -p cistella-core
cargo test -p cistella-desktop
cargo check -p cistella-desktop

# F:\dev-repo\cistella\crates\openalex-analysis-studio
node --test src/localSearchPolling.test.mjs
pnpm build
```

- `cargo fmt --check`：通过。
- Core：66 个 library tests + 工单 07 / 08 / 09 portable 6 项通过。
- Desktop：4 项通过；`cargo check -p cistella-desktop` 通过。
- 前端 polling 回归：4 项通过；`pnpm build` 通过，630 modules transformed。
- 非阻断提示：Rust incremental 收尾 `os error 5`；Vite 主 bundle 1,381.84 kB（gzip 452.74 kB）超过 500 kB 建议线。

### 停止点

- **M3 P1 返工实施完成，待新的不同实例干事只读复审。** 本实施干事不作复审或经理终审；M4 与工单 11—12 继续锁定。

## 2026-08-26｜M3 二次 P1 返���实施：控制请求显式上下文化（待不同实例复审）

- 接收独立初审的单一 P1：前端此前只在响应写入阶段检查 generation + path，Tauri 的同步、重建、取消 IPC 未携带请求来源，因此延迟的旧连接 command 可能在后端按执行时的当前连接误启动或误取消新 Vault 任务。
- 仅在 M3 范围内实施。新增驼峰序列化的 `SearchIndexTaskControlRequest { expectedGeneration, expectedVaultPath }`；`runLocalSearchTask()` 和 `cancelLocalSearchTask()` 捕获同一个 `VaultRequestContext`，以 `{ req: request }` 发送给 Tauri，响应状态写入仍保留已有双重门禁。
- 后端在连接锁内以 `search_task_owner_for_request()` 比较 generation 与 Vault path。sync/rebuild 在 `Vault::open_any()` 与 registry mutation 前失败；cancel 在 registry 查询/取消前失败。失配统一为 `Stale local search task control request`，没有任何新 Vault 的读取、owner 重新绑定、任务创建、取消或状态污染副作用。
- 新增 Desktop 回归：A 的延迟 cancel 到达 B 时 B cancellation 仍为 false；A 的延迟 synchronize/rebuild 到达 B 时 registry 仍为空；A1 → B2 → A3 中 A1 的延迟 start/cancel 不影响 A3 的 building 状态或 cancellation。该测试直接调用 Tauri command 共用的 start/cancel 控制路径，而不是仅测试 registry 内部函数。
- 自验通过：`cargo fmt --check`；`cargo test -p cistella-core`（66 个 library tests + 工单 07/08/09 portable 6 项）；`cargo test -p cistella-desktop`（7 项）；`cargo check -p cistella-desktop`；前端 `node --test src/localSearchPolling.test.mjs`（4 项）及 `pnpm build`（630 modules）。已知非阻断提示仍为 Rust incremental `os error 5` 与 Vite >500 kB bundle warning。
- 工单四件套日期审计：仅使用 **2026-08-26**；未进入 M4，未开启工单 11/12，未派发子代理，未执行 clean/reset/回滚。停止于 **待新的不同实例干事只读复审**，本实施实例不作初审或终审。

## 2026-08-26｜M3 第三次 P1 范围受限返工：启动提交临界区（实施干事）

- **触发与授权**：独立复审确认此前 `start_search_index_task()` 在请求校验/打开 A 后释放 `connection` 锁，再取得 `search_tasks` 槽；A 在该窗口切换到 B 后仍可能启动后台 A 任务。仅修复该 P1；不进入 M4，不开启工单 11/12，不派发子代理，不执行 clean/reset/回滚。
- **实施**：采用 prepare/commit。`Vault::open_any()` 仅在无锁 prepare 阶段获得可启动输入；提交阶段重新取得 `connection`，复验 `expectedGeneration + expectedVaultPath` 后以统一 `connection → search_tasks` 顺序选择 owner、检查/占用槽并在锁仍受持有时 `thread::spawn`。因此连接无法在复验、owner 选择、槽占用和 worker 启动之间变化；prepare 后发生切换则 commit 返回 `Stale local search task control request`，没有槽或 worker 副作用。
- **并发证据**：新增可控钩子测试。A 已打开但提交前暂停，主测试线程完成 B 连接并建立 B `building` 槽；恢复 A 后，A 失败为 stale，`worker_started == false`、不存在 A 槽，B 槽保持 `building` 且 cancellation 仍为 false。原有延迟 cancel/start、A1 → B2 → A3 和 registry 隔离测试保持并通过。
- **自验**：`cargo fmt --check`；`cargo test -p cistella-core`（66 library + 工单 07/08/09 portable 6）；`cargo test -p cistella-desktop`（8）；`cargo check -p cistella-desktop`；`node --test src/localSearchPolling.test.mjs`（4）；`pnpm build`（630 modules）均通过。非阻断提示为 Rust incremental `os error 5` 与 Vite 1,381.85 kB（gzip 452.75 kB）主 bundle >500 kB 建议线。
- **停止点**：本实施干事停止于 **M3 第三次 P1 范围受限返工实施完成，待新的不同实例干事只读复审**；未作初审或经理终审，M4 与工单 11—12 继续锁定。



## 2026-08-26｜M3 经理终审通过，M4 实施启动

- 独立复审 `PASS`，确认 prepare/commit 在 A prepare 后切 B 的精确窗口中不会占槽或启动 worker；控制请求、轮询和A/B/A隔离完整。
- 经理独立全量验收通过：Core 66 + portable 6、Desktop 8、polling 4、fmt/check/build。
- **M3 正式关闭；仅授权M4。** 工单11—12继续锁定。

## 2026-08-26｜M4 portable、重建、数据边界与回归验收实施（待不同实例初审）

- **授权边界**：仅执行工单10 M4；已先读取工程治理规范和工单四件套。未进入 M4 之外的功能，不开启工单11/12，不派发子代理，不执行 `reset`、`clean` 或回滚。
- **实施**：新增 `crates/openalex-analysis-core/tests/工单10_m4_本地全文检索portable.rs`，以公开 API 建立最小 Vault、文献、库内 PDF、DocumentAsset、ReadingSession 和 Search generation。整树复制后，副本保留同一 `item_id + asset_id` 命中；ReadingSession 的受控 opener callback 收到的 canonical PDF 位于副本 Vault 根内，generation JSON 不含任一 Vault 绝对路径。
- **恢复闭环**：覆盖 `derived/search/` 删除、manifest 非法 JSON、版本不兼容、未提交 working generation。每类派生异常都不阻断 Vault 打开/权威数据读取；适用时 rebuild 恢复与原结果等价的 Search 命中。
- **原子与无幽灵闭环**：补强并以 M4 命名原子 commit failure/首次 failure 测试：旧 ready generation 的公共 query 在 commit 失败后保持；首次失败返回 `Failed` 与结构化 unavailable，权威数据未受影响。集成测试覆盖已 ready/首次 cancellable rebuild、PDF 替换、增量新增和删除、metadata 修改、文献删除、无文本/损坏/缺失/外链，确认没有旧正文或删除对象的幽灵命中。
- **写入边界**：M4 完整路径—字节快照保留所有 authority 目录/文件，只排除允许改变的 `derived/`；建立、query、sync/reconcile 和 rebuild 后，manifest/tables/user/files/PDF/资产/阅读会话/Source fixture 全部不变。
- **性能与验收**：固定样本（2 Vault、1 单页库内 PDF、608 B）定向结果：初建 29 ms、副本恢复 rebuild 30 ms、query 9 ms、derived 根直接文件 228 B；阈值 build/rebuild <5 s、query <1 s。通过 `cargo fmt --check`、Search 23 项、Core 66 + 工单07—10 portable 7、Desktop 8、`cargo check -p cistella-desktop`、polling 4、`pnpm build`（630 modules）。非阻断：Rust incremental `os error 5`；Vite 1,381.85 kB / gzip 452.75 kB 的 >500 kB 建议性 warning。
- **停止点**：**M4 实施完成，待新的不同实例干事只读初审。** 本实施实例不作初审或经理终审。

## 2026-08-26｜M4 P1 范围受限返工：全 Vault 写入白名单精确化（实施干事）

- **授权执行**：仅处理独立初审提出的“整个 `derived/` 被快照豁免”P1。仅改 `crates/openalex-analysis-core/tests/工单10_m4_本地全文检索portable.rs` 与本工单 `03/04`，没有进入 M4 外实现，没有启动工单11/12，没有派发子代理，也没有执行 `reset`、`clean` 或回滚。
- **修改**：快照不再在 `relative == derived` 时停止递归；改为仅当相对路径属于精确 `derived/search/` 子树时跳过。`derived/` 本身和所有非 Search 兄弟目录/文件均被登记为目录条目或字节条目。
- **新增回归**：在创建初始快照前预置 `derived/other-engine/authority-like.json`。初建、query、reconcile、最终 explicit rebuild 均使用同一相对路径 + 目录/字节快照断言，因而任何对该文件、其父目录或任意非 `derived/search/` 路径的创建、删除、类型替换或内容改变都会失败。复制 Vault 的恢复/rebuild 快照同样覆盖其副本。
- **自验**：`cargo fmt --check` 通过；M4 定向集成 1/1（输出：首次 13 ms、复制后 rebuild 15 ms、query 2 ms）；Core 66 library + 工单07—10 portable 7、Desktop 8、`cargo check`、polling 4、`pnpm build`（630 modules）全部通过。已知非阻断提示仍为 Rust incremental `os error 5` 与 Vite 大 bundle 建议警告。
- **交接**：停止于 **M4 P1 范围受限返工实施完成，待新的不同实例干事只读复审**；未进行初审或终审，工单11—12保持锁定。

