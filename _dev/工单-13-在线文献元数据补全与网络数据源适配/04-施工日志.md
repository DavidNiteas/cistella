# 04. 施工日志：在线文献元数据补全与网络数据源适配

- 2026-08-31：经理基于设计路线第 13 章「多数据源扩展」立项工单 13，明确在工单 14 完成后推进。
- 2026-08-31：经理拍板工单 13 的 A/B/C 三层决议，明确 M1 做 Crossref DOI 解析、M2 做 PubMed/Europe PMC、M3 做 OpenAlex API / Semantic Scholar 与择优策略。
- 2026-08-31：本干事按四件套纪律完成 `01-目标与设计.md`、`02-施工路线图.md`、`03-开发状态.md`、`04-施工日志.md` 撰写；未实施任何代码，未进入 M1。
- 2026-08-31：经理通过阶段门并授权 M1「Crossref DOI 解析」施工；M2/M3 仍未授权。
- 2026-08-31：本干事完成 M1 实施：
  - 新增 `crates/openalex-analysis-core/src/import/remote/` 模块（identifier / cache / resolver / crossref / registry / mod）。
  - 在 `crates/openalex-analysis-core/src/error.rs` 新增 `RemoteMetadataUnavailable` / `RemoteMetadataNotFound` / `RemoteMetadataMalformed`。
  - 在 `crates/openalex-analysis-core/Cargo.toml` 声明 `reqwest` 0.12 与 `tokio` 1.x 依赖。
  - 在 `crates/openalex-analysis-core/src/import/library_importer.rs` 提取 `commit_import_preview` 并新增 `import_single_record`。
  - 在 `crates/openalex-analysis-studio/src-tauri/src/lib.rs` 新增 `resolve_remote_metadata` 与 `import_by_identifier` 命令并注册。
  - 在 `crates/openalex-analysis-studio/src/App.tsx` 实现 Reading 工作区「通过标识符导入」UI 与双语字符串。
  - 新增集成测试 `crates/openalex-analysis-core/tests/工单13_m1_crossref_doi解析.rs`，使用本地 mock server 覆盖缓存、force_refresh、网络失败、404、字段解析、端到端导入。
  - 全部验收命令通过（core/desktop 全量测试因本机 page file 限制使用 `CARGO_BUILD_JOBS=1`）。
  - 手动验证真实 Crossref API 可返回 `10.1038/s41586-021-03819-2` 的 JSON。
  - M1 状态更新为「已完成，待初审/经理终审」。
- 2026-08-31：经理对 M1 进行终审复跑，`cargo fmt --check`、`cargo check -p cistella-desktop`、`cargo test -p cistella-core --lib`（100 passed）、`cargo test -p cistella-core --test '工单13_m1_*'`（7 passed）、`cargo test -p cistella-core`（全量通过）、`cargo test -p cistella-desktop`（8 passed）、`pnpm build` 均通过；结论 **PASS**，M1 闭合。
