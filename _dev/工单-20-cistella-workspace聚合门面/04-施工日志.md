# 施工日志：cistella-workspace 聚合门面

## 2026-09-05：A 类问题审查与施工循环

### A 类问题审查

- 未发现需要用户额外拍板的 A 类问题。
- 工单 18 与 19 已经固化底座与插件层边界，工单 20 只需在既定边界内完成聚合门面落地。

### M1：聚合 API

- 新增 `cistella-workspace` crate。
- 固化 `WorkspaceKind`、`WorkspaceMode`、`WorkspaceStatus`、`WorkspaceManifest`、`WorkspaceSummary`。
- 固化 `WorkspaceHandle::create/open/close/refresh/summary`。

### M2：子树挂载与依赖边界

- 默认子树：literature、references、writing、assets、analysis、imports、system、cache。
- 聚合层仅依赖 `cistella-tree-space` 注册入口，不回头直连 tree-space 内部布局。

### M3：桌面门面与命令协议

- 在 `cistella-desktop` 中接入 `workspace_contract` 命令。
- 通过 `cargo check -p cistella-desktop` 和 `cargo test --workspace -j 1` 验证接口可编译、可回归。

### 终审

经理终审通过：工单 20 达成。下一步可进入工单 21：legacy Vault 迁移与兼容层。
