# 施工路线图：legacy Vault 迁移与兼容层

| 里程碑 | 状态 | 实施结果 |
|---|---|---|
| M1 | ✅ 已完成 | legacy manifest 识别与只读兼容报告 |
| M2 | ✅ 已完成 | dry-run 计划、zip 备份、staging Workspace、迁移报告 |
| M3 | ✅ 已完成 | 目标覆盖保护、失败清理、迁移后只读审计材料 |

## 验收

- `cargo fmt --all --check`
- `cargo test -p cistella-workspace`
- `cargo check -p cistella-desktop`
- `cargo test --workspace -j 1`

## 关键验证点

- legacy Vault 原目录不被改写。
- 已存在目标根返回 `TargetFrozen`。
- 成功迁移产物具有 `workspace.json`、`system/legacy-vault.zip` 与 `system/legacy-vault-migration.json`。
- 迁移后的 Workspace 可重新打开并通过 manifest 根校验。
