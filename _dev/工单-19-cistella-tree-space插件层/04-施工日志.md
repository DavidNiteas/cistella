# 施工日志：cistella tree-space 插件层

## 2026-09-04：立项

- 确认 cistella 需要专用 tree-space 插件层。
- 确认结构化领域数据使用 Arrow，原始材料使用 Opaque。

## 2026-09-04：设计细化

- 固化 `cistella.<domain>.<kind>` 命名方向。
- 明确注册对象必须包含版本、能力、编解码和校验信息。
- 明确插件层不依赖 desktop/frontend。

## 下一步

等待工单 18阶段门通过后，创建插件 crate 并实现最小双形态 block。


## 2026-09-05：A 类问题审查与施工循环

### A 类问题审查

- 未发现需要用户额外拍板的 A 类问题。
- 工单 18 已经完成方向/范围层决策，工单 19 只需在既定边界内完成插件层落地。

### M1：注册契约

- 新增 `cistella-tree-space` crate。
- 接入 tree-space 的 `RegisteredBlock`、`BlockCaps`、`ArrowCaps` 和 `register_block`。
- 固化注册入口 `register()`，使用 `OnceLock` 保证幂等。
- 固化初始 block 名称：`cistella.literature.items`、`cistella.assets.object`。

### M2：最小 Arrow/Opaque block

- 实现 `LiteratureItemsBlock`：Arrow-capable 文献条目最小表。
- 实现 `AssetObjectBlock`：Opaque 资产对象，携带恢复元数据和 SHA-256 校验。

### M3：验证与终审

- 补齐 5 个单元测试：注册幂等、Arrow roundtrip、schema mismatch、Opaque roundtrip、digest mismatch。
- `cargo fmt --check` 通过。
- `cargo check -p cistella-tree-space` 通过。
- `cargo test -p cistella-tree-space` 通过。
- `cargo test --workspace -j 1` 通过。

### 终审

经理终审通过：工单 19 达成。下一步可进入工单 20：`cistella-workspace` 聚合门面。
