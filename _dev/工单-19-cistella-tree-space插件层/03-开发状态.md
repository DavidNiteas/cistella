# 开发状态：cistella tree-space 插件层

- **立项日期**：2026-09-04
- **达成日期**：2026-09-05
- **当前阶段**：已达成（最小插件层固化）
- **前置依赖**：工单 18 M3（已满足）

## A 类问题审查

- 无需升级给用户的新 A 类问题。
- 工单 18 已完成方向决策：tree-space 为唯一底座、Workspace Tree 为聚合根、Arrow/Opaque 双形态并存。
- 工单 19 的剩余选择均属于 B/C 层：crate 命名、block 最小样例、错误映射和测试覆盖，已由经理在施工循环内拍板。

## 里程碑状态

| 里程碑 | 状态 | 备注 |
|---|---|---|
| M1 | ✅ 已完成 | 注册协议、命名、版本、错误模型 |
| M2 | ✅ 已完成 | Arrow/Opaque 最小 block |
| M3 | ✅ 已完成 | 编码、解码、校验、重载与全量回归 |

## 当前决策

- 新增 crate：`crates/cistella-tree-space`。
- 插件层作为 tree-space 与领域 crate 的唯一适配边界。
- 最小注册 block：
  - `cistella.literature.items`：Arrow-capable 文献条目表。
  - `cistella.assets.object`：Opaque 资产对象，带恢复元数据。
- 注册入口：`cistella_tree_space::register()`，进程级幂等。
- block 命名、schema、语义版本、能力声明、编解码和校验均已固化。

## 遗留清单

- 无阻断遗留。
- 后续领域完整模型、Workspace 聚合与 legacy 迁移分别进入工单 20/21。
- 若 tree-space 上游 API 变化，作为后续偏离申请处理。

## 阶段门记录

- **构建中 → 施工中**：已通过；无 A 类问题需要用户额外确认。
- **M1 → M2**：已通过，注册协议与命名稳定。
- **M2 → M3**：已通过，Arrow/Opaque 最小 block 已实现。
- **M3 → 已达成**：已通过，格式化、crate 级测试与 workspace 串行回归通过。
