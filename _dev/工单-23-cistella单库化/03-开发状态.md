# 开发状态：cistella 单库化

- **立项日期**：2026-09-06
- **完成日期**：2026-09-06
- **当前阶段**：已达成（已施工、回归并终审）
- **前置依赖**：工单 18—22 tree-space 架构线已达成
- **关联工单**：工单 17（前端结构升级）、工单 20（cistella-workspace 聚合门面）、工单 21（legacy Vault 迁移与兼容层）、工单 22（前端工作区映射与回归）

## 目标达成判断

- 用户可见的持久根对象统一为单一 cistella 库；Reading、Notes、Search、Source、Settings 是当前库的工作面。
- tree-space / cistella-workspace 继续作为混合库与技术聚合底座；前端没有暴露 block、bucket、ref 或子树实现细节。
- `Vault` 已从新增普通用户文案中移除，保留为 legacy/internal 兼容术语；不进行破坏性全量重命名。
- 导入流程产品化为选择来源目录并导入到当前库，不再以“输出库”或 Source 独立库引导用户。

## 里程碑状态

| 里程碑 | 状态 | 验收证据 |
|---|---|---|
| M0 | ✅ 已完成 | 立项、A 层方向确认 |
| M1 | ✅ 已完成 | `05-术语与页面映射.md` 的现状盘点与处理结果 |
| M2 | ✅ 已完成 | 单库术语表、数据域、页面职责、Library façade 分层 |
| M3 | ✅ 已完成 | 前端文案、导航、命令面板、导入流程与空状态收敛；bin_test 已重建 |
| M4 | ✅ 已完成 | `library_*` / `import_to_library` façade 已注册，旧命令兼容 |
| M5 | ✅ 已完成 | 前端、bin_test/headless、Rust locked offline 回归均通过 |

## 终审与回归

已通过：

- `pnpm.cmd --dir crates/openalex-analysis-studio build`
- `pnpm.cmd --dir crates/openalex-analysis-studio build:bin-test`
- `pnpm.cmd --dir crates/openalex-analysis-studio test:headless`
- `cargo check -p cistella-workspace --locked --offline`
- `cargo check -p cistella-desktop --locked --offline`
- `git diff --check`

非阻断提示：Windows Cargo incremental cache 在本机出现 `os error 5`，只影响增量缓存复用；Vite 报告单个压缩后前端 chunk 偏大。这两项均不影响本工单的构建、bin_test 自包含或 headless 冒烟结果。

## 兼容边界

- 旧 `vault_*`、`connect_vault`、`import_sources` 等命令继续可用；新增前端调用产品化的 Library façade。
- legacy Vault 读取、迁移、备份/恢复路径保持可用。
- React/Rust 内部 `Vault` 命名按 `05-术语与页面映射.md` 的允许残留清单保留，后续若做全量内部重命名应另立工单。

## 终审结论

工单 23 的设计、施工、回归与验收完成。单库化已在产品术语、前端流程、Tauri façade 与 bin_test 验证层形成闭环；无待用户拍板的 A/B 层问题。