# 开发状态

## 仓库级事实

- cistella 是本地化个人文献保管库 + 阅读/管理/分析工作台。
- 桌面应用是主要用户入口；CLI 不作为对外产品功能。
- OpenAlex 是导入适配器，不是产品中心。
- 新架构方向已确定：底层直接构建在 tree-space 上，Workspace 是聚合树，各业务库以子树挂载。

## 工单登记

- 工单 01—15：已达成并归档。
- 工单 16：编译与入口易用改造（构建中）。
- 工单 17：前端结构升级（施工中）。
- 工单 18：tree-space 底座化与 Workspace 架构重构（已达成）。
- 工单 19：cistella tree-space 插件层（已达成）。
- 工单 20：cistella-workspace 聚合门面（已达成）。
- 工单 21：legacy Vault 迁移与兼容层（已达成）。
- 工单 22：前端工作区映射与回归（已达成）。

## 当前状态

### 工单 16：编译与入口易用改造

- 2026-09-01 已立项，当前处于构建中（设计阶段）。
- 目标包含多 bin 入口、commands/ 拆分、bin_test/ 目录和统一发布脚本。

### 工单 17：前端结构升级

- 当前处于施工中（M2 侧边栏施工）。
- 已完成一级类别/二级入口、写作占位、search 双归属和命令面板分组的首轮实现。
- 侧边栏标题保持短文本；快捷键与具体介绍放入 hover/focus tooltip。
- **默认约定**：任何前端修改都必须执行构建，并同步编译产物到 `bin_test/cistella-frontend/`。

### 工单 18—22：tree-space 架构线

- 依赖顺序固定为：`18 → 19 → 20 → 21 → 22`。
- 工单 18负责总架构、树拓扑、数据分类和接口边界。
- 工单 19负责 cistella 专用插件与 Arrow/Opaque block 注册。
- 工单 20负责 Workspace 聚合门面和桌面协议。
- 工单 21负责 legacy Vault 只读兼容、迁移、校验和回滚。
- 工单 22负责前端工作区映射、兼容路由和全量回归。
- 当前工单 18—22 已达成；tree-space 架构线已完成当前最小闭环。

## 阶段门审查

- 工单 16：设计已写入四件套，待阶段门审查。
- 工单 17：施工中，前端文案乱码问题已纳入当前修复；产物同步约定已固化。
- 工单 18：已达成；Workspace/Library/Vault 边界、子树拓扑、数据分类和 crate 依赖方向已固化。
- 工单 19：已达成；最小插件层、Arrow/Opaque block 与注册入口已固化。
- 工单 20：已达成；聚合门面与 contract 已固化。
- 工单 21：已达成；legacy Vault 兼容报告、staging 迁移和审计产物已固化。
- 工单 22：已达成；前端 Workspace contract 映射、回归和 bin_test 同步已固化。

## 验收基线

- 历史基线：`cargo fmt --check`、`cargo test --workspace`、`cargo check -p cistella-desktop` 和前端构建均曾通过。
- 本轮前端文案修改后的验收：执行 `pnpm build`、ESLint，并执行 `pnpm build:bin-test` 完成 bin_test 同步。
- 已知非阻断项：Windows cargo incremental cache 的 `os error 5` 提示、Vite bundle 体积告警。

## 设计与日志入口

- 总入口：`_dev/README.md`
- 设计路线：`_dev/设计路线.md`
- 工单状态与阶段门：本文件