# 施工路线图：前端工作区映射与回归

| 里程碑 | 状态 | 实施结果 |
|---|---|---|
| M1 | ✅ 已完成 | Workspace contract TypeScript DTO 与 hook |
| M2 | ✅ 已完成 | Sidebar contract-aware 接入；任务导航、hover/focus 提示、旧路由保持兼容 |
| M3 | ✅ 已完成 | build、lint、workspace 回归与 bin_test 同步 |

## 验收命令

- `pnpm build`
- `npx eslint src --max-warnings=0`
- `pnpm build:bin-test`
- `cargo test --workspace -j 1`

## 回归要点

- 前端不显示底层 bucket/block/path。
- 工作区文本没有乱码或 `???`。
- `bin_test/cistella-frontend/` 与最新 `dist/` 同步。
