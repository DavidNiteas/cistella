# 树与桶管道：03 开发状态

**工单：树与桶管道（tree & bucket pipeline）**
**Status：已立项；P-0 / P-IO-1..P-IO-7 / P-XU 已关闭，P-SH / P-IO-8 移交交换协议工单**
**Date：2026-08-22**
**本文件是工单状态的唯一真相源**；全局工单索引见 [`../开发状态.md`](../开发状态.md)。规范见 [`01-目标与设计.md`](01-目标与设计.md)；路线图见 [`02-施工路线图.md`](02-施工路线图.md)；过程记录见 [`04-施工日志.md`](04-施工日志.md)（随首段施工创建）。

---

## 当前状态

| 批次 | 状态 | 一句话 |
|---|---|---|
| P-IO（磁盘 IO） | 🟢 已关闭：P-IO-2..6 已验收关闭（落盘/GC/重开/verify/ephemeral/acceptance/解冻）；**P-IO-7 磁盘布局三阶段已关闭（2026-08-23 大回顾终态）**：7.1 统御（`TbLayout`+`BlockMaterializer` 双实现+身份⊥地址+探测+`TbLibrary` 解耦）→ 7.2 扁平（`ParquetDir`→`FlatDir` 正名 + 双物化写入 + parquet golden）→ 7.3 单文件（每 commit 一 epoch + 对象目录 + 强制 IPC + repack GC）；三阶段全部 golden 零变动；3 项偏离裁定接受。层化目录树中心布局独立工单挂起；P-IO-8 后置横向能力（统御下游消费者） | 两布局 + 统御、sync/并发写 |
| P-XU（xpath 寻址归一） | 🟢 已验收关闭（P-XU-0/P-XU-1，2026-08-22）：删 `Step::Key` 双语义、双键归 Field、bytes16 hex、取代归档 Key 步语义——01 §2 全项落地；verify 恢复精确（P-IO-4 裁定 1 记录链闭合） | 寻址层归一，P-IO-5 前置 |
| P-SH（共享） | ↪ 移交交换协议工单（2026-08-23，见 `../交换协议/`） | SharedRegion 接入并入交换协议「访问形式 / 运行时配置系统」 |
| P-PB（polars 桥） | 🟢 已验收关闭（2026-08-25）：双向桥（读 4 入口 + 写 1 入口）P-PB-1..4 全关；FFI 主通道 + IPC 兜底 + 身份黄金（RefId + canonical 字节逐字节一致）+ rust-195 MSRV 门；独立 crate `tree-space-polars`（authority 见其 `_dev/archive/polars 桥/`） | 双向 Arrow 边界转换，tree-space 本体零 polars |

## 里程碑登记

### P-IO 批次

| 里程碑 | 状态 |
|---|---|
| P-0 authority 落笔 | 🟢 已关闭（2026-08-22，01 §0–§1 定稿：接续声明、批次划分、磁盘模型四决策、v4 追加式演进纪律） |
| P-IO-1 磁盘模型细化 | 🟢 已关闭（2026-08-22，01 §1 修订 + §1.9 对象规范定稿。四项决策：① manifest 取消，由派生 TB 引用表吸收〔三列 xpath/ref_id/address，GC 单跳、重开自算〕；② 新对象家族 + 新通道〔tb-blocks/tb-trees/tb-refs；v4 通道零改动〕；③ 旧读端降级 = 空 v4 tree + 空 metadata 快照；④ ParquetDir 先行 / SingleFile 后置〔P-IO-7〕/ Memory 即内存桶，`DESIGN_VERSION` 维持 "v4"。golden 新清单 16–21 定稿） |
| P-IO-2 块落盘 + 降级对象 | 🟢 已关闭（2026-08-22 验收方裁定：`tb-blocks/` 通道、桶重开、降级对象；golden 16–17 冻结；地址伪造负向绿） |
| P-IO-3 树落盘 + commit 扩展 + GC | 🟢 已关闭（2026-08-22 验收方裁定：`tb-trees/`、`tb-refs/`、引用表派生、commit 追加列、GC 扩展；golden 18–19 冻结；GC 完整性正负向绿；纯 v4 回归绿） |
| P-IO-4 重开 + verify 扩展 | 🟢 已关闭（2026-08-22 验收方裁定：重开〔TreeImage/桶/XPathIndex/typed project〕、verify 三列互验；golden 20 行为冻结 + 21 降级断言；负向全绿） |

### P-XU 批次（xpath 寻址归一）

| 里程碑 | 状态 |
|---|---|
| P-XU-0 authority 落笔 | 🟢 已关闭（2026-08-22，01 §2 定稿：删 `Step::Key`、命名子统一 Field〔String 原样 / bytes16 小写 hex〕、`[...]` 仅 Index、Locator 层不动、取代归档工单 01 §4.4 Key 步语义与 N-8 决策；单射性论证与可达性边界已记录；golden 零字节重冻结核实） |
| P-XU-1 归一施工 | 🟢 已关闭（2026-08-22 验收方独立复跑裁定：四条命令全绿；既有 golden 断言零数值变化；全仓无 `Step::Key`/`.key()` 存活、0x4b 仅幂等拒签；`mzml-db`/`uni-mass-db` 同 workspace 编译零断点；无偏离。04 验收批注 P-XU-1） |
| P-IO-5 ephemeral 落盘 + derive 回补 | 🟢 已关闭（2026-08-22 验收方独立复跑裁定：四命令全绿；golden 19 重冻结 8 列 id 不变；golden 22–25 冻结；既有 golden 其余零变化；两处实现决策实名记录无偏离。04 验收批注 P-IO-5） |
| P-IO-5b 树上下文深层 schema 校验 | ⏳ 后置小里程碑，P-IO-5 闭合后立项（§1.8 #8） |
| P-IO-6 acceptance + 解冻评审 | 🟢 已关闭（2026-08-23 验收方独立复跑裁定：五命令全绿；§8.4 A1–A6/B1–B3/C1〔C2〕/D 全绿；`tiny.mzML` 跨 crate 探路 verify clean + 8 表 ref_id 逐表一致；golden 零变化、生产零改动；`联合开发.md` §3.2 解冻已生效。两款偏离申请〔A6 字段命名避遮蔽 / B3 命名 lint〕验收方接受并登记遗留。04 验收批注 P-IO-6） |
| P-IO-7.1 统御 | 🟢 已关闭（`TbLayout` trait 泛型化解耦 `TbLibrary`〔11 方法面 + 块/树/引用/commit 对象读写〕；`BlockMaterializer`〔`IpcMaterializer` 默认 / `ParquetMaterializer` 仅 `ArrowTable`〕+ `select_materializer` 统一入口 + `probe_block_materialization` 探测〔ARROW1/PAR1/native〕；身份⊥地址落地：`ref_id` 物化无关、`address`=hash(物化字节)；`ParquetDirLayout` 挂靠为首例、`SingleFile` 挂 `impl TbLayout`〔读写 todo!("P-IO-7.3")〕；2 项偏离裁定接受〔decode 返回 `Envelope` / parquet 临时 scratch 文件〕；新增 `TbLibrary::create_with_materialization`、`CommitNode` pub 化；golden 零变动） |
| P-IO-7.2 扁平 | 🟢 已关闭（`ParquetDir`→`FlatDir` 正名重构：struct/`StorageKind` 字符串/模块文件/错误串全替换，`DESIGN_VERSION` 与对象字节零改动、golden 17/22–25 零变动守卫绿；扁平双物化写入分派〔`select_materializer`〕+ 重开/verify 探测接线复核完整；parquet 块地址 golden 新增冻结：样本 ArrowTable 物化地址 `efaf05e1754df32a1ebec16a0a5a881d`、语义身份 `27ab77718c2c3124acd84f687a5b4615`；7 断言全绿〔roundtrip/混物化/Blob·opaque 拒 parquet/golden17 守卫/字符串同步〕。`StorageKind` 持久串变更为 `flat_dir`〔规范内，下游 v4 表目录不受 TB 层影响〕。无偏离） |
| P-IO-7.3 单文件 | 🟢 已关闭（v4 容器机制原样保留、`TbLayout` 15 个 todo 全填真值：每 commit 一 epoch〔块增量+树字节+引用表+commit + 对象目录快照〕、对象去重〔目录 exists→skip〕、强制 IPC〔`block_materialization()`恒 Ipc + `create_with_materialization(Parquet)`→`TargetFrozen`〕、GC = compaction repack〔可达集重写临时文件+rename〕、committed = 尾部 trailer 链〔v4 `find_latest`〕——真·单文件；单文件入口经 `SingleFileTbLibrary` trait 承载〔偏离已裁定接受，原因：同名 inherent create/open 于两 impl 块会 E0034，trait 保调用形状 `TbLibrary::<SingleFileLayout>::create`〕；9 断言全绿〔roundtrip/对象字节与扁平逐项相等/每 commit 一 epoch/物理去重/parquet 拒绝/GC 保留窗口、repack 后续 commit/坏 trailer 回退/新库 genesis/commit_pruned 重开〕；既有 golden 零变动 + v4 单文件零回归；无新增字节 golden） |
| 层化目录（树中心布局） | ⏳ 独立工单挂起（三关系裁定 §1.11.8），不在 P-IO-7 |
| P-IO-8 sync + 并发写（横向能力） | ↪ 移交交换协议工单（2026-08-23，见 `../交换协议/`）——sync 并入「同步」，并发写并入「权限/代理」 |

### P-SH / P-PB

⏳ 待批次立项展开。

## golden 登记

- 本工单 golden 清单 16–21 已定稿（01 §1.9.7）：降级对象 / 块 blob 地址 / 树 blob 地址 + 引用表字节 / TB commit 字节 / 重开等价性 / 旧读端降级断言——待 P-IO-2 起逐里程碑冻结；P-IO-5 续 22–25（剪枝树字节 / 剪枝引用表 / `tb_pruned` 列 commit 字节 / 读取尽力，§1.9.8（g））；
- 既有 golden（块身份 1–10、树身份 11–15、v4 全套）继续有效且**不得变更**（01 §6 纪律 2/3）。
- **16 降级对象**（P-IO-2 冻结，2026-08-22）：空 v4 tree 对象 id `94f7112c7db38093bcd4d7879177ac2c`（=`tree_id_golden(&[])`）；空 metadata 快照引用 `6e9b3f26ff015f878a4c3e1f166244dc`（=`metadata_ref(BootstrapImage::built_in())`）；
- **17 块 blob 地址**（P-IO-2 冻结，2026-08-22）：样本 Blob envelope（payload `74622d73616d706c6500`）帧字节 = 1530 hex 常量（冻结于 `tests/p_io_2_blocks.rs`）；地址 `d796cdd97c2ecac5dbeb3cc6ac9dc605`；`RefId` `d58716fc4ce784b1c0ff8569b6a22440`（地址 ⊥ 语义身份）。
- **18 树 blob + 引用表**（P-IO-3 冻结，2026-08-22）：样本树（`block` Ref + `chunks` Table 组 2 项）树 blob 地址 `daeccfca911e826a3609331e7cd83f18`、`TreeId` `72b0a3c2460535a2e3201c97a0934ec3`；三行引用表（`/block`、`/chunks[0]`、`/chunks[1]`）对象字节 2612 hex 常量、地址 `c35c93d83d536237e4ec44e356e5d542`；
- **19 TB commit 字节**（P-IO-3 冻结 7 列，2026-08-22 **P-IO-5 重冻结为 8 列**）：单行 batch（seq 9、parent `0a..`、root `1a..`、tree `2a..`、refs `3a..`、`tb_pruned`=null）对象字节 5860 hex 常量、id `ea454fc52da0fdc1bcce099eb7f75c9e`（**id 不变**：五 part 公式未入 `tb_pruned`，§1.9.8 (h)）；
- **20 重开等价性**（P-IO-4 行为冻结，2026-08-22）：构造 → 落盘 → 重开 ≡ 内存原象——`TreeImage` 相等、桶 envelope 集相等、`XPathIndex` 查询等价、typed `project` 重编码字节相同（`tests/p_io_4_reopen_verify.rs`）；
- **21 旧读端降级**（P-IO-4 覆盖，2026-08-22）：v4 `open` TB 目录 → 空表集、不报错（`p_io_3_old_reader_degrades_to_empty_library`）；纯 v4 目录新读端行为不变由全量 v4 回归守护。
- **22 剪枝树字节**（P-IO-5 冻结，2026-08-22）：样本 ephemeral 树（persistent `block` Ref、`/common/table` Ref；ephemeral Node `/scratch/lazy` Ref、ChunkGroup `/cache`〔Table×2〕）在前缀 `{/scratch, /cache}` 下剪枝 = `{block, common/table}`——canonical 字节 11988 hex 常量；树 blob 地址 `25fb5e48798d234969eaa329aca585af`；`TreeId` `73004bba1dad372179818e1519c8c994`；recorded maximal 前缀（排序）= `/cache` `4605000000000000006361636865` + `/scratch` `46070000000000000073637261746368`（冻结于 `tests/p_io_5_ephemeral.rs`）。
- **23 剪枝引用表**（P-IO-5 冻结，2026-08-22）：两行（`/block`、`/common/table`）对象字节 2612 hex 常量、地址 `3aa14961d6d1c041ec82e40cfe25e1f0`；被剪块（`/scratch/lazy`、`/cache` chunks）不在其中。
- **24 含 `tb_pruned` 列 commit**（P-IO-5 冻结，2026-08-22）：8 列单行 batch（seq 9、前缀列表 = `/cache`+`/scratch` canonical 字节）对象字节 6244 hex 常量；**id `ea454fc52da0fdc1bcce099eb7f75c9e` = golden 19**（公式未变）。
- **25 读取尽力**（P-IO-5 行为冻结，2026-08-22）：`#[tree_space(ephemeral)]` 容器/节点字段 commit_pruned → 重开 `project` = `Default::default()`（空 Vec/`None`/节点 Default）；非 ephemeral 缺失仍 `SchemaMismatch`（`p_io_5_reopen_best_effort_defaults_and_non_ephemeral_still_required`）。

## 遗留清单

- `dev-all-gpu` 环境工单（独立，沿归档工单登记，非本工单范围）；
- 下游冻结解除：P-IO-6 已解冻（2026-08-23，`crates/_dev/联合开发.md` §3.2 回写生效——下游可向树+桶面迁移设计，v4 过渡保证维持）；迁移是下游自己的后续工作（01 §1.10.4）。
- 后置批次：P-IO-7（磁盘布局三+统御，§1.11 规范已定稿）、P-IO-8（sync + 并发写，§1.10.5 可行性 + §1.11.5 统御前置）、P-IO-5b（深层 schema 校验）；
- 解冻后小修建议（验收方裁定后可执行）：`tree-space-derive` 解码生成绑定名与字段名冲突（字段字面命名 `map` 会遮蔽内部 `map`，见 04 P-IO-6 偏离申请）——建议改内部绑定为 `field_map` 等非遮蔽名。

## 验证证据

- 2026-08-22 立项：纯文档（无代码改动），无验证命令义务。
- 2026-08-22（P-IO-2 施工完成）：四条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_2_blocks 6 passed + 全量历史回归）；rust-189 `-p tree-space -p tree-space-derive --all-targets`；dev-rust workspace `--all-targets`。
- 2026-08-22（P-IO-3 施工完成）：四条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_3_trees_commits 11 passed + 全量历史回归）；rust-189 `-p tree-space -p tree-space-derive --all-targets`；dev-rust workspace `--all-targets`。
- 2026-08-22（P-IO-2..4 验收关闭，验收方独立复跑）：四命令全绿——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_2 6 + p_io_3 11 + p_io_4 10 + 全量回归）；rust-189 `--all-targets`；dev-rust workspace `--all-targets`（79 组零失败零警告）。
- 2026-08-22（P-IO-4 施工完成）：四条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_4_reopen_verify 10 passed + 全量历史回归）；rust-189 `-p tree-space -p tree-space-derive --all-targets`；dev-rust workspace `--all-targets`。
- 2026-08-22（P-XU-1 施工完成，待验收）：四条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_xu_1 4 passed + n5 9 passed 归一行为 + 全量历史回归）；rust-189 `-p tree-space -p tree-space-derive --all-targets`；dev-rust workspace `--all-targets`（80 组，零失败零警告）。既有 golden 断言（16–21、树身份 11–15、v4 全套）零数值变化。
- 2026-08-22（P-XU 验收关闭，验收方独立复跑）：四命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_xu_1 4 + n5 9 归一 + 全量历史回归）；rust-189 `--all-targets`；dev-rust workspace `--all-targets`（80 组，零失败零警告）。golden 零重冻结实锤（ab1/a2_1 tree_id 11–15/a2_3 view/p_io_2/p_io_3 常量/digest_golden/m2 原值通过）；`mzml-db`/`uni-mass-db` 同 workspace 零断点；全仓 `Step::Key`/`.key()` 无存活、0x4b 仅幂等拒签与负向。无偏离，01 §2.2 全项兑现。
- 2026-08-22（P-IO-5 施工完成，待验收）：四条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_5_ephemeral 12 passed + derive 单测 10 passed + 全量历史回归含 golden 19 重冻结）；rust-189 `-p tree-space -p tree-space-derive --all-targets`（真工具链门全绿）；dev-rust workspace `--all-targets`（全组零失败零警告）。既有 golden（16–18、20、21、树身份 11–15、v4 全套）零数值变化；golden 19 按 §1.9.8 (h) 重冻结为 8 列（id 不变）；golden 22–25 冻结。
- 2026-08-23（P-IO-6 评审施工完成，待验收方裁定）：五条验收命令全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_6_acceptance 9 passed + 全量历史回归）；rust-189 `-p tree-space -p tree-space-derive --all-targets`（真 MSRV 门全绿）；dev-rust workspace `--all-targets`（83 组测试目标零失败零警告，含 `tb_consumption` 2 passed）；dev-rust `-p mzml-db --test tb_consumption` 显式通过（2 passed）。既有 golden（16–25、树身份 11–15、v4 全套）数值零变化（零生产代码/零既有测试改动，仅两个新测试文件于工作树）。
- 2026-08-23（P-IO-6 验收关闭，验收方独立复跑）：五命令独立复跑全绿、零警告——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（p_io_6_acceptance 9 + 全量回归）；rust-189 `--all-targets`；dev-rust workspace `--all-targets`（83 组零失败零警告）；dev-rust `-p mzml-db --test tb_consumption`（2 passed）。golden 零变化实锤（ab1/a2_1/a2_3/object_golden/digest_golden/p_io_2..5/xu_1 原值）；生产代码/既有测试零改动；`联合开发.md` §3.2 解冻回写生效；两款偏离申请（A6 字段命名避遮蔽、B3 命名 lint）验收方接受记遗留。无规范偏离。
