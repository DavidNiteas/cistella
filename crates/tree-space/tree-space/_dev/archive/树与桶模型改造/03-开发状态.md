# 树与桶模型改造：03 开发状态

**工单：树与桶模型改造（tree & bucket transformation）**
**Status：🟢 终态闭合（2026-08-22）——TB/AB/A-2 三批次全部闭合，工单归档；后续工作归管道工单（见文末「工单终态闭合」）**
**Date：2026-08-22**
**本文件是工单状态的唯一真相源**；全局工单索引见 [`../../开发状态.md`](../../开发状态.md)。规范见 [`01-目标与设计.md`](01-目标与设计.md)；路线图见 [`02-施工路线图.md`](02-施工路线图.md)；过程记录见 [`04-施工日志.md`](04-施工日志.md)。

---

## 当前状态

| 批次 | 状态 | 一句话 |
|---|---|---|
| TB（树与桶模型层） | 🟢 已闭合（2026-08-21） | 块类型系统与树语义重塑（纯内存）；TB-0..TB-6 全部关闭 |
| AB（块与桶 Arrow 化） | 🟢 已闭合（2026-08-22） | 内建块 payload 全 Arrow + 块开放注册制；AB-0..AB-2 全部关闭 |
| A-2（树对象 Arrow 化） | 🟢 已闭合（2026-08-22） | 规范定稿（01 §5）+ A2-0..A2-3 全部关闭；golden 11–15 冻结 |
| 管道（IO/共享/polars） | ⏳ 独立工单（未立项） | 前置依赖 A-2；本工单随 A-2 验收闭合归档（颗粒度决策 2026-08-22） |

## 里程碑登记

### TB 批次（已关闭）

| 里程碑 | 状态 |
|---|---|
| TB-0 authority 落笔 | 🟢 已关闭（2026-08-20） |
| TB-1 块身份与四块重塑 | 🟢 已关闭（2026-08-21，含返工） |
| TB-2 桶（Bucket）重塑 | 🟢 已关闭（2026-08-21） |
| TB-3 动态节点 + chunking | 🟢 已关闭（2026-08-21） |
| TB-4 view 组合器 | 🟢 已关闭（2026-08-21） |
| TB-5 持久化标记 | 🟢 已关闭（2026-08-21） |
| TB-6 验收收尾 | 🟢 已关闭（2026-08-21） |

闭合经过：第一轮实现与施工日志 → 验收批注（两红牌：原子重塑未执行、TB 块 golden 未冻结）→ 返工完成（原子重塑、golden 冻结、trait 裁定落实、Persistence 范围回写）→ 偏差审查修复（`AccessOut` 收敛三变体、`Sequence::decode` 补防御、derive 弱 set 桥语义、`Registered` 变参 arity、`Select` 越界报错、文档漂移修正、引用与文档纪律清理）。详见 [`04-施工日志.md`](04-施工日志.md) TB 各节与验收批注/返工/偏差审查记录。

### AB 批次（已关闭）

| 里程碑 | 状态 |
|---|---|
| AB-0 authority 修订落笔 | 🟢 已关闭（2026-08-21） |
| AB-1 内建块 payload Arrow 化 | 🟢 已关闭（2026-08-22） |
| AB-2 开放注册制 | 🟢 已关闭（2026-08-22） |

### A-2 批次（已立项，规范定稿 2026-08-22）

| 里程碑 | 状态 |
|---|---|
| A2-0 authority 落笔 | 🟢 已关闭（2026-08-22，01 §5 定稿：平面条目表 / `tb-tree` 域 / 无信封 / 字典序字段序 / 类型名与注记不进 canonical / TreeImage 两级解码） |
| A2-1 条目表编解码内核 | 🟢 已关闭（2026-08-22，验收方裁定：规范逐条核对通过；golden 11–13 冻结；§5.4 负向全绿；四命令验收方复跑全绿） |
| A2-2 类型化投影 + derive 接线 | 🟢 已关闭（2026-08-22，验收方裁定：`TreeCodec` derive + `project`；golden 15 行为冻结；误装/结构不符负向绿） |
| A2-3 view/chunk 全量 + acceptance | 🟢 已关闭（2026-08-22，验收方裁定：golden 14 冻结；acceptance 闭环等价成立） |

## golden 登记（现行冻结值）

清单义务见 [`01-目标与设计.md`](01-目标与设计.md) §7（A-2 项 11–15 已于 A2-1/A2-3 冻结，见下）；历史值（TB-1 原冻结、AB 重冻结前）见 [`04-施工日志.md`](04-施工日志.md)。

- **四空块 RefId（AB-1 重冻结）**：Table `2e03c8d73e566e91b0b9f25a21e95e7a`、Sequence `f6ca637e41b0f2482371adcf33025fab`、Kv `f5ec2e12f2ed4bc9a47b0794847809ea`、Blob `de893f27591e1f583296da21860da0dc`；
- **命名域空 payload（AB-2 冻结）**：`org.example.ab2.empty` → `df0cb4cf5da303c6ce36bf7cde7e5408`（envelope v2 帧）；
- **view 组合器（TB-4 冻结，Table 路径不受 AB 影响）**：Select `5d2ef3a4840c3f150f15112fa9f1350f`、Join Inner `b1fe1f785700915f72de24428f755b29`、Join Left `8bab92d8ac12dc60fe24fcfcd436c377`；
- **A-2 树对象 TreeId（A2-1/A2-3 冻结，`tb-tree` 域）**：
  - 11 空树（单根行）：`5c9c5f4a7c6b041adfa300da949b00bf`；树表列 schema 8 列 `parent/name/key16/kind/value/ref/block_kind/combinator`（UInt64/Utf8/FSB(16)/UInt8/DenseUnion(22)/FSB(16)/Utf8/Utf8）；
  - 12 内联标量树（Bool、I64、Time32(ms)、Timestamp(ns,"UTC")、Decimal128 参数袋）：`34e83ce7b30bec1475d9b7139de219d1`；
  - 13 块引用 + chunk 组树（嵌套组、保序、stats 缺省）：`33e321dca3b11c3ec87ec725a1622d0e`；
  - 14 view 树（Join+ViewParam+嵌套 Select+Registered `example.slice`）：`9499fba60111b5601604c548b546ca93`；
  - 15 无独立常量——行为冻结：typed/dynamic 同构造同 TreeId + 字段声明序无关 + 编码→弱解码→重编码字节相同（canonical 闭环）。
- 另冻结：异构 DenseUnion/参数袋 roundtrip 与 Arrow IPC magic 断言（AB-1）、Opaque/Arrow 两路注册块 roundtrip（AB-2）、digest 与对象 schema v4 golden（v4 路径，未动）。

## TB 批次闭合登记（2026-08-21）

- 关闭验证（全部实跑）：dev-rust `-p tree-space -p tree-space-derive` 测试、fmt `--check`、dev-rust workspace `--all-targets` 测试、**rust-189 真工具链（1.89.0）`--all-targets` 测试**——全绿、零警告。
- 范围外代码改动（工具箱修复，与 TB 分离提交）：根 `rust/Cargo.toml` 的 `python = []` 特性标记、`mzparse-core/src/api.rs` 的 arrow cfg 化。

## AB 批次闭合登记（2026-08-22）

- **AB-1**：新增 `src/block/arrow.rs`；Sequence/Kv/Blob payload 改为 Arrow IPC；Table 路径保持 canonical IPC；废弃旧 Value/Cursor 线格式解码路径。同构值走 typed Arrow 数组；混合类型或参数化元数据不一致走固定 22-child DenseUnion（参数袋保留值级元数据）；Kv 按新逻辑规范序（Utf8/Binary 内容字典序）；Blob 单行单列 Binary。
- **AB-2**：`BlockKind::Named(Arc<str>)`（去 Copy）；Envelope v2（tag 5 命名块帧，v1 兼容）；`src/block/registry.rs`（进程级注册表 + `RegisteredBlock` + blanket `Block` impl + `BlockCaps::{Opaque,Arrow}`）；`read_block` 对 Named 查注册表（未注册 → `TypeNotFound`）。强类型主路经 `RegisteredBlock::decode(canonical_payload)` 重建，不引入 `Any`；view 封闭核拒组合 Named 块。
- 关闭验证（全部实跑 + 提交前复跑）：fmt `--check`、dev-rust 包级测试、rust-189 `--all-targets`、dev-rust workspace `--all-targets`——全绿（提交 `02fd590 feat(tree-space): AB block Arrow-ization and open registration`）。

## A-2 批次闭合登记（2026-08-22）

- **交付**：`src/tree/codec.rs`（树表八列 schema、`TreeId`（`tb-tree` 域独立 newtype）、`TreeImage` 通用意象（`Locator`/`ImageField`/`ImageContent`/`ImageView`/`ImageViewInput`）、`encode`/`decode`/`encode_node`/`tree_id`/`tree_schema`、类型化投影运行期助手、`DecodeBlock`/`decode_block_leaf`、`view_to_image`/`image_to_view`、`to_image`/`from_image`）；`tree-space-derive` 新增 opt-in `#[derive(TreeCodec)]`（生成 `EncodeTree` + `DecodeTree`）；`src/block/arrow.rs` 既有 Value↔Arrow 机制提升为 `pub(crate)` + `union_to_values` 恶意 type_id/offset 防御修复（AB 遗留隐患）；测试 `tests/a2_1_codec.rs`（11）、`tests/a2_2_projection.rs`（10）、`tests/a2_3_view_chunk.rs`（6）+ codec 单测 21。
- **验收结论**：规范逐条核对、golden 审计、纪律审计（v4 零触及/零新依赖/无 git 操作）、验收方独立复跑四命令全绿——详见 [`04-施工日志.md`](04-施工日志.md)「A-2 批次验收批注」。三项提请裁定项全部接受（ViewParam 行序澄清合法；value 列物理约束接受并回填 01 §5.3 规则 7；空 chunk 组 kind 为实现自由度决策）。
- **规范变更随批落定**：01 §5.2（ViewParam 行序，实施方）；01 §5.3 规则 7（value 列 IPC V5 物理约束，验收方回填）；01 §5.4（投影含桶/编码无需桶，交底前澄清）。
- **遗留（非关闭判据）**：`TreeImage` 与 v4 `TreeInstance`/`XPathIndex` 的接线、树对象磁盘包装与 commit 可见性——归管道工单；derive 类型化 ephemeral 标注同前。

## 遗留清单

- **延后项（非关闭判据）**：类型化（derive）实例的 ephemeral 标注与类型化重建的树上下文 schema 校验 → IO 管道批次（[`01-目标与设计.md`](01-目标与设计.md) §3.5 / TB-2 日志）；
- **环境工单（独立，非本工单范围）**：`dev-all-gpu` 环境更新在 maturin/uv-cache build env 阶段失败（该环境缺 pip，无法安装 cffi），cargo 未启动；修复归入独立工单（maturin/Pixi 工具链）；
- **下游冻结**：`mzml-db` / `uni-mass-db` 冻结在 v4 公共 API 直至 tree-space 完善（`crates/_dev/联合开发.md` §3.2）。

## 验证证据

- **2026-08-22（A-2 验收关闭，验收方独立复跑）**：四命令全绿——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`（codec 单测 21 + a2 集成 27 + 全量回归）；rust-189 `--all-targets`；dev-rust workspace `--all-targets`（全 crate 零警告）。
- **2026-08-22（A-2 施工完成，A2-1/A2-2/A2-3 三条验收命令复跑）**：
  - `pixi run -e dev-rust cargo fmt --manifest-path mzparse/rust/Cargo.toml --all -- --check` ✓
  - `pixi run -e dev-rust cargo test --manifest-path mzparse/rust/Cargo.toml -p tree-space -p tree-space-derive` ✓（29 组全绿）
  - `pixi run -e rust-189 cargo test --manifest-path mzparse/rust/Cargo.toml -p tree-space -p tree-space-derive --all-targets` ✓（27 组全绿，零警告）
  - `pixi run -e dev-rust cargo test --manifest-path mzparse/rust/Cargo.toml --workspace --all-targets` ✓（76 组全绿，零警告）
- **2026-08-22（AB 闭合 + 提交前复跑）**：四命令全绿——fmt `--check`；dev-rust `-p tree-space -p tree-space-derive`；rust-189 `-p tree-space -p tree-space-derive --all-targets`；dev-rust workspace `--all-targets`（mzml-db / mzparse-core / table-index / uni-mass-db 连带回归无恙）。

## 工单终态闭合（2026-08-22）

- **闭合依据**：02 §6 颗粒度决策——本工单范围止于编码（TB/AB/A-2），随 A-2 验收闭合归档。三批次全部关闭，golden 1–15 冻结，编码使命（模型层 → 块 Arrow 化 → 树对象 Arrow 化）完成：块与树的序列化形态已统一为 canonical Arrow IPC（北极星达成）。
- **归档时状态**：全部里程碑 🟢；工作树干净（施工提交 `02fd590` / `77595c1` / `6c18b94` 等）；无挂起项。
- **移交管道工单的接续清单**（未立项，立项时须在其 01 声明接续关系）：
  1. 树对象磁盘包装（存储帧）与 commit 可见性点延伸（v4 PreparedCommit 机制衔接）；
  2. `TreeImage` 与 v4 `TreeInstance`/`XPathIndex` 的接线；
  3. ephemeral 写盘剪枝 / 读取尽力语义落地（01 §3.5 前向契约的兑现）；
  4. derive 类型化实例 ephemeral 标注 + 类型化重建的树上下文 schema 校验回补；
  5. SharedRegion 接入新模型（零拷贝只读 + 惰性拷贝）；polars 桥（经 Arrow 边界，tree-space 本体不引入 polars 依赖）；
  6. 下游解冻协调：`mzml-db` / `uni-mass-db` 自 v4 公共 API 向新面的迁移（`crates/_dev/联合开发.md` §3.2 冻结决策的解除条件）。
- **规范基线移交**：本工单 [`01-目标与设计.md`](01-目标与设计.md) 全文（尤其 §1 铁律、§2 块规范、§4 树运行时契约、§5 树序列化）+ [`../V4-磁盘协议/设计与路线图-v4.md`](../V4-磁盘协议/设计与路线图-v4.md)（v4 协议事实规范）继续作为现行规范被引用——归档 ≠ 失效（`_dev/README.md` §3）。
- **环境工单（独立）**：dev-all-gpu maturin/pip 问题不随本工单闭合，另行立项。
