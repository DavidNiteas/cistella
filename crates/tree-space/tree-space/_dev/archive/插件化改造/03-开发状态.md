# 插件化改造：03 开发状态

**工单：tree-space 插件化改造（plugin overhaul）**
**Status：已归档（2026-08-24 闭合；PL-1 M1 S1–S8 + PL-2 M2 S1–S2 全关收口）**
**Date：2026-08-24**
**本文件是工单状态的唯一真相源**；全局工单索引见 [`../开发状态.md`](../开发状态.md)。规范见 [`01-目标与设计.md`](01-目标与设计.md)；路线图见 [`02-施工路线图.md`](02-施工路线图.md)；过程记录见 [`04-施工日志.md`](04-施工日志.md)。

---

## 当前状态

| 阶段 | 状态 | 一句话 |
|---|---|---|
| authority 落笔（01 + 02 + 03） | 🟢 已落笔（2026-08-24） | 北极星 + A 层决策 10 条 + 三个新拍板 + B 层方案（插件 trait / 版本 / boot / 身份解耦 / 退化 / v4 废弃）+ 现状盘点 + 范围边界；PL-1（M1）八步 + PL-2（M2）两步施工细则到「可执行」粒度 |
| PL-1（=M1）打通插件机制 + 版本路由 + boot + 版本侧表 + 退化 + arrow55 插件化 + v4 内部废弃 | 🟢 已关闭（2026-08-24） | S1 版本枚举 → S2 插件 trait + arrow55 插件化 → S3 boot + tb-boot → S4 引导八步 + ref 表驱动 → S5 版本侧表 + commit 9 列 + golden 19/24 重冻 → S6 退化 → S7 v4 内部废弃 + 门控 → S8 测试锚定；四命令终审全绿，8 commit 已推送 |
| PL-2（=M2）身份语义化 | 🟢 已关闭（2026-08-24） | S1 semantic.rs（SemanticValue 标量 + 表/树语义指纹）+ golden 28–30 冻结 → S2 ref_id/tree_id 切语义公式 + converter 挂钩 + 身份类 golden 重冻 16 项；地址/schema 字节类零动 |

## 里程碑登记

| 里程碑 | 状态 |
|---|---|
| authority 落笔（01 + 02 + 03） | 🟢 已落笔（2026-08-24）：01 八节（背景定位 / 北极星 / A 层决策 10 条 / 三个新拍板 / B 层方案 / 现状盘点 / 范围边界 / 纪律接续）；02 七节（阶段划分 / 里程碑表 / 验收命令 / 施工协议 + PL-1 八步细则 + PL-2 两步细则 + 待确认可调项）；03 本文。纯文档，无代码改动 |
| PL-1（=M1） | 🟢 已关闭（2026-08-24 终审通过）：八个施工单元（S1..S8）串行完成，8 个 commit——S1 `9691158`、S2 `554a153`、S3 `e017d93`、S4 `21662d9`、S5 `d7fb011`、S6 `36e1fa9`、S7 `34b58b2`、S8 `26bc08d`（分支 refactor/arrow-scientific-workspace，已推送）；关闭标准（02 §5.10 八条）全部满足；四命令终审复跑全绿；04 施工日志 S1–S8 + PL-1 关闭判定齐 |
| PL-2（=M2） | 🟢 已关闭（2026-08-24 终审通过）：两个施工单元（S1..S2）串行完成，2 个 commit——S1 `5c2842f`（semantic.rs + golden 28–30 冻结）、S2 `fa86cb2`（身份语义化 + converter 挂钩 + 身份类 golden 重冻 16 项）（分支 refactor/arrow-scientific-workspace，已推送）；关闭标准（02 §6.5 五条）全部满足；四命令终审复跑全绿（workspace 107 组 ok，零警告）；04 施工日志 PL-2 S1–S2 + PL-2 关闭判定齐 |

## golden 登记

- **PL-1 实际态（2026-08-24 终审核对）**：
  - **M1 重冻例外（唯一）**：**golden 19 + golden 24**——同一 commit 身份 `ea454fc52da0fdc1bcce099eb7f75c9e` 的两个字节视图（8 列 / 9 列 commit 单行 batch），S5 因 commit 批 8→9 列连带重冻；**id 均保持不变**（五 part 公式不入第 9 列），主代理已逐条核对；
  - **M1 新增冻结**：**golden 26 = boot schema IPC 字节**（S3 冻结、S8 编号）；**golden 27 = tb_versions schema IPC 字节**（S5 冻结、S8 编号）；
  - **M2 新增冻结编号顺延为 28–30**（原 02 §7.6 预留 26–28 被 S8 占用：26/27 已由 M1 的 boot / tb_versions schema 冻结收编，M2 的 `SemanticValue` 标量 / `semantic_table` 指纹 / `semantic_tree` 指纹顺延 28–30——登记说明见 04「S8 施工日志」偏离②）；
  - **M2 重冻面（已裁定，PL-2 执行）**：身份类 golden（树身份 11–15、content golden、p_io_3 TREE_ID 等）；**保住面**：地址/schema 字节类（17/18 地址、19 字节、22/23 地址）。
- **PL-2 实际态（2026-08-24 终审核对，兑现上条「已裁定」预判）**：
  - **M2 身份类 golden 重冻（16 项，diff 登记：前值→新值，主代理逐项核对）**：
    - 1 空 Table RefId `2e03c8d7…`→`1c6c9cb0…`；2 空 Sequence `f6ca637e…`→`ddf9ce63…`；3 空 Kv `f5ec2e12…`→`82bdddac…`；4 空 Blob `de893f27…`→`5a9fa702…`；
    - 7 Select `5d2ef3a4…`→`5eb4ec85…`、Join-Inner `b1fe1f78…`→`765df74e…`、Join-Left `8bab92d8…`→`77909bc5…`；10 注册空块 `df0cb4cf…`→`386083ec…`；11 空树 `5c9c5f4a…`→`b421e79e…`；12 inline 树 `34e83ce7…`→`80d36a83…`；
    - 13 ref+chunk 树 `33e321dc…`→`83c6aaaa…`；14 view 树 `9499fba6…`→`6dfa6239…`；17 Blob RefId `d58716fc…`→`d6479661…`；18 TreeId `72b0a3c2…`→`2a34434f…`；22 pruned TreeId `73004bba…`→`98719b9c…`；Pq 表身份（p_io_7_2 语义身份）`27ab7771…`→`4ee1fd39…`；
  - **M2 新增冻结**：golden 28 = SemanticValue 标量 encode 字节（S1 冻结）；golden 29 = semantic_table 形状指纹（S1 冻结）；golden 30 = semantic_tree walk 指纹（S1 冻结）；
  - **地址/schema 字节类保住（零动，主代理复核）**：17 地址 `d796cdd9`、18 树地址 `daeccfca` + ref 表地址 `c35c93d8`、19/24 commit id `ea454fc5`、22 地址 `25fb5e48`、23 地址 `3aa14961`、Pq 地址 `efaf05e1`、digest 协议、golden 26/27。
- 树与桶管道 golden 16–25 详值登记在 `../树与桶管道/03-开发状态.md`；本工单以 8 列/7 列现值为基线——golden 19/24 已按 9 列重冻（id `ea454fc5...` 不变），18/22/23 等其余 TB 面 golden **零变化**。

## 遗留清单

- **S7 v4 删除清单已执行（2026-08-24，删除面登记，与提交逐项核对）**：
  - 删 `src/io/{mod,layout}.rs` 整个模块（含 `lib.rs` 的 `pub mod io;` 与 io re-export 块）；
  - 删 `tests/object_golden.rs`、`tests/registry_m1.rs` 整文件；
  - 删 `tests/tree_space_contract.rs` 的 v4 部分（保留 m7 lock 面）；
  - 删 `p_io_3` 的 v4 半面（`pure_v4_commit_columns` 后半段 + `old_reader_degrades_to_empty_library` 整个）、`p_io_7_2` 的 `storage_kind_string_is_synced`；
  - TB 面 golden 16–25 **全部保留**（零变化）。
- **下游门控桥（用户 2026-08-24 裁定接受）**：S7 给下游 mzml-db（lib.rs + 3 tests）、uni-mass-db（lib.rs + 10 tests）加 `#![allow(deprecated)]` 纯 lint 抑制（零语义变化，均带「迁移后移除」标记）；移除责任在 v4→TB 迁移工单。
- **可调项七项实际取值（02 §7 回填，主代理终审裁定）**：
  ① `plugin` 模块落点 = 新增 `src/plugin/{version,block,tree,registry,boot,degraded}.rs`（`semantic.rs` 归 PL-2）；
  ② `Materialization` 保留枚举 + 兼容壳转发 + `From<DiskLayout>` / `TryFrom<Materialization>` 互转；
  ③ boot `library_id` = `"tree-space"` 固定字面量；
  ④ 退化磁盘版本 = `DiskVersion(String)` 原样，probe-None 时固定 `"unknown"`；
  ⑤ `ErrorCode::PluginMissing` 新增（独立错误码，未复用既有码）；
  ⑥ semantic golden 编号顺延 28–30（26/27 被 M1 占用）；
  ⑦ `TbLayout` 新增方法 write_boot / read_boot 均 **default method**。
- **polars 插件 + 转换器实体**：**不在此工单**（01 §3-2 / §7），单独工单承接；本工单 PL-2 只落 `converter` 挂钩（02 §6.4）；
- **下游 v4→TB 迁移**：**不在此工单**（01 §6 / §2-10），另开工单——含移除下游门控桥 lint 抑制（「迁移后移除」标记）的职责。
- **语义化已知边界（PL-2 实际态）**：`semantic_slot` 不覆盖的 View 族列（LargeUtf8 / LargeBinary / Utf8View / BinaryView）整表 fallback 回 IPC 字节公式——该类表**跨编码（IPC vs Parquet）身份可能不一致**；polars 工单扩展跨编码承诺时处理（届时把 View 族纳入列级 cast 即消除）。
- **已知优化项（PL-2 实际态）**：字典（Dictionary）/ RunEndEncoded 列 `semantic_slot` 走逐行 cast（O(n²)），大表有可观察开销——后续列级 cast 优化（语义公式不变，纯性能面）。

## 验证证据

- 2026-08-24（authority 落笔 / 立项）：纯文档，无代码，无验证命令义务（对齐树与桶管道 03 / 数据验证系统 03 立项先例）。
- **2026-08-24（PL-1 终审，主代理独立复跑）**：四命令全绿——fmt `--check`=0；dev-rust `-p tree-space -p tree-space-derive`=0；rust-189 `--all-targets`=0 零警告；dev-rust workspace `--all-targets`=0 零警告。关键 golden 断言全部冻结通过：17/18/19/22/23/24（TB 面字节）+ 26/27（本工单新增）+ p_io_7_2 parquet 对（地址 `efaf05e1...` / 语义身份 `27ab7771...`）。
- **2026-08-24（PL-2 终审，主代理独立复跑）**：四命令全绿——fmt `--check`=0；dev-rust `-p tree-space -p tree-space-derive`=0；rust-189 `--all-targets`=0 零警告；dev-rust workspace `--all-targets`=0 零警告（**107 组 ok**）。关键断言全绿：身份类 golden 重冻 16 项逐项通过（diff 登记如上）+ 地址/schema 字节类原值通过（17/18 地址、19/24 commit id `ea454fc5`、22/23 地址、Pq 地址 `efaf05e1`、digest 协议、golden 26/27）+ 跨编码四路相等断言 + converter 恒等断言。