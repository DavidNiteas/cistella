# 插件化改造：04 施工日志

**工单：tree-space 插件化改造（plugin overhaul）**
**本文件是追加型过程记录**：实现者按 [`02-施工路线图.md`](02-施工路线图.md) §4 施工协议逐小节追加施工日志；初审 / 终审结论与裁定在此批注。状态真相见 [`03-开发状态.md`](03-开发状态.md)。

---

## S1 施工日志（2026-08-24）

- **改动摘要**：新增 `src/plugin/version.rs`——`MemLayout::{Arrow55}`（内存枚举，`as_str` = `"arrow55"`）/ `DiskLayout::{ArrowIpc, ArrowParquet}`（磁盘字符串，`as_str` = `"arrow-ipc"` / `"arrow-parquet"`）+ `from_str`（显式映射，未知 → `None`）+ `probe_magic`（`ARROW1` → ArrowIpc、`PAR1` → ArrowParquet、其它 → `None`）；`src/lib.rs` 挂 `pub mod plugin;` + 根级 re-export `MemLayout` / `DiskLayout`。纯新增，零既有公共 API 改动。
- **测试**：`tests/p_l_1_versions.rs` 起步 3 断言——as_str→from_str 往返恒等（含大小写/未知串 → `None`）/ probe_magic 三分 / mem-disk 正交（编译即证，as_str 互不重叠）。四命令全绿；零新依赖。
- **偏离**：无（测试义务文字精确化非偏离——「三值 as_str → from_str 往返恒等」落为上述断言面）。
- **关闭判定**：✅ S1 关闭。零新依赖、零既有公共 API 改动（纯新增）。

## S2 施工日志（2026-08-24）

- **改动摘要**：
  - 新增 `src/plugin/block.rs`：`BlockPlugin` trait + `ArrowIpcBlockPlugin`（复刻 IpcMaterializer 行为）/ `ArrowParquetBlockPlugin`（复刻 ParquetMaterializer 行为，仅 `Table` kind）；
  - 新增 `src/plugin/tree.rs`：`TreePlugin` trait + `Arrow55TreePlugin`（包装 `tree::codec` 的 encode/decode/tree_id）；
  - 新增 `src/plugin/registry.rs`：`PluginRegistry` 进程级单例（`new` / `register` / `register_tree` / `validate_pair` / `route_disk` / `tree_plugin` / `global`；同 `(MEMORY, DISK)` 冲突 → `Err(TypeConflict)`，未配对组合 → `Err(SchemaMismatch)`）；
  - `layout/materializer.rs` 改写为兼容薄面：probe 委托 `DiskLayout::probe_magic`（返回值映射回 `Materialization`，签名不动）；native→Ipc 兜底保留；`Materialization` 枚举保留 + `From<DiskLayout>` / `TryFrom<Materialization>` 互转（可调项②取值）。
- **测试**：`tests/p_l_1_plugins.rs` 5 断言——IPC 字节恒等（golden 17 帧零字节变化证明）/ parquet 仅 Table（非 Table kind 拒）/ 配对冲突 / 路由正负例 / 树插件包 codec 等值。既有 p_io_7.2（parquet 混物化）/ p_io_7.3（force-IPC）零回归。
- **偏离**：
  ① const `MEMORY`/`DISK` → 方法 `memory()`/`disk()`——stable 1.89 dyn-compatible 约束下关联 const 不可作 trait 对象索引用，改方法形态语义零变化；
  ② `IpcMaterializer` / `ParquetMaterializer` 保留为零逻辑兼容壳——§5.2「公共 API 零破坏」优先于「删除」字面，既有调用点/测试依赖其存在，壳内转发插件。
- **返工**：IPC 插件 `matches` 恒 true——`kinds()` 全量含 Named 注册块，Named 块经 IPC 帧头匹配不误入退化候选。
- **关闭判定**：✅ S2 关闭。golden 17 零字节；插件冲突/路由测试绿；零新依赖。

## S3 施工日志（2026-08-24）

- **改动摘要**：
  - 新增 `src/plugin/boot.rs`：`BOOT_VERSION = 1` / `BootRecord`（boot_version / library_id / tree_mem_version / tree_disk_version）/ `boot_schema` 4 列（boot_version u16、library_id utf8、tree_mem_version utf8、tree_disk_version utf8）/ `encode_boot` / `decode_boot` 深校验（列名/类型/nullability 深比对）；
  - `TbLayout` 加 default method `write_boot` / `read_boot`（可调项⑦取值）；
  - FlatDir：`tb-boot/boot.ipc` 固定名原子写（对齐 `write_committed` 风格）+ `ensure_tb_dirs` 增建 `tb-boot/`；
  - SingleFile：`ObjectKind::Boot` 变体 + 对象目录 kind 字符串同步 + **固定 id** 寻址 + GC 可达集对 Boot 恒保留；
  - `create_with_materialization` 尾部写 boot（`library_id = "tree-space"`，可调项③取值）；SingleFile 两入口同构追加。
- **测试**：`tests/p_l_1_boot.rs` 3 断言——双布局 create→写→reopen 往返（4 列值正确）/ 坏、缺、未知树版本三路 `Err(BootstrapIncomplete)` / `boot_schema()` IPC 字节冻结（S8 编号 golden 26）。
- **偏离**：
  ① SingleFile boot 走 stage/epoch 管线（保全 p_io_7.3 trailer `[0,1,2]` 不回退）；
  ② `read_boot` 也实现为 default method（02 §5.5 只列 `write_boot` 为 default，read 同型补全）；
  ③ `decode_boot` 内置 tree_disk_version 校验（未知串 → `Err(BootstrapIncomplete)`，引导链第 1 步早失败，S4 依赖）。
- **返工**：结构性坏 boot → `BootstrapIncomplete`（非 `PayloadMalformed`——坏 boot 属引导失败，不归 payload 解码分类）。
- **关闭判定**：✅ S3 关闭。boot 三组测试绿（正/坏/未知版本全 `BootstrapIncomplete`）；既有 p_io_7.3（single_file GC 可达集）零回归；零新依赖。

## S4 施工日志（2026-08-24）

- **改动摘要**：
  - `open_with` 重排为 **§4-3 引导链八步**：①读 boot + decode_boot（校验 boot_version、解析树版本对，未知 → `BootstrapIncomplete`）→ ②路由树插件（None → `BootstrapIncomplete`）→ ③read_head → read_commit → TbPointers → ④读树字节 + tree_id 重算 + assert_tree_address → 树插件 decode → TreeImage → ⑤读 ref 表 + 版本侧表（pointers.versions Some 时）→ ⑥ref 表驱动重建桶（含退化槽，S6 填分支）→ ⑦XPathIndex 重建 → ⑧结构可解析检查；
  - `restore_bucket` 改 ref 表驱动：逐行按 row.address 读块对象（对象缺失 → `Err(DanglingReference)`——对象缺失 ≠ 插件缺失）；版本标定 = 侧表行 disk_ver 优先 + `probe_magic` 探测兜底；`route_disk` 命中 → 插件 decode → `put_envelope`；地址校验（hash vs row.address 不匹配 → `DigestMismatch`，不修复）；
  - `block_objects()` 保留（verify 全量面 / 既有测试仍用），扫描式重建彻底移除于 open 路径。
- **测试**：`tests/p_l_1_ref_driven.rs` 3 断言——ref 表驱动 ≡ 扫描式重建（Healthy 双布局库、envelope 集逐项相等）/ open 缺对象 → `DanglingReference`（区别于退化）/ boot 缺失 → 引导第 1 步 `BootstrapIncomplete`（不进入读 commit，证明引导顺序）。
- **偏离**：
  ① v_4 / v_5 夹具迁移（弱图为头，语义保留）；
  ② route 未命中时 S4 暂态 `SchemaMismatch`——退化面（S6）未落地前的过渡分类，S6 正式换退化语义。
- **关闭判定**：✅ S4 关闭。ref 表驱动与扫描式重建等价断言绿；既有 p_io_*/v_* 全量零回归；零新依赖。

## S5 施工日志（2026-08-24）

- **改动摘要**：
  - 新增 `tb_versions_schema`（3 列：xpath Binary(nn) / mem_ver Utf8(nn) / disk_ver Utf8(nn)）+ `encode_versions_table`（canonical 单批、按 xpath 升序）+ `decode_versions_table`（深 schema 校验 + 升序校验）+ `VersionRow`；
  - **commit 批 8→9 列**：第 9 列追加 `tb_versions: FixedSizeBinary(16), nullable`；`decode_tb_commit_pointers` 上限放开 8→9；`TbPointers` 加 `versions: Option<[u8;16]>`（7/8 列 commit 解码 → `None`，兼容既有读路径）；`tb_commit_batch` 追加 `versions` 参数；
  - `tb_commit_id` 五 part 公式**不变**（第 9 列不入公式）→ golden 19 重冻 id 不变预期成立；
  - `TbLayout` 加 default method：`tb_versions_path` / `write_versions_object` / `read_versions_object`；FlatDir `tb-versions/` 目录 + `ensure_tb_dirs` 扩展，SingleFile 走对象目录；
  - `write_commit` 插入步骤 3.5（ref 表写后、commit 对象写前）：leaf_refs 每行 → (canonical xpath, mem_ver, disk_ver)，来源 = 路由该 leaf 的块插件 MEMORY/DISK 声明；全库单磁盘版本也**显式写入**（不做稀疏优化）；
  - GC 可达集扩展 versions 对象（遍历与 sweep 保护）。
- **测试**：`tests/p_l_1_versions.rs` 扩展 4 断言——commit→读回侧表 roundtrip（含 parquet 混物化行 disk_ver = `"arrow-parquet"`）/ 9 列 decode 正确 + **8 列旧 commit 仍可读**（versions = None）/ golden 19 重冻 **id 不变** / `tb_versions_schema` IPC 字节冻结（S8 编号 golden 27）。
- **偏离**：
  ① **golden 24 连带重冻**——golden 24 与 golden 19 为同一 commit 身份 `ea454fc52da0fdc1bcce099eb7f75c9e` 的两个字节视图（8 列 / 9 列单行 batch），commit 批 8→9 列必然双变；id 断言均不变；
  ② GC 可达集扩展（versions 对象纳入 GC 遍历与 sweep 保护）；
  ③ 版本侧表地址域 `tb-versions`（新域标签，不改既有 tb-* 域语义）。
- **返工**：物理 8 列 decode 断言补强（旧 commit 读侧兼容由反射实现 + 断言双重证明）。
- **关闭判定**：✅ S5 关闭。**golden 19/24 id 保持 `ea454fc5...`**（主代理逐条核对）；既有 p_io_5（8 列 commit 相关）读侧零回归；其余 golden 零变化。

## S6 施工日志（2026-08-24）

- **改动摘要**：
  - 新增 `src/plugin/degraded.rs`：`DiskVersion(String)`（可调项④取值：未知字面量原样，probe-None 固定 `"unknown"`）/ `DegradedBlock{kind, disk, raw}`（kind 从帧头解析，无法解析 → `PayloadMalformed` 不退化）；
  - `Bucket` 加并行槽：`put_degraded` / `degraded` / `degraded_get`（按 RefId 索引，与 blocks 并行）；
  - `ErrorCode::PluginMissing` 新增（可调项⑤取值，`TreeSpaceError` with_context("kind"/"disk")）；
  - project 路径命中退化叶 → `Err(PluginMissing{kind, disk})`；verify 退化块**只验地址**（`block_blob_address(raw) == row.address`，不比对 canonical 身份）+ 计入 degraded 计数；
  - restore 退化三触发：未知磁盘版本 / `route_disk` 无插件 / 插件 decode 失败 → `put_degraded` 保留原字节；**地址校验恒执行**（不因退化豁免完整性）。
- **测试**：`tests/p_l_1_degraded.rs` 4 断言——未知版本块库 open 成功（树可解析）+ 其余块可读 + `project<T>` 命中退化叶 → `Err(PluginMissing{kind, disk})` / verify 只验地址（篡改退化 raw → `DigestMismatch`、身份面不比对）+ degraded 计数 / 退化块 hash 校验仍执行（地址伪造负向绿）/ 同树退化与正常块共存——正常块投影/读取不受影响。
- **偏离**：
  ① 投影挂钩落点 = `decode_block_leaf` 叶子面（全 project 路径一致，不单挂 `TbLibrary::project`）；
  ② probe-None 时磁盘版本固定 `"unknown"`（可调项④的取值细节，不新造枚举变体）；
  ③ probe 兜底语义升级：未知 magic → 退化（正式替换 S4 暂态 `SchemaMismatch`）；
  ④ `put_degraded` 冲突语义：同 id 重复 put 覆盖（真正冲突才 `Err`，按实现登记）。
- **关闭判定**：✅ S6 关闭。4 组退化测试绿；既有 verify 全量零回归；零新依赖。

## S7 施工日志（2026-08-24）

- **改动摘要**：
  - v4 内部废弃：删 `src/io/{mod,layout}.rs` 整个模块（含 `lib.rs` `pub mod io;` 与 io re-export 块）；删除面逐项执行——`tests/object_golden.rs`、`tests/registry_m1.rs` 整文件，`tests/tree_space_contract.rs` v4 部分（保留 m7 lock 面），`p_io_3` v4 半面（`pure_v4_commit_columns` 后半段 + `old_reader_degrades_to_empty_library` 整个），`p_io_7_2` 的 `storage_kind_string_is_synced`；**TB 面 golden 16–25 全部保留**；
  - `Library` 面模块级 `#![deprecated = "v4 table-space library is superseded by TbLibrary (插件化改造)"]` 门控；crate 内部消费点 `#[allow(deprecated)]` 精确覆盖；
  - lib.rs 死 re-export 清理（`io` / v4 专属 verify 面移除，保留 `TbLibrary` / `TbLayout` / `validate` 等活性面）；
  - **下游门控桥（用户 2026-08-24 裁定接受）**：mzml-db（lib.rs + 3 tests）、uni-mass-db（lib.rs + 10 tests）加 `#![allow(deprecated)]` 纯 lint 抑制（零语义变化，均带「迁移后移除」标记）；移除责任在 v4→TB 迁移工单。
- **测试**：新增 `p_l_1_v4_deprecated`（下游可编译面：`use tree_space::Library` / create 经显式 `#[allow(deprecated)]` 编译通过）；全量回归全绿（`--all-targets` 零警告）。
- **偏离**：① 下游 lint 抑制门控桥（用户裁定接受，登记 03 遗留清单）。
- **关闭判定**：✅ S7 关闭。删除面与 03 登记逐项一致（主代理核对）；`Library` 面下游仍可编译（门控生效）；TB 面 golden 16–25 零变化；零新依赖。

## S8 施工日志（2026-08-24）

- **改动摘要**：测试锚定收口——
  - 新增 `p_l_1_golden_no_regression`：28 条 LEDGER（现存 golden 全数登记 + 零变化断言）+ 新增 golden 26/27 冻结编号（boot / tb_versions schema 字节）；
  - 新增 `p_l_1_s8_anchors`：`boot_unknown_tree_version` open e2e（未知树版本 → `BootstrapIncomplete`）+ route 正负例跨点断言；
  - `p_io_5` legacy 7 列注释修正 + typed-project 断言补强。
- **测试**：四命令全绿；关键 golden 断言（17/18/19/22/23/24/26/27、p_io_7_2 parquet 对：地址 `efaf05e1...` / 语义身份 `27ab7771...`）全部冻结通过。
- **偏离**：
  ① S6 观察 1 前提纠正——Blob 叶无版本侧表行 → probe 命中 `ARROW1` → **健康恢复（非退化）**；S6 退化测试构造据此修正；
  ② **golden 26/27 编号占用**——M1 的 boot / tb_versions schema 冻结收编 26/27，M2 semantic golden 顺延 **28–30**（可调项⑥实际取值）。
- **关闭判定**：✅ S8 关闭。可调项⑥实际取值落定（golden 编号被 M1 占用收口）；全系列七项可调项均经 S2/S3/S5/S6/S8 逐项回填、主代理终审裁定完毕。

## PL-1 关闭判定（2026-08-24）

- **判定**：✅ **PL-1 关闭**——四命令全绿（fmt `--check`=0；dev-rust `-p tree-space -p tree-space-derive`=0；rust-189 `--all-targets`=0 零警告；dev-rust workspace `--all-targets`=0 零警告）；02 §5.10 八条关闭标准全部满足：现存 golden 零字节变化（唯一例外 golden 19/24 按 9 列重冻、id 不变、主代理逐条核对）；缺插件场景树可解析 / 块退化 / 其余可读 / verify 报 degraded（§2-7 全语义实证）；boot 坏 / 缺 / 未知树版本 → `Err(BootstrapIncomplete)`；`project<T>` 命中退化块 → `Err(PluginMissing{kind, disk})`；ref 表驱动等价绿；版本侧表读写绿（含 8 列旧 commit 读侧兼容）；**M1 零新依赖**；既有公共 API 零破坏（`Materialization` / `TbLayout::block_materialization` 等兼容面签名未动，`TbLayout` 只有 default method 加法）；04 S1–S8 施工日志齐 + 本文档回填完成。
- **登记**：`03-开发状态.md`——Status →「PL-1 已关闭（2026-08-24）；PL-2 待施工」；里程碑登记 PL-1 🟢（附 8 commit、分支 refactor/arrow-scientific-workspace、已推送）；golden 登记改写为 PL-1 实际态；遗留清单回填（S7 删除面 / 下游门控桥 / 可调项七项取值）；验证证据追加终审四命令全绿。**本工单无 git 写操作**（提交/推送由主代理另行执行，04 仅登记现状）。

## PL-2 S1 施工日志（2026-08-24）

- **改动摘要**：新增 `src/plugin/semantic.rs`——`SemanticValue` 八变体（Null / Bool / Int(i64) / UInt(u64) / Float(f64) / Bytes / Str / List）+ `encode` / `decode` 自描述字节流（tag + u32 长度前缀，不经 arrow metadata）；`semantic_table`（粗粒度逻辑形状指纹：字段名 / 类型 / nullability / 嵌套逻辑形状，走类型对位矩阵）；`semantic_tree`（确定性 walk，**复用 `order_children` 排序**——语义指纹与物理行序解耦，同一逻辑树不同存储序 → 同指纹）。golden 28 / 29 / 30 冻结为 hex 常量断言。
- **类型对位矩阵（实现登记）**：Dictionary / RunEndEncoded 列**解引用到值类型**再语义化；Utf8 / Binary / List 系列归一（宽度独立标签，编码宽度无关）；Float 规范化（-0.0→+0.0、NaN 规范位型）；Timestamp 区分 `tz=None` 与空串 tz。
- **测试**：`tests/p_l_2_semantic.rs` **3+1 断言**——`p_l_2_semantic_value_roundtrip`（golden 28 冻结 + 长度前缀解码往返）/ `p_l_2_semantic_table_fingerprint_frozen`（golden 29 冻结 + **加/减 arrow metadata 指纹不变**实证）/ `p_l_2_semantic_tree_walk_frozen`（golden 30 冻结 + 行序等价重排 → 同指纹）+ 补强 `p_l_2_semantic_tree_keyed_order_independent`（keyed 顺序独立性）。
- **偏离**：
  ① golden 编号顺延 **28–30**（§6.3 原 26–28——26/27 已被 M1 的 boot / tb_versions schema 冻结占用，可调项⑥实际取值）；
  ② §6.3 签名 `#[derive(Eq, PartialEq, Ord, PartialOrd)]` 因 `Float(f64)` 非 Ord 改**手写 Eq/Ord**（编译硬限制；手工实现语义对齐派生意图——编码期 NaN 已规范化、派生态可穷举对齐）。
- **关闭判定**：✅ PL-2 S1 关闭。三 golden 冻结绿 + metadata 无关 / 行序无关实证绿；零新依赖；纯新增文件、零既有公共 API 改动。

## PL-2 S2 施工日志（2026-08-24）

- **改动摘要**：
  - `src/plugin/block.rs`：`ArrowIpcBlockPlugin` / `ArrowParquetBlockPlugin` 的 `ref_id` 切语义公式——表块 `hash(kind.domain(), [semantic_table_value(形状 + 列数据)])`；非表块按 kind 映射（Blob→Bytes、Sequence→List、Kv→List 对、Named→Bytes）；`BlockPlugin::semantic` 挂钩落地；
  - `src/plugin/tree.rs`：`Arrow55TreePlugin::tree_id` 切 `hash(tb-tree, [semantic_tree(image)])`；`TreePlugin::semantic` 挂钩落地；
  - `src/plugin/registry.rs`：`converter` 挂钩（01 §4-1 [M2]）——`MemoryConverter` trait + 内置恒等实现 `IdentityMemConverter` + `register_converter` / `converter(from, to)` 定向查询（同版本恒等自动注册、重复注册 → `TypeConflict`、未注册组合 → `None`；polars 转换器实体归 polars 工单）；
  - 身份类 golden 重冻 **16 项**：diff 登记于 03（前值→新值逐项）。
- **测试**：`tests/p_l_2_identity.rs` 3 断言——`p_l_2_semantic_identity_cross_encoding`（**M2 关闭标准核心**：同一逻辑内容「IPC 编码」与「手工语义指纹直算」两路构造 → `RefId` 四路相等；`TreeId` 同理）/ `p_l_2_identity_goldens_refrozen`（身份类重冻后绿 + **地址/schema 类原值通过**）/ `p_l_2_converter_hook`（`converter(Arrow55, Arrow55)` 恒等可用；未注册组合 → `None`；重复注册 → `TypeConflict`）。
- **偏离**：
  ① **表身份 = 形状 + 列数据**（非 §6 单形状）——单形状致「同 schema 不同数据」同 RefId，BTreeMap 去重会丢数据（实证），故列数据入指纹；
  ② 遇**不可解析字节**：表/树身份 fallback 回 M1 字节公式；`ipc.rs` 加 `catch_unwind`（arrow reader 遇损坏帧 panic → `Err`，不崩进程）；
  ③ Named 统一 `Bytes(payload)` 字节语义（直接编 payload 字节，不经块内层语义化）；
  ④ 域标签沿 **`kind.domain()`**（tb-block-* 家族）精确化——各 kind 域各自独立，不合并域语义。
- **关闭判定**：✅ PL-2 S2 关闭。跨编码四路相等绿（身份类别编码 = 同一逻辑内容）；身份类重冻 16 项 diff 与 03 登记逐项一致（地址/schema 字节类零动）；converter 三态绿；零新依赖；消费侧（verify / project / ref 表驱动）零改动。

## PL-2 关闭判定（2026-08-24）

- **判定**：✅ **PL-2 关闭**——四命令全绿（fmt `--check`=0；dev-rust `-p tree-space -p tree-space-derive`=0；rust-189 `--all-targets`=0 零警告；dev-rust workspace `--all-targets`=0 零警告，**107 组 ok**）；02 §6.5 五条关闭标准逐条满足：同一逻辑内容经不同编码 RefId/TreeId 相等（跨编码四路断言绿，语义 golden 28–30 钉死）；身份 golden 重冻 diff **仅限身份类 16 项**（地址/schema 字节类原值通过）；semantic golden 28–30 冻结；**零新依赖**；公共 API 零破坏（`converter` 挂钩为纯加法）；**PL-2 关闭即本工单 PL 系列全关**——01/02/03 终态登记、工单待闭合归档（沿 `../README.md` §4 闭合流程）。
- **登记**：`03-开发状态.md`——Status →「PL-1 / PL-2 已关闭（2026-08-24）；待闭合归档」；当前状态表 / 里程碑登记 PL-2 🟢（附 commit S1 `5c2842f`、S2 `fa86cb2`、分支 refactor/arrow-scientific-workspace、已推送）；golden 登记追加 PL-2 实际态（重冻 16 项 diff / 新增冻结 28–30 / 地址类零动清单）；遗留清单追加（语义化已知边界 / 已知优化项）；验证证据追加 PL-2 终审四命令全绿。**本工单无 git 写操作**（提交/推送由主代理另行执行，04 仅登记现状）。