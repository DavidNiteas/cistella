# 交换协议：03 开发状态

**工单：交换协议（数据交换与同步）**
**Status：E-1/E-2/E-3/E-4 已关闭，交换协议工单收口**
**Date：2026-08-23**
**本文件是工单状态的唯一真相源**；全局工单索引见 [`../开发状态.md`](../开发状态.md)。规范见 [`01-目标与设计.md`](01-目标与设计.md)；路线图见 [`02-施工路线图.md`](02-施工路线图.md)；过程记录见 [`04-施工日志.md`](04-施工日志.md)（随首段施工创建）。

---

## 当前状态

| 阶段 | 状态 | 一句话 |
|---|---|---|
| E-1 核心抽象 + 运行时配置系统 | 🟢 已关闭（2026-08-23 验收）：`src/exchange/{mod,config}.rs` + `lib.rs` 接线 + 12 断言绿；块抽象（`ExchangeKey` 树/块）+ 运行时配置（`RuntimeConfig` L1/L2 + `RuntimeTable` L3） | 类型层表达树约束（`TreeAccess` 无 Lazy、`ExchangeKey::Tree` 无 payload） |
| E-2 权限 + 访问形式 | 🟢 已关闭（2026-08-23 终审通过）：`src/exchange/{ownership,access,proxy}.rs` + `mod.rs`/`config.rs`/`lib.rs` 加法 + `tests/e_2_ownership_access.rs`(12)/`tests/e_2_proxy.rs`(8)；所有权判定 + 代理写（回执三态）+ 三种访问形式（树仅映射/全读） | 20 断言绿；四命令全绿零回归；6 项偏离裁定接受 |
| E-3 同步 | 🟢 已关闭（2026-08-23 终审通过）：`src/exchange/{merge,sync}.rs` + `mod.rs`/`lib.rs` 接线 + `tests/e_3_merge.rs`(13)/`tests/e_3_sync.rs`(12)；pull/push + merge 原语 + xpath 范围 | 25 断言绿；四命令全绿零回归；5 项偏离裁定接受 |
| E-4 转换器接口 + 布局实现 | 🟢 已关闭（2026-08-23 终审通过）：`src/exchange/translator.rs` + `layout/{flat_dir,single_file}.rs` 的 `ObjectSpan` 接缝 + 磁盘 mmap + 失效暴露 + IPC 抓取 + Lazy + `persist_block` | 24 断言绿；四命令全绿零回归；1 项偏离裁定接受 |

## 里程碑登记

| 里程碑 | 状态 |
|---|---|
| authority 落笔（01 + 02 + 03） | 🟢 已落笔（2026-08-23，九节：块抽象 / 运行时配置系统 / 权限 / 访问形式 / 同步 / 同步原语清单 / 失效语义 / 范围边界 / 纪律接续） |
| E-1 核心抽象 + 运行时配置系统 | 🟢 已关闭（2026-08-23 验收） |
| E-2 权限 + 访问形式 | 🟢 已关闭（2026-08-23 终审通过） |
| E-3 同步 | 🟢 已关闭（2026-08-23 终审通过） |
| E-4 转换器接口 + 布局实现 | 🟢 已关闭（2026-08-23 终审通过） |
| E 系列大回顾 | 🟢 已完成（2026-08-23）：E-1（12）→ E-2（20）→ E-3（25）→ E-4（24）共 81 断言，四阶段四命令全绿零回归，工单收口 |

## 偏离裁定登记（E-2，2026-08-23 终审，6 项全部接受）

| # | 偏离 | 裁定 | 理由 |
|---|---|---|---|
| ① | 根级不 re-export `read_block` | 接受 | 与冻结的 `block::read_block` 重名；保留完整路径 `exchange::access::read_block`（02 §6.3 接线注 + §6.8） |
| ② | `OwnedSnapshot`/`ProxyRequest` 手写相等 | 接受 | `TreeImage`/`Bucket` 无派生 `PartialEq`；手写 `PartialEq`/`Eq` 语义等价，避免为派生改动冻结类型 |
| ③ | `await_receipt` 回显 | 接受（返工已修） | 回执按 `request_id` 回显匹配；返工修复 Unix `dup<0` 的 `from_raw_fd(-1)` UB |
| ④ | `Source::Ipc` 不可字节序列化（wire 只带 tag） | 接受 | IPC 句柄（fd/名字）不可稳定字节序列化；wire 层只带 `Source` tag，句柄旁路传递 |
| ⑤ | `publish_*` region 生命周期契约 | 接受（补 01 §3.2） | 发布方须持有 `SharedRegion` 至接收方消费完（已补入 01 §3.2 书面规则） |
| ⑥ | Send+Sync 第 12 断言 | 接受 | `MappedView`/`LazySnapshot` 的 `Send + Sync` 编译期断言为 `e_2_ownership_access.rs` 第 12 项，由 02 §6.5 关闭标准覆盖（§6.4 数量口径注） |

## 偏离裁定登记（E-3，2026-08-23 终审，5 项全部接受）

| # | 偏离 | 裁定 | 理由 |
|---|---|---|---|
| ① | `fetch_snapshot` FullTree 取源全桶（不裁剪） | 接受 | §7.4 `e_3_fetch_snapshot_full_tree` 断言 `bucket == 源全桶`；「按 `reachable_refs` 裁剪」措辞与之冲突，修正为 FullTree → 源全桶（不裁剪）、TreeFragment → 裁剪到范围内可达集（02 §7.2/§7.3） |
| ② | `touched = shadow ∪ in_scope` | 接受（理由以初审更正为准） | 「前缀在下、祖先 Node 需递归穿透」——in-scope 前缀的祖先 `Node` 必须一路穿透，故 `touched` = 穿透祖先（shadow）∪ in-scope 字段；flash 原说理写反 |
| ③ | `merge_image`/`reachable_refs` 不实际产 `Err` | 接受 | 纯函数签名保留 `Result` 对齐错误语义面，但当前实现无错误路径，不引入虚假错误分支 |
| ④ | pull 目的地须已加载库 | 接受 | `dest.commit` 依赖已加载的 `LoadedState`，pull 前置要求 dest 已加载库 |
| ⑤ | pull 后旧块留孤儿、roundtrip 断言放宽 | 接受 | `commit` 只写不删（01 §6），dest 旧块留孤儿直到 GC，故「bucket 与 source 一致」不可达；放宽为「源可达块 ⊆ dest 桶（重开后）」（02 §7.4） |

## 偏离裁定登记（E-4，2026-08-23 终审，1 项全部接受）

| # | 偏离 | 裁定 | 理由 |
|---|---|---|---|
| ① | 改 E-2 断言 `e_2_mapped_disk_source_rejected` → `e_2_mapped_disk_source_maps_committed_tree` | 接受 | `Source::Disk` 映射升级为真实磁盘映射（`Err(MappingAmbiguous)` 分支落地为映射已提交树，02 §8.7-1）；行为升级、非代码回归 |

## golden 登记

- 无冻结（纯新工单，尚无代码）。

## 遗留清单

- 交换协议取代树与桶管道 P-SH / P-IO-8（待回写树与桶管道批次表与全局索引）；
- 运行时表 schema 先覆盖现状字段（持有者/来源/获取方式/访问形式/失效状态 + 树库级所有权），下游可魔改；
- 转换器逐项实现同步原语清单时，单文件「原子写→全量写」是首项确认点。

## 验证证据

- 2026-08-23（authority 落笔）：纯文档，无代码，无验证命令义务。
- 2026-08-23（E-1 验收）：12 断言绿；四命令全绿；既有零回归。
- 2026-08-23（E-2 终审）：四条命令独立复跑全绿；既有 P-IO-2..7 / v4 全套零回归；20 断言绿（12 + 8）；零新增第三方依赖；`MappedView`/`LazySnapshot` `Send + Sync`；既有公共 API 零破坏；6 项偏离裁定接受（见上）。
- 2026-08-23（E-3 终审）：四条命令独立复跑全绿；25 断言绿（13 + 12）；既有零回归；零新增第三方依赖；既有公共 API 零破坏；5 项偏离裁定接受（见上）。
- 2026-08-23（E-4 终审）：四条命令独立复跑全绿；24 断言绿（9 + 10 + 5）；既有 E-1(12)/E-2(20)/E-3(25) 与 P-IO-2..7 / v4 全套零回归；零新增第三方依赖；既有公共 API 零破坏；1 项偏离裁定接受（见上）。
