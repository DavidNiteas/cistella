# tree-space 模块化子树改造 · 开发状态

- 工单：`crates/tree-space/_dev/archive/模块化子树改造/`
- 立项：2026-08-30（用户 A 层直接指令；上游 = uni-mass-db MVP 重设计 R0）
- 当前里程碑：**M1 🟢 关闭**（2026-08-31：施工 → 初审 → 返修 → 经理终审 workspace 全绿；S1c 路关闭）
- 拍板记录：语义七条（§1 六条 + §5 多位置派生第七条，用户 2026-08-30 拍板，见 01）

## 待拍板（已全部关闭，存档）

- ~~模板 Rust 载体~~ → P3 候选c（拍板落定）。
- ~~编译期报错机制~~ → const-eval 断言 + 宏接线（P3 的一部分，M1 已落地）。
- ~~语义解读确认~~ → P4 读法A（拍板落定）。
- ~~definition 双层关系裁定~~ → P5 单向投影（拍板落定）。


## 拍板记录

- P3 候选c 混合 / P4 读法A / P5 单向投影（2026-08-31 用户拍板，01 §7）。

## M1 施工范围（经理裁定 2026-08-31）

- **范围 = 图纸 02 §6 任务 1-3 + 5**：const 特性 spike → `template` 模块（§1.7 类型全集 + 正交/合并 const 判定 + `tree_compose!` 宏 + `points_to` 端点组合期检查）→ `project` 投影函数 + 等价守卫框架；七棵树模板落纸（任务 4）归 MVP 重设计 S2、workspace 超集示范（任务 6）后续。
- **编译期报错验证零新依赖**：用 rustdoc `compile_fail` doc-test 三例（正交违例 / 端点缺失 / 槽位非法）自动验证——不引入 trybuild；值级冲突检测另以单元测试覆盖 `CONFLICT`/const fn 面。
- **跨 crate 等价守卫**（project ≡ uni-mass-db translate 产物）：依依赖方向（tree-space 不依赖 uni-mass-db）该测试归 S2 落地；本工单交付框架 + tree-space 内 fixture 模板自测。
- 文件面：`tree-space/src/template/` 新模块 + `lib.rs` 一行导出；其余文件零改动（内核零破坏约束 §4.3）。

## M1 关闭记录（2026-08-31）

- **施工**（干事-g53f-autodl 续派）：template/ 七文件（spec/composite/check/project/compose/tests/mod）+ `tree_compose!` 宏（单条 const assert + CONFLICT 明细）+ 五步检查管线（坐标系/挂载槽/At 锚/端点/路径冲突）+ project 投影 + 等价守卫框架 + 25 模板单测 + 3 compile_fail doc-test。spike 结论：泛型 const fn 读关联 const 可用（rustc 1.95.0）；const panic 消息走 concat!+stringify! 烘焙。**391 passed / 0 failed**（64 suites）。
- **初审**（干事-g53f-autodl 新实例，只审不改）：P3/P4/P5 + 语义七条逐条对位通过；compile_fail right-reason 独立复核（Temp 目录真实编译，恰好 3 个 E0080 且 CONFLICT kind 探针亲证三类冲突对应）；数字亲证 391/81/58/183 全属实；边界零破坏（template/** + lib.rs 一行，Cargo.toml 零改动）；两项设计补白同意。**P2-1 = TypeId 派生公式与 uni-mass-db persist/typeid.rs 权威规则不一致 → S2 等价守卫结构性恒假风险**。
- **经理裁定（P2-1）**：模板层 type_id 定位 = **plan 内临时身份（provisional）**——模板层无 owner 维度、不可能复现权威公式；权威 TypeId 由 S2 的 uni-mass-db 侧投影按既有 typeid 规则（owner 注入）重派；跨 crate 等价守卫以重派后全值比较为准。不统一公式、不改既有 manifest 对账面。
- **返修**（施工干事）：纯文档修正五处（provisional 定位 / mod.rs 过度承诺消除 / equivalent_to 口径 / 补白① rustdoc / 六处 doc 链接清零）；391 不变、零行为改动。
- **经理终审**：全 workspace 测试 0 failed + fmt 零 diff。M1 关闭。

## 工单终态（2026-08-31）

- 全部里程碑关闭（M0 语义图纸 → M1 施工/初审/返修/经理终审）。下游消费兑现 = MVP 重设计 S2 七棵树模板落纸 + MvpComposite 组合（§5.3 清单全部落地；类型扩展接入点 ConstType List 已扩）。workspace 超集组合示范（图纸 §6 任务 6）留后继工单。工单归档 → `_dev/archive/模块化子树改造/`。