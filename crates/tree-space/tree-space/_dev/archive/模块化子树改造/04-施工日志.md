# tree-space 模块化子树改造 · 施工日志

## 2026-08-30 · 经理：立项

- 用户 A 层指令：模块化子树 = 编译期树模板组合系统（语义六条落纸 01 §1，最高优先级）；运行期结构不可变；树嵌套之后考虑。
- 与 uni-mass-db MVP 重设计工单 R0 建立引用关系（R0 = 本工单的 uni-mass-db 侧投影）。
- M0 语义图纸待派发（模板载体/组合算子/兼容路径/definition 映射四任务）。

## 2026-08-31 · 图纸干事：M0 语义图纸

- 产出 `02-设计图纸.md`（覆盖 02-施工路线图 M0 任务 1-4 全部四项）：§1 载体三候选深比、§2 组合算子与检查时机矩阵、§3 多位置派生与挂载坐标系机制化、§4 向后兼容与内核零破坏清单、§5 definition 双层映射与七棵树模板清单衔接、§6 M1 拆解草案。
- 拍板点呈报：①模板载体推荐**候选 c 混合**（结构 const 化 + 身份/挂载类型化 + 组合宏接线；const-eval 是稳定 Rust 上编译期正交检查的唯一可行路，候选 a 因无 negative reasoning 被否、候选 b 因 const 泛型容量税被否）；②编译期报错机制推荐 **const-eval 断言 + 宏接线**（类型系统/build.rs 否决理由在案）。
- 语义解读呈报一项：05 R0 增补组合规则②「叶子重名 → 结构必须一致」存在读法 A/B（§2.2），图纸按读法 A 落纸（结构一致 = 正交模式放行闸门），请确认。
- 裁定建议：definition = 模板的运行时投影 + 校验目标（单向投影；属性分工表 §5.2——description/semantic/unit/relation 归下游附着表，IndexPolicy 双层均不进走 RuntimeConfig）。
- 纪律自核：禁 commit（全程零 git 操作）；证据 文件:行号；产出文件 UTF-8 无 BOM（字节自检通过）。

## 2026-08-31 · 经理：M1 派发 + 范围裁定

- P3/P4/P5 拍板已落（01 §7；读法 A 确认关闭 §2.2 呈报）。M1 施工按 §6 任务 1-3+5 派发干事-g53f-autodl（续派图纸会话）。
- **范围裁定**：任务 4（七棵树落纸）归 MVP 重设计 S2、任务 6 后续；compile_fail doc-test 三例为编译期报错的自动化验证（零新依赖）；跨 crate 等价守卫测试归 S2（依赖方向）。登记于 03。

## 2026-08-31 · 施工干事：M1 交付

- 范围：图纸 §6 任务 1-3+5。落地面 = `tree-space/src/template/{mod,spec,composite,check,project,compose,tests}.rs`（7 文件，新增）+ lib.rs 一行 `pub mod template;`；其余文件零改动（宏经 `#[macro_export]` 供根路径 `tree_space::tree_compose!`）。
- **spike 结论（rustc 1.95.0，临时区验证未入库）**：①泛型 const fn 读泛型参数关联 const **可用**（含 `&'static str` 关联 const 逐字节比较、const item 内求值）——留回归测试 `generic_const_fn_reads_associated_consts`；②const panic 消息仅接受**展开期字面量**：`assert!(cond, "lit")` 与 `concat!`+`stringify!` 组合可用；const `&str` 值报 "format argument must be a string literal"、格式化参数报 E0015——故宏消息在展开期 `concat!`+`stringify!` 烘焙、明细走生成物 `CONFLICT` const；③附带验证：const fn 内 `Option::is_none`/带载荷枚举匹配/固定缓冲写入/`&CONST` 传参全可用。采用策略：单条 `const _: () = assert!(composite_check(&SPEC).is_none(), concat!(…))`（值传具体关联 const，不依赖泛型 const 边界）。
- 机制要点：复合 = 结构化 `CompositeSpec`（不折叠、复合不实现 TreeTemplate、声明序 fold）；检查管线五步（坐标系合法性 → 挂载槽/required_slots → At 锚 → points_to 端点 → 路径声明冲突；intra-source 两模式均查、cross-source 仅正交查——P4 同指纹去重/异指纹报错，合并模式交 fold）；投影 = composite_check 重跑（纵深防御）→ 确定性 fold（后到整叶覆盖；表下被覆盖内容 drop）→ `DomainType`/`TableType` 物化（TypeId = xxh3_128 模板层 domain tag 派生、tier 实例 `{prefix}{i}` 枚举）；`TypeRegistryPlan::equivalent_to` 为等价守卫框架。
- 施工修复记录：①宏挂载数组重复补逗号分隔（`$(…),*`——无分隔相邻结构体字面量解析错）；②arrow 55 `Timestamp` 时区参数为 `Arc<str>`；③check.rs 补 `TemplateTable` import；④空 path 条目在 `validate_spec_shape` 拒绝。
- 设计补白与追认推论（呈报经理）：①投影子域命名 = 节点段名，同名同版不同形的域在注册时报 IdentityCollision、同形去重——跨位置同名域需模板作者错名（fixture 以 geo-consensus 平铺示范）；②P4 推论：同槽位重复挂载同一模板（指纹全同）正交模式下去重放行（图纸 §3.3「同位双挂必报错」被 P4 覆盖），`SameSlotDedup` 测试固化。
- 验收证据：tree-space **64 suites / 391 passed / 0 failed**（含模板模块 25 单测 + 全 integration + doc 4 例，compile_fail×3 经 no_run 翻转验证 right-reason = E0080 const 断言）；`cargo fmt --all -- --check` 零 diff；零 warning；template/** 纯 ASCII、UTF-8 无 BOM（字节自检）；下游回归 **tree-space-index 26/81、mzml-db 17/58、uni-mass-db 25/183** 全绿零失败。
- 纪律：禁 commit（零 git 写操作）；未触碰 table-index/uni-mass-db/exchange（并行干事改动原状保留）；并行构建锁等待正常应对，未设 timeout。

## 2026-08-31 · 施工干事：P2 返修（TypeId 派生定位，纯文档）

- **经理 P2-1 裁定**：模板层 `project()`/`to_registry()` 产出的 type_id 定位为 **plan 内临时身份（provisional）**——模板层无 owner 维度，不可能复现 uni-mass-db persist/typeid.rs 的权威派生公式（xxh3_128 长度前缀 [owner,table,version] → to_be_bytes）；权威 TypeId 在 S2 由 uni-mass-db 侧投影（project_definition）按既有 typeid 规则（owner 注入）重派后方可进入持久化/manifest 对账；跨 crate 等价守卫以**重派权威 id 后**的全值比较为准。不统一公式、不改动既有库 manifest 对账面。
- 修正点（全部仅 doc 注释，零代码行为改动）：
  1. `project.rs` `domain_type_id`/`table_type_id` doc：定位改「provisional (plan-internal)」——plan shaping 与 in-crate 注册自测专用；权威 id 由下游 owner 注入重派；公式不统一；S2 等价守卫以重派后全值比较为准。
  2. `mod.rs` 模块文档 Type-id note：消除过度承诺——跨 crate 等价守卫 = **structural comparison after authoritative TypeId re-derivation at the uni-mass-db projection boundary (S2)**；`equivalent_to` 为同一比较的 in-crate 结构化形态（重派前）。
  3. `project.rs` `equivalent_to` doc：同口径补 P2-1 定位句（in-crate structural form）。
  4. P3-1 rustdoc 补白：`mod.rs` 新增 Naming constraint 段——跨位置同名域需模板作者错名；同名同版异形域在 TypeRegistry 注册时报 IdentityCollision、同形去重（geo-consensus 平铺先例援引）。
  5. 顺手清零 template/** 六处 doc 链接警告（`tree_compose`/`project` 歧义/`CompositeSpec`/`ConflictReport`/`ConflictKind::ChainTooDeep` 改全路径）——其余 16 处 doc 警告均为既有模块（含 exchange 禁区），未触碰。
- 自检：tree-space **391 passed / 0 failed**（与返修前基线逐字节一致，纯 doc 无行为差）；`cargo fmt --all -- --check` 零 diff；template/** 七文件 nonASCII=0、无 BOM；`cargo doc` template 警告清零。
- 纪律：禁 commit；零 git 写操作；未触碰任何函数签名/实现/测试。
