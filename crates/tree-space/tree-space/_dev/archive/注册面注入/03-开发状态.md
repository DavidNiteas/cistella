# 注册面注入（B1）：03 开发状态

**工单：注册面注入（B1 kernel registry injection）**
**Status：✅ 已闭合——TS-B1-1、TS-B1-2 全关（2026-08-26 初审验收通过），B1 注册面注入工单完工归档**
**规范：设计与规范见 [`01-目标与设计.md`](01-目标与设计.md)；施工细则见 [`02-施工路线图.md`](02-施工路线图.md)；本文件为**状态真相**（里程碑 + golden + 遗留 + 偏离），过程记录在 [`04-施工日志.md`](04-施工日志.md)（随首段施工创建）。**

## 里程碑状态

| 里程碑 | 内容 | 状态 | 关闭日期 | 关闭证据 / 批注 |
|---|---|---|---|---|
| TS-B1-1 | 注入构造器 + 路由走字段 | ✅ 已关闭 | 2026-08-26 | 初审独立复跑五命令全绿（`-p tree-space` 21 单元 + 集成 + 3 新测试；rust-195 两条；workspace `--all-targets` 零失败零警告）；三处路由改造逐点核对（tb_library.rs `PluginRegistry::global()` 零残留，仅 materializer.rs:177 写侧红线段合法未动）；零红线（semantic.rs/registry.rs/materializer.rs/block/* diff 空）；M2 golden 原样；B1-T1/T2/T4 绿；diff 面 = 仅 tb_library.rs(+112/−23) |
| TS-B1-2 | 组合生命周期 + 闭合 | ✅ 已关闭 | 2026-08-26 | 初审独立复跑五命令全绿（workspace `--all-targets` 156 ok 零失败零警告）；B1-T3（同 Arc 贯穿 create/open three-编排 + project 全链 + 退化编排 + 两套注册面正交）/ T5（mzml-db/uni-mass-db 零迁移）/ T4+（红线 + M2 golden 原样）全绿；零 src 改动（纯测试+文档收口）；diff 面复核通过；**B1 系列全关** |

## golden / 行为断言登记

> 本工单**不新增、不改动**任何字节 golden（红线：`semantic.rs` 不动 / M2 身份 golden 零变动 / 既有测试零破坏）。本表只登记**回归断言**（既有的、本工单必须原样通过的锚）。

| 编号 | 断言 | 归属里程碑 | 状态 |
|---|---|---|---|
| R-1 | M2 身份 golden 测试（tree-space 既有测试集内身份字节冻结断言）**原样全绿** | TS-B1-1（B1-T4）/ TS-B1-2（B1-T4+） | ✅ 已复跑：`p_l_2_identity` 三测试（cross_encoding/goldens_refrozen/converter_hook）命令 2/3/5 三重复跑原样全绿，零 refreeze |
| R-2 | 默认构造等价（默认注入 `PluginRegistry::new()` == 改造前 `global()` 路由：同输入同输出） | TS-B1-1（B1-T1） | ✅ 已验收：`t_s_b1_1_default_equivalent`（receipt/verify/桶内容级等价，`plugin_registry()` 行为面逐项） |

## 遗留清单

> 遗留 = 已登记、未关闭的问题/风险。新发现 → 在此登记（编号递增）→ 关闭时划掉留痕。**TS-B1-2 复核（2026-08-26）：无遗留进入关闭裁定**——初审观察项（03 过渡态 / B1-T3 编排② project 断言容差 / B1-T5 守卫快照性 / 风格级）均不构成返工，随工单闭合登记。

## 偏离登记

> 任何实现偏离 01/02 的规范，施工方先在日志记「偏离申请」继续施工，验收裁定后回写 01 并在此登记（编号递增）。

**TS-B1-1 施工（2026-08-26）**：
1. `plugin_registry()` 访问器落位 `impl<L: TbLayout>` 泛型（02 §5.2/01 §4 画在 FlatDir impl）——**已裁定接受**：字段在泛型结构体、覆盖 single-file 面，01 §3.1 已回写落位说明；02 §7-3 本即 C 层可调，非实质偏离。
2. 测试命名归一：主管派遣时写 `t_s_b1_1_injected_effective`/`t_s_b1_1_redline_static`，与 02 §5.3 表（`t_s_b1_2_injected_effective`/`t_s_b1_4_redline_static`）不一致——**已裁定回 02 §5.3 权威命名**（改文件名 `t_s_b1_2_injected_effective`/`t_s_b1_4_redline_static`）。

### 软性前置登记（非偏离，工单间事实）

- **串行前置**：本工单开工 = 阶段 1 桥（`../tree-space-index/_dev/tree-space-index 桥/`，TS-I-1→3）全关后（A 层拍板串行顺序）——本工单 03 不依据该前置更新里程碑状态，仅在开工时核对。

## 演进史（压缩）

- **2026-08-26 立项**：决策期第 ④ 步落 01/02/03——目标（B1 注入式 registry：`TbLibrary` 持 `Arc<PluginRegistry>` + `create_with_registry`/`open_with_registry` + 引导链三处路由改走字段）；范围（最小改动面 = 仅 `src/tb_library.rs` 字段/构造器/三处路由；B2/内核查询入口永久否决）；红线（`semantic.rs` Named `byte_value` 不动 / M2 golden 零变动 / 既有测试零破坏 / `select_materializer` 签名不动 / `global()` LazyLock 不动）；公共 API（`create_with_registry`/`open_with_registry`/`plugin_registry()`）；默认 = `PluginRegistry::new()`（与 `global()` 内容等价 + B1-T1 断言）；串行前置 = 阶段 1 桥全关；联动 `设计路线.md` 新增「插件注册面注入（B1）」节点 `[构建中]`、`开发状态.md` 工单一览补行。
- **2026-08-26 TS-B1-1 验收关闭（初审）**：`registry: Arc<PluginRegistry>` 字段 + `create_with_registry`/`open_with_registry` 注入构造器 + 默认构造委托 `Arc::new(PluginRegistry::new())` + `plugin_registry()` 访问器（落位泛型 impl，偏离 #1 已裁定）+ 三处路由改走字段（`open_with` step2 `registry.tree_plugin` / `restore_bucket_from_refs` 增 registry 参数 / `verify` `self.registry.route_disk`）。初审独立复跑五命令全绿；零红线（semantic.rs:637 Named 分支字节未变 + select_materializer 签名未动 + M2 golden 原样 + `git diff --name-only src/plugin src/layout/materializer.rs src/block` 空）；B1-T1（默认等价）/T2（注入生效：empty→BootstrapIncomplete/DegradedBlock 可编排）/T4（红线静态）全绿；偏离 #2（测试命名）已裁定回 02 §5.3 权威命名（`t_s_b1_2_injected_effective`/`t_s_b1_4_redline_static`，函数名同步改）。TS-B1-2（组合生命周期+闭合）未开工。
- **2026-08-26 TS-B1-2 施工完成（待验收裁定，02 §4.1-3 干事不自行宣布关闭）**：新增 `tests/t_s_b1_2_injected_lifecycle.rs`（B1-T3）/ `tests/t_s_b1_4_redline_final.rs`（B1-T4+）/ `tests/t_s_b1_5_downstream_zero.rs`（B1-T5）+ 04 施工日志 TS-B1-2 节；**零 src 改动**（纯测试 + 文档收口）。B1-T3 三编排（new() 完整基面 / empty()+树插件退化编排 / 测试内自制 `BenchV2` RegisteredBlock 两套注册面正交）全绿——三编排 create/open 两端 `plugin_registry()` 与注入 Arc `Arc::ptr_eq`（同一 Arc 贯穿）、project 全链恢复字节一致、退化分支 verify 行数/分类一致、Named 帧经注入面 IPC 插件路由健康；B1-T4+ 红线复跑 + M2 golden 三测试函数名编译期清单 pin；B1-T5 两消费侧（mzml-db 14 + uni-mass-db 19 src 文件）零引用 B1 注入面 token 静态守护 + §3 命令 5 workspace 全量承载消费侧原样通过。改动后五命令全绿（fmt ✓ / `-p tree-space` 63 ok / rust-195 --all-targets 62 ok / rust-195 check 零警告 / workspace --all-targets 156 ok 零失败零警告）；M2 golden 零变动；`git diff --stat` tracked 空、红线文件零 diff、零新增依赖、无 git 写操作。关闭登记（02 §6.4）待验收方：TS-B1-2 关闭 → B1 系列全关 → 01/02/03 终态登记 + `设计路线.md` 注册面注入节点 `[构建中] → [已达成]` + `开发状态.md` 工单一览更新 → 工单闭合归档。
- **2026-08-26 TS-B1-2 验收关闭（初审），B1 系列全关、工单闭合**：初审独立复跑五命令全绿（workspace `--all-targets` 156 ok 零失败零警告）；B1-T3 同 Arc 贯穿（三编排 `Arc::ptr_eq` 两端）+ project 全链字节一致 + 退化编排分支按既有语义（healthy=0/degraded=2/verify=2）+ 两套注册面正交（进程级 `register_block` vs 注入面）；B1-T5 mzml-db/uni-mass-db 零迁移（三 token 零引用静态守护 + 命令 5 承载）；B1-T4+ 红线复跑（semantic.rs:637 字节未变 / select_materializer 签名未动）+ M2 golden 三测试原样；零 src 改动（TS-B1-2 纯测试+文档）。**TS-B1-1/TS-B1-2 全关 = B1 注册面注入工单完工归档**——阶段 2 收官，table-index 再设计（阶段 0 + 阶段 1 桥 + 阶段 2 B1）全链路闭合。