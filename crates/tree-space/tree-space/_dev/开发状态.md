# tree-space 开发状态（全局索引）

Status: 现行（薄索引）
Date: 2026-08-26（本次登记：注册面注入工单闭合归档）
规范：全局《工单模型》= skill `engineering-governance` 第一章（常驻薄指针 `work-order-model.md`）；本目录组织见 [`README.md`](README.md)。本文件只做导航与仓库级事实登记，**状态真相在各工单 `03-开发状态.md`，禁止双写**。
需求/目标层的连贯路线见 [`设计路线.md`](设计路线.md)。

## 工单一览

| 工单 | 状态 | 位置 | 一句话 |
|---|---|---|---|
| 树与桶管道 | 🟢 活跃（已立项） | [`./树与桶管道/`](树与桶管道/) | 接续树与桶模型改造：P-IO-1..7 已关闭（磁盘对象/GC/重开/verify/ephemeral/acceptance/解冻/磁盘布局〔统御+扁平+单文件〕）；P-SH（共享）/P-IO-8（sync+并发写）**已移交并完成于交换协议工单（E-2/E-3/E-4）**；P-PB（polars 桥）已关闭（独立 crate `tree-space-polars`，P-PB-1..4 全关，见其 `_dev/archive/`）；下游已解冻；层化目录独立工单挂起 |
| 注册面注入 | 📦 已归档（2026-08-26，TS-B1-1/2 全关闭合） | [`./archive/注册面注入/`](archive/注册面注入/) | B1 注入式 registry：TbLibrary 持 `Arc<PluginRegistry>` + `create_with_registry`/`open_with_registry` 注入构造器 + 引导链三处路由改走字段；`global()`/`select_materializer` 不动；B2/内核查询入口永久否决；TS-B1-1 注入构造器+路由走字段 → TS-B1-2 组合生命周期+闭合 |
| 模块化子树改造 | 📦 已归档（2026-08-31，M0 语义图纸 + M1 闭合） | [`./archive/模块化子树改造/`](archive/模块化子树改造/) | 编译期树模板组合系统（用户语义七条落纸）：候选c 混合载体（TemplateSpec const 化 + TreeTemplate trait + tree_compose! 宏）+ 正交/合并两模式 const-eval 编译期报错（compile_fail doc-test ×3 零新依赖）+ points_to 端点组合期检查 + project 投影 + 多位置派生（SlotName/mount/At 锚/as 角色标签）；P3/P4/P5 用户拍板；被 uni-mass-db MVP 重设计 S2 全量消费（七棵树模板 + MvpComposite） |
| 插件化改造 | 📦 已归档（2026-08-24，PL-1+PL-2 全关收口） | [`./archive/插件化改造/`](archive/插件化改造/) | PL-1（M1）S1–S8 + PL-2（M2）S1–S2 全关闭；版本化插件体系 + boot + 版本侧表 + 退化 + v4 废弃 + 身份语义化全部落地 |
| 交换协议 | 📦 已归档（2026-08-23，E-1..E-4 全关闭收口） | [`./archive/交换协议/`](archive/交换协议/) | 以「交换行为」为支点统一数据流动：块抽象（树=特殊块）+ 运行时配置系统 + 权限（所有权/代理）+ 访问形式（映射/全量/惰性）+ 同步（pull/push/merge）+ 转换器接口；只做 IPC，RPC 另开工单；承接并完成 P-SH / P-IO-8（E-2 权限/代理 + E-3 同步 + E-4 转换器接口/磁盘 mmap） |
| 数据验证系统 | 📦 已归档（2026-08-23，V-1..V-5 全关闭） | [`./archive/数据验证系统/`](archive/数据验证系统/) | 统一验证系统：L1 完整性必选 + L2 引用一致（verify 保留）+ L3 结构 opt-in；统一字典 ValidateDepth/报告/入口 validate + assert 族 |
| 层化目录 | ⏸ 挂起（2026-08-23） | [`./suspended/层化目录/`](suspended/层化目录/) | 树中心布局（第三种布局哲学），完全层化（每层自描述 schema + 节点目录 + 块文件）；三关系裁定（去重/身份/引用表去留）权威在管道工单 §1.11.8；解锁条件：P-IO-7 关闭或下游「目录即导航」需求 |
| 树与桶模型改造 | 📦 已归档（2026-08-22 终态闭合） | [`./archive/树与桶模型改造/`](archive/树与桶模型改造/) | 编码使命完成：TB（模型层）/AB（块 Arrow 化）/A-2（树对象 Arrow 化）全部闭合，golden 1–15 冻结；其 01 的铁律/块规范/树运行时契约/树序列化仍是现行规范基线（归档 ≠ 失效） |
| V4-磁盘协议 | 📦 已归档 | [`./archive/V4-磁盘协议/`](archive/V4-磁盘协议/) | 磁盘协议与基础设施全套（Phase A–G / M0–M12，2026-08-11 闭合）；协议字节仍现行——`DESIGN_VERSION = "v4"`，下游冻结依赖，树与桶工单声明零改动 |
| V5-树运行时 | 📦 已归档 | [`./archive/V5-树运行时/`](archive/V5-树运行时/) | Nested Tree Contract（N-1..N-10，2026-08-19 闭合）+ 树运行时契约；叶子层被树与桶改造重塑取代，契约全文已吸收进活跃工单 `01` §4 |

（挂起工单见 [`./suspended/`](suspended/)；当前无。）

## 仓库级事实

- **下游冻结**：已解除——mzml-db / uni-mass-db 均已迁至 TB 面（TB 迁移 2026-08-29 闭合）；v4 `Library` 面已删除（遗留卫生收尾工单 2026-08-31）。
- **零警告基线**（2026-08-20）：workspace `--all-targets` 编译零警告。
- **crate 改名**（2026-08-20）：`table-space` → `tree-space`（含 derive crate 与 `TreeSpaceError`/`TreeSpaceService`）；磁盘协议、golden 字节与数据兼容性不变。
- **环境遗留**：`dev-all-gpu` maturin/uv-cache build env 缺 pip 导致 cargo 无法启动——独立环境工单，等价验证以 `dev-rust` 为准。

## 演进史（压缩；细节见各工单与 git 历史）

- **v4**（2026-08-11 闭合）：三布局 content-addressed commit-tree、PreparedCommit 事务、FaultPlan、std 原生锁、digest golden、verify L1–L3、sync、transform、service 五动词、跨进程共享区域（双平台验证）。
- **v5**（2026-08-19 验收）：树运行时、叶子系统、XPathIndex、LeafStore、io 三驱动，175 tests。
- **TB**（2026-08-21 闭合）：块类型系统与树语义重塑（纯内存模型层）。
- **AB**（2026-08-22 闭合）：内建块 payload 全 canonical Arrow IPC + 块集合开放注册制（envelope v2、BlockRegistry、RegisteredBlock）。
- **A-2**（2026-08-22 验收闭合）：树对象规范序列化——平面条目表 canonical Arrow IPC、`tb-tree` 身份域、TreeImage 两级解码、`TreeCodec` derive；编码使命（TB/AB/A-2）全部完成。
- **工单终态闭合**（2026-08-22）：树与桶模型改造工单归档（`archive/树与桶模型改造/`）。
- **树与桶管道立项**（2026-08-22）：接续工单（P-IO 磁盘 IO / P-SH 共享 / P-PB polars 桥，层化目录后置）；磁盘模型核心决策定稿（envelope blob / 严格铁律 + manifest / 单真相 + 派生条目表 / commit 可选字段追加）。
- **工单模型重组**（2026-08-22）：`_dev` 按工单模型重组（规范见 [`README.md`](README.md)）。
- **注册面注入立项**（2026-08-26）：B1 工单立项（决策期第 ④ 步落 01/02/03，见 [`./archive/注册面注入/`](archive/注册面注入/)）；`设计路线.md` 新增「插件注册面注入（B1）」节点 `[构建中]`；串行前置 = 阶段 1 桥（`tree-space-index`，零内核）TS-I-1→3 全关（其 `_dev` 落位于 sibling crate，不在本目录）。
- **注册面注入闭合**（2026-08-26）：TS-B1-1（`registry` 字段 + 注入构造器 + 三处路由走字段）+ TS-B1-2（组合生命周期：同 Arc 贯穿 create/open + project 全链 + 退化编排 + 两套注册面正交 + 下游零迁移）全关闭合；workspace 156 ok 零失败零警告；零红线（semantic.rs Named 分支 / select_materializer / M2 golden 原样）；源码改动面 = 仅 `tb_library.rs`（TS-B1-1）→ 零 src（TS-B1-2）；工单归档 → `archive/注册面注入/`；`设计路线.md` 节点 `[构建中] → [已达成]`。
- **模块化子树改造关闭归档**（2026-08-31）：M0 语义图纸（载体三候选深比 + 组合算子 + 检查矩阵 + 多位置派生机制化）→ 用户拍板 P3=候选c/P4=读法A/P5=单向投影 → M1 施工（template/ 七文件 + tree_compose! + 五步检查管线 + project + 25 单测 + compile_fail ×3）→ 初审（语义七条逐条对位 + compile_fail E0080 独立复核 + TypeId provisional 裁定）→ 返修（纯文档）→ 经理终审。391 tests；零新依赖；物理面/实例层/exchange 零触碰。工单目录移入 `archive/`；消费 = uni-mass-db MVP 重设计 S2。
