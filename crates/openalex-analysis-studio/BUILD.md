# cistella ????

cistella ???????????`cistella-headless` ??????????????????????? CLI ???

## ????

?????????????

```powershell
pnpm build
```

???????? GUI bin??????????????

```powershell
cargo build --bin cistella
```

??/??? GUI ???? Tauri CLI ?????? `frontendDist` ????????

```powershell
pnpm exec tauri build --no-bundle
```

?? headless ?? bin?

```powershell
cargo build --bin cistella-headless
```

???????? Rust ??????

```powershell
cargo test -p cistella-desktop --lib
cargo test --bin cistella-headless
```

## Release ? bin_test

???????? `crates/openalex-analysis-studio/package.json`?

```powershell
pnpm build:release
```

????

```powershell
node scripts/build-release.js
```

?????

1. ?? `pnpm build` ?????
2. ?? `pnpm exec tauri build --no-bundle` ?????????? GUI ??????
3. ?? `cargo build --release --bin cistella-headless` ?? headless ???
4. ? `target/release/cistella.exe` ? `target/release/cistella-headless.exe` ??????? `bin_test/`?
5. ?? `bin_test/cistella-portable/` marker ???
6. ?????? `make-portable.js` ?? portable zip?

`bin_test/` ????????????? `.gitignore` ???

## Headless ????

??? headless bin?

```powershell
cargo build --bin cistella-headless
```

????

```powershell
pnpm test:headless
```

??

```powershell
node scripts/headless-smoke-test.js
```

????????????? Vault??? `doctor`?`vault open`?`literature list`?`reading sessions`?`notes list`?`settings info`?`search` ????????`source rank` ???? sources ????????? Vault ????????????

## Tauri ??/??

?????

```powershell
pnpm tauri dev
```

Tauri ??????

```powershell
pnpm tauri build
```

`crates/openalex-analysis-studio` ??????????????????????????? cistella?OpenAlex ???????????????????????????????
